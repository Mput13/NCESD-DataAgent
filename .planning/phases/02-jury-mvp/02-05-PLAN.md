---
phase: 02-jury-mvp
plan: "05"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-04"
files_modified:
  - app/workflow/state.py
  - app/workflow/graph.py
  - app/workflow/service.py
  - app/workflow/run_graph.py
  - tests/test_phase2_workflow_service.py
autonomous: true
requirements:
  - NLU-01
  - NLU-02
  - NLU-03
  - NLU-04
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - ART-01
  - ART-02
  - ART-03
  - ART-04
  - ART-05
  - ART-06
  - RBST-01
  - RBST-02
  - RBST-03
  - RBST-04
  - ENG-02
  - ENG-03
must_haves:
  truths:
    - "A user query runs through one product workflow service used by CLI, evals, and Streamlit."
    - "The graph exposes Supervisor, Intent Analyst, Research Designer/direct path, scouts, coverage, extraction planner, deterministic tools, critic, visualization, and narrator states."
    - "Clarification and not-found routes terminate honestly instead of producing fake passed answers."
  artifacts:
    - "app/workflow/state.py defines the Phase 2 typed state."
    - "app/workflow/graph.py builds a LangGraph StateGraph."
    - "app/workflow/service.py implements run_user_query."
    - "app/workflow/run_graph.py delegates to the service."
  key_links:
    - "service.py calls graph.py."
    - "graph.py calls app.workflow.nodes.* from plan 02-04."
    - "run_graph.py and Streamlit/evals use service.py, not separate logic."
---

<objective>
Build the real Phase 2 workflow runtime and service entrypoint over the previously defined contracts and nodes.

Purpose: replace the Phase 1 smoke graph per D-11/D-12/D-13/D-14/D-15A while preserving trace and source-bound invariants.
Output: LangGraph-backed `run_user_query` and updated CLI path.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@.planning/phases/02-jury-mvp/remote-workstream-review.md
@app/workflow/service.py
@app/workflow/graph_contract.py
@app/workflow/run_graph.py
@app/workflow/nodes/scouts.py
@app/workflow/nodes/coverage.py
@app/workflow/nodes/extraction_planner.py
@app/llm/yandex_ai_studio.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 2 graph state and intent service</name>
  <files>app/workflow/state.py, tests/test_phase2_workflow_service.py</files>
  <read_first>app/workflow/graph_contract.py, app/artifacts/workflow_artifacts.py, app/llm/yandex_ai_studio.py</read_first>
  <behavior>
    - Phase 2 state includes `run_id`, `query`, `intent`, `research_design`, `evidence`, `coverage_reports`, `extraction_plan`, `dataset_artifacts`, `final_outcome`, `trace_events`, and `component_statuses`.
    - Ambiguous golden cases GC-001, GC-006, GC-009, and GC-010 set `needs_clarification=True`.
    - Qwen/Yandex structured output is the target path; deterministic classifier fallback is explicitly marked `test_only`.
  </behavior>
  <action>Create `app/workflow/state.py` with `Phase2State(TypedDict, total=False)` containing the fields above and helper `new_run_id() -> str` returning prefix `phase2-`. Add `class IntentAnalysisResult(BaseModel)` if needed, but reuse `IntentFrame` as the durable artifact. Implement `analyze_intent(query: str, *, live_llm_required: bool) -> IntentFrame`: target path calls `YandexAIStudioClient.structured_chat` using the verified `app.llm.yandex_ai_studio` client; tests may monkeypatch it. If `live_llm_required=False`, allow deterministic fallback with `open_reasoning` containing `test_only_intent_fallback`. Never return a fake live Qwen success when credentials are missing per D-37/D-38.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_service.py tests/test_yandex_ai_studio.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/state.py` contains `class Phase2State`; contains `phase2-`; contains `test_only_intent_fallback`; tests monkeypatch `YandexAIStudioClient`.</acceptance_criteria>
  <done>Graph state and intent analysis have a real-Qwen target path and explicit test fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build LangGraph workflow and service implementation</name>
  <files>app/workflow/graph.py, app/workflow/service.py, tests/test_phase2_workflow_service.py</files>
  <read_first>app/workflow/service.py, app/workflow/state.py, app/workflow/nodes/scouts.py, app/workflow/nodes/coverage.py, app/workflow/nodes/extraction_planner.py, app/artifacts/workflow_artifacts.py</read_first>
  <behavior>
    - `build_phase2_graph()` returns a compiled LangGraph graph with named nodes.
    - `run_user_query()` returns `WorkflowResponse`, not `NotImplementedError`.
    - Component statuses may be internal, but final outcomes are only `passed`, `needs_clarification`, or `not_found`.
  </behavior>
  <action>Create `app/workflow/graph.py` using `langgraph.graph.StateGraph`, `START`, and `END`. Add nodes named exactly `supervisor`, `intent_analyst`, `research_designer`, `source_scouts`, `coverage_schema`, `extraction_planner`, `deterministic_tools`, `methodology_critic`, `visualization`, and `narrator`. Implement conditional routing: ambiguous intent -> `narrator` with final outcome `needs_clarification`; no selected sources after bounded search -> `narrator` with `not_found`; otherwise coverage -> extraction -> critic -> visualization -> narrator. Update `app/workflow/service.py` so `run_user_query` builds/invokes this graph, writes run artifacts under `WorkflowRunConfig.artifact_dir`, and returns `WorkflowResponse`.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_service.py tests/test_phase2_workflow_nodes.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/graph.py` contains `StateGraph`; contains all ten node names; `app/workflow/service.py` no longer contains `NotImplementedError("Phase 2 graph implementation is provided by plan 02-05")`.</acceptance_criteria>
  <done>The product workflow has one executable graph path for CLI/eval/UI callers.</done>
</task>

<task type="auto">
  <name>Task 3: Update graph CLI to use the service path</name>
  <files>app/workflow/run_graph.py, tests/test_phase2_workflow_service.py</files>
  <read_first>app/workflow/run_graph.py, app/workflow/service.py, .planning/phases/01-data-architecture-research/golden-cases.yaml</read_first>
  <action>Refactor `app/workflow/run_graph.py` so the CLI supports both `--case-index` and `--query`. It must load a golden case when `--case-index` is provided, call `run_user_query(query, run_config=WorkflowRunConfig.default().model_copy(update={"case_id": case_id}))`, and write the serialized `WorkflowResponse` JSON to `--json-output`. Preserve module invocation `python3 -m app.workflow.run_graph`; do not keep a separate Phase 1-only success path for user queries.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_service.py tests/test_workflow_graph.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/run_graph.py` contains `run_user_query`; parser contains `--query`; output JSON contains `final_outcome` in tests.</acceptance_criteria>
  <done>Workflow CLI, future evals, and future Streamlit all share the same service execution path.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_phase2_workflow_service.py tests/test_phase2_workflow_nodes.py tests/test_workflow_graph.py -q`.
</verification>

<success_criteria>
The Phase 2 service can execute the architecture-stack workflow roles and return strict response outcomes through one shared path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-05-SUMMARY.md`.
</output>
