---
phase: 02-jury-mvp
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-10T17:44:09.104Z
---

<current_state>
Paused during Phase 2 post-execution/manual-UAT hotfixing on branch `codex/phase-2-jury-mvp-planning`.

The remote web frontend has been pulled (`b66e66a Add DataAgent web frontend`) and is the current manual-test surface at `http://localhost:8787`.

The session focused on defects found while manually testing the frontend and workflow:
- the old Streamlit/translit examples were replaced with Russian prompts;
- a no-UI workflow runner was added at `scripts/run_workflow_query.py`;
- `not_found` WorkflowResponse validation was fixed so `not_found_evidence` is always present;
- preflight clarification stopped asking for clarification on specific questions like `Какой ВВП России в 2024 году?`;
- lightweight retrieval cards are now hydrated from the Phase 1 SQLite source catalog;
- FedStat/World Bank adapters can extract parquet files from the local Phase 1 zip archives;
- FedStat wide-table years such as `2024.0` normalize to `2024`;
- `Russia` now matches `Российская Федерация` rows in FedStat;
- extraction planning prefers a covered source with the requested period, not merely the first ok source;
- local fallback narrator now reports concrete dataset record values;
- the new web frontend renders `answer_blocks` and a dataset row preview instead of showing only sources.

The important real workflow checks now pass:
- `Какой ВВП России в 2024 году?` reaches `outcome: passed`, source `57395`, and a non-empty DatasetArtifact with 2024 value `1376477.9`.
- `Главные факторы, повлиявшие на ВВП России в 2012 году` reaches `outcome: passed`, source `40579`, and the frontend displays `Российская Федерация, 2012: 24274.0 99.43.4 доллар США` plus a limitation that local fallback did not prove causal factors.
</current_state>

<completed_work>

- Remote frontend pull - Done. `git pull --ff-only` brought in `b66e66a Add DataAgent web frontend`.
- Frontend launch path - Done. Use `PYTHONPATH=. python3 -m app.web.server --port 8787`, then open `http://127.0.0.1:8787/`.
- CLI runner - Done. `scripts/run_workflow_query.py` can run a complete query workflow without using the UI.
- Preflight clarification fix - Done. Specific GDP Russia/year queries no longer become pointless clarification requests.
- Source hydration and zip-backed extraction - Done. Coverage/extraction can read real FedStat parquet from local archives via hydrated source cards.
- FedStat filtering fixes - Done. Periods like `2024.0` normalize; Russia aliases match Russian FedStat geography rows; source-level indicator filters no longer delete all rows when FedStat has no indicator column.
- Extraction planner source choice - Done. Planner selects a report with the requested period when possible.
- Narrator/frontend fallback readability - Done. Local fallback reports concrete record values and frontend renders answer blocks and dataset rows.
- Verification - Done. Latest expanded test run: `PYTHONPATH=. python3 -m pytest tests/test_phase2_extraction_adapters.py tests/test_phase2_workflow_nodes.py::TestRunCoveragePreview tests/test_phase2_workflow_service.py tests/test_phase2_finalization.py tests/test_web_frontend.py -q` -> `93 passed, 1 warning`.
- Browser check - Done. In-app browser confirmed the new UI shows answer text, methodology/limitations, and dataset rows.
</completed_work>

<remaining_work>

- Decide commit scope. The dirty tree has real code/docs/test fixes plus many generated run directories. Do not blindly commit all untracked `.planning/phases/02-jury-mvp/workflow-runs/phase2-*`.
- Continue manual UAT over the remaining golden and realistic cases in the new web frontend.
- Improve factor/causal questions beyond local fallback. Current behavior is honest but shallow: it extracts numeric rows and warns that causal drivers are not proven.
- Decide whether generated successful runs should be promoted as evidence artifacts in `.planning` or left untracked.
- Run full phase-level verification after commit scope is settled.
- Existing placeholder/TBD scan found older files with placeholder markers:
  - `.planning/phases/01-data-architecture-research/01-01-SUMMARY.md`
  - `.planning/phases/01-data-architecture-research/01-04-SUMMARY.md`
  - `.planning/phases/01-data-architecture-research/langgraph-contract.md`
  - `.planning/phases/02-jury-mvp/02-03-SUMMARY.md`
  - `.planning/phases/02-jury-mvp/02-10-PLAN.md`
  - `.planning/phases/02-jury-mvp/02-10-SUMMARY.md`
</remaining_work>

<decisions_made>

- Use the new `app.web.server` frontend on port `8787` for current manual testing.
- Keep Streamlit as a diagnostic/legacy surface; current frontend defect reports refer to `app/web/static/js/app.js` and `app/web/server.py`.
- Hydrate retrieved source-card snippets from `.local/dataagent/phase1/source-catalog.sqlite` before coverage/extraction because retrieval cards do not include enough local path metadata.
- Treat local fallback narrator as deterministic and honest, not as a real analytical narrator. It may summarize DatasetArtifact records, but it must not invent causal/factor explanations.
- For extraction planning, prefer sources whose coverage includes the requested period. This fixed the GDP Russia 2024 path by choosing `57395` instead of earlier GDP/PPS cards without 2024 coverage.
</decisions_made>

<blockers>

- Manual UAT is still pending human judgement. The user was actively testing the frontend.
- Many generated run artifacts are untracked. They need a deliberate keep/discard decision before any broad commit.
- Local fallback does not answer factor/driver questions deeply. It now says this explicitly, but a jury-quality answer needs a real narrator/factor route or deterministic supporting indicators.
</blockers>

<context>
Current server process at pause time:
- `Python` PID `40588` listening on `127.0.0.1:8787`.

Expected local runtime:
- `.env` should point to `QDRANT_URL=http://localhost:6333` and `QDRANT_COLLECTION=phase1_source_cards`.
- Qdrant collection previously verified as `phase1_source_cards` with `36321` points.

Dirty tracked files at pause time:
- `.planning/phases/01-data-architecture-research/golden-cases.yaml`
- `.planning/phases/02-jury-mvp/golden-coverage-matrix.json`
- `.planning/phases/02-jury-mvp/manual-uat.md`
- `README.md`
- `app/data/fedstat_adapter.py`
- `app/data/world_bank_adapter.py`
- `app/ui/streamlit_app.py`
- `app/web/static/js/app.js`
- `app/workflow/nodes/coverage.py`
- `app/workflow/nodes/deterministic_tools.py`
- `app/workflow/nodes/extraction_planner.py`
- `app/workflow/nodes/narrator.py`
- `app/workflow/service.py`
- `app/workflow/state.py`
- `tests/test_phase2_extraction_adapters.py`
- `tests/test_phase2_finalization.py`
- `tests/test_phase2_workflow_service.py`

Important untracked files:
- `app/data/source_card_lookup.py`
- `scripts/run_workflow_query.py`
- `.planning/phases/02-jury-mvp/manual-runs/`
- many `.planning/phases/02-jury-mvp/workflow-runs/phase2-*`
</context>

<next_action>
Start with: run `git status --short`, inspect/stage only the intended code/docs/test fixes, leave generated run directories uncommitted unless deliberately promoted, then continue manual UAT from `http://localhost:8787`.
</next_action>
