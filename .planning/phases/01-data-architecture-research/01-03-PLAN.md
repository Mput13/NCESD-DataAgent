---
phase: 01-data-architecture-research
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/retrieval/embedding_index.py
  - app/retrieval/hybrid_retrieval.py
  - scripts/build_embedding_index.py
  - scripts/run_retrieval_spike.py
  - .planning/phases/01-data-architecture-research/embedding-index-build.md
  - .planning/phases/01-data-architecture-research/embedding-index-manifest.json
  - .planning/phases/01-data-architecture-research/retrieval-comparison.md
  - .planning/phases/01-data-architecture-research/retrieval-eval.csv
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - NLU-04
must_haves:
  truths:
    - "The phase uses Qdrant as the mandatory vector-store abstraction and materializes the source-card embedding/search index as a durable Qdrant collection when embeddings are available."
    - "When embedding credentials are absent, only vector population is gated: the manifest still records Qdrant mode/path or URL, collection name, missing env vars, and dense retrieval status without substituting a custom local vector index."
    - "The index manifest records provider/model URI, document/query split, dimensions, chunk count, corpus hash, metadata version, Qdrant mode/path or URL, collection name, local artifact paths, and rebuild command."
    - "Retrieval outputs are scored against the golden set and include lexical BM25/FTS, dense, bge-reranker-compatible rerank/fallback status, rejection reasons, and metadata-rich source cards."
    - "The retrieval interface uses Qdrant now; local persistent mode is allowed, but a throwaway custom vector index is not."
    - "Verification runs the index builder and retrieval spike; marker-only checks are insufficient for this plan."
  artifacts:
    - path: "app/retrieval/embedding_index.py"
      provides: "Embedding provider and Qdrant vector store interfaces"
    - path: "app/retrieval/hybrid_retrieval.py"
      provides: "Hybrid retrieval over the prepared corpus/index"
    - path: "scripts/build_embedding_index.py"
      provides: "Long-running embedding/index build entrypoint"
    - path: ".planning/phases/01-data-architecture-research/embedding-index-build.md"
      provides: "Embedding/index build log, credential status, and operational notes"
    - path: ".planning/phases/01-data-architecture-research/embedding-index-manifest.json"
      provides: "Committed manifest for the local prepared index artifact"
    - path: ".planning/phases/01-data-architecture-research/retrieval-comparison.md"
      provides: "Trade-off and fallback evidence for lexical, dense, and rerank"
    - path: ".planning/phases/01-data-architecture-research/retrieval-eval.csv"
      provides: "Case-by-case retrieval evidence"
  key_links:
    - from: "scripts/build_embedding_index.py"
      to: ".planning/phases/01-data-architecture-research/embedding-index-manifest.json"
      via: "Materialized index manifest"
      pattern: "provider|model_uri|chunk_count|dimensions|corpus_hash|metadata_version"
    - from: "scripts/build_embedding_index.py"
      to: "app/retrieval/embedding_index.py"
      via: "Embedding provider and index writer imports"
      pattern: "embedding_index"
    - from: "scripts/run_retrieval_spike.py"
      to: "app/retrieval/hybrid_retrieval.py"
      via: "Retrieval spike runner"
      pattern: "hybrid_retrieval"
---

<objective>
Materialize the prepared source-card embedding/search index and validate hybrid retrieval over it.

Purpose: Satisfy D-06 through D-08E with a durable prepared data/index artifact, not a throwaway dense-retrieval spike.
Output: embedding provider/index module, long-running index builder, embedding-index manifest/build log, hybrid retrieval module, retrieval spike runner, comparison artifact, and retrieval eval CSV.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/phases/01-data-architecture-research/01-01-PLAN.md
@.planning/phases/01-data-architecture-research/01-02-PLAN.md
@.planning/phases/01-data-architecture-research/golden-cases.yaml
@app/artifacts/source_cards.py
@app/data/source_card_builders.py
@.planning/phases/01-data-architecture-research/embedding-corpus-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the embedding provider and materialized index builder</name>
  <files>app/retrieval/embedding_index.py, scripts/build_embedding_index.py, .planning/phases/01-data-architecture-research/embedding-index-build.md, .planning/phases/01-data-architecture-research/embedding-index-manifest.json</files>
  <read_first>app/retrieval/embedding_index.py, scripts/build_embedding_index.py, .planning/phases/01-data-architecture-research/embedding-index-build.md, .planning/phases/01-data-architecture-research/embedding-index-manifest.json, .planning/phases/01-data-architecture-research/embedding-corpus-contract.md, app/artifacts/source_cards.py, scripts/build_embedding_corpus.py, .planning/phases/01-data-architecture-research/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: The builder reads the embedding corpus from Plan 01-02 and writes a manifest with provider/model URI, document/query split, dimensions, chunk count, corpus hash, metadata version, Qdrant mode/path or URL, collection name, local artifact paths, and rebuild command.
    - Test 2: Yandex AI Studio embeddings use `YANDEX_EMBEDDING_DOC_MODEL=emb://<folder_id>/text-search-doc/latest` for corpus chunks, `YANDEX_EMBEDDING_QUERY_MODEL=emb://<folder_id>/text-search-query/latest` for query vectors, and `YANDEX_EMBEDDING_DIMENSIONS=256` in the manifest.
    - Test 3: Missing credentials create an explicit gated-skip manifest/build log, not a silent pass or a fake dense index.
  </behavior>
  <action>Create `app/retrieval/embedding_index.py` with interfaces for Yandex embedding requests, credential checks, Qdrant collection creation/upsert/search, cosine distance, and a gated-skip status object. Use `qdrant-client` and support `QDRANT_MODE=local` with `QDRANT_PATH=.local/qdrant` as the default Phase 1 mode; if `QDRANT_URL` is provided, support a remote/server Qdrant without changing retrieval code. Create `scripts/build_embedding_index.py` as the long-running build entrypoint: it must load the Plan 01-02 embedding corpus manifest, initialize the configured Qdrant client/collection abstraction, call Yandex embeddings when credentials are available, upsert vectors into the Qdrant collection, persist Qdrant local artifacts outside git, and write `embedding-index-manifest.json` plus `embedding-index-build.md`. The manifest must be committed but Qdrant storage/vector artifacts must stay local/ignored. If credentials are absent, write a manifest with `status: gated_skip`, exact missing env vars, Qdrant mode/path or URL, collection name, and the same expected artifact schema; do not create an alternate custom vector index. Read model settings from `YANDEX_EMBEDDING_DOC_MODEL`, `YANDEX_EMBEDDING_QUERY_MODEL`, and `YANDEX_EMBEDDING_DIMENSIONS`; do not hardcode a different embedding model.</action>
  <acceptance_criteria>`rg -n "qdrant_client|QDRANT_MODE|QDRANT_PATH|QDRANT_COLLECTION|YANDEX_EMBEDDING_DOC_MODEL|YANDEX_EMBEDDING_QUERY_MODEL|YANDEX_EMBEDDING_DIMENSIONS|text-search-doc|text-search-query|gated_skip|chunk_count|corpus_hash|metadata_version|collection_name" app/retrieval/embedding_index.py scripts/build_embedding_index.py .planning/phases/01-data-architecture-research/embedding-index-build.md .planning/phases/01-data-architecture-research/embedding-index-manifest.json` returns required markers, `python scripts/build_embedding_index.py ...` writes a valid manifest, and manifest validation proves either a ready Qdrant collection with vector count or an explicit credential gate with Qdrant config preserved.</acceptance_criteria>
  <verify>
    <automated>rg -n "qdrant_client|QDRANT_MODE|QDRANT_PATH|QDRANT_COLLECTION|YANDEX_EMBEDDING_DOC_MODEL|YANDEX_EMBEDDING_QUERY_MODEL|YANDEX_EMBEDDING_DIMENSIONS|text-search-doc|text-search-query|gated_skip|chunk_count|corpus_hash|metadata_version|collection_name" app/retrieval/embedding_index.py scripts/build_embedding_index.py && python scripts/build_embedding_index.py --corpus-manifest .planning/phases/01-data-architecture-research/embedding-corpus-manifest.json --manifest .planning/phases/01-data-architecture-research/embedding-index-manifest.json --build-log .planning/phases/01-data-architecture-research/embedding-index-build.md && python - <<'PY'
import json
from pathlib import Path
m = json.loads(Path('.planning/phases/01-data-architecture-research/embedding-index-manifest.json').read_text())
assert m.get('vector_store') == 'qdrant'
assert m.get('collection_name')
assert m.get('qdrant_mode') in {'local', 'remote'}
assert m.get('chunk_count', 0) > 0
assert m.get('corpus_hash')
status = m.get('status')
assert status in {'ready', 'gated_skip'}
if status == 'ready':
    assert m.get('vector_count') == m.get('chunk_count')
    assert m.get('qdrant_collection_status') == 'ready'
else:
    assert m.get('missing_env_vars')
    assert m.get('dense_status') == 'gated_skip'
print(status, m['collection_name'])
PY</automated>
  </verify>
  <done>The repo contains a materialized Qdrant embedding/search collection build path and a durable manifest, or explicit gated-skip evidence with the same contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hybrid retrieval over the prepared index and evaluate it</name>
  <files>app/retrieval/hybrid_retrieval.py, scripts/run_retrieval_spike.py, .planning/phases/01-data-architecture-research/retrieval-comparison.md, .planning/phases/01-data-architecture-research/retrieval-eval.csv</files>
  <read_first>app/retrieval/hybrid_retrieval.py, scripts/run_retrieval_spike.py, .planning/phases/01-data-architecture-research/retrieval-comparison.md, .planning/phases/01-data-architecture-research/retrieval-eval.csv, .planning/phases/01-data-architecture-research/embedding-index-manifest.json, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/phases/01-data-architecture-research/golden-cases.yaml, app/artifacts/source_cards.py, app/data/source_card_builders.py, app/retrieval/embedding_index.py</read_first>
  <behavior>
    - Test 1: Lexical retrieval returns source cards with exact/title/code and RU/EN keyword evidence using BM25/FTS or an explicitly documented local approximation.
    - Test 2: Dense retrieval reads the prepared Qdrant collection and manifest when available, or records the gated-skip status without silently dropping the dense requirement.
    - Test 3: Rerank decisions use a bge-reranker-v2-m3-compatible interface or a documented gated fallback, and rejected candidates are captured in the eval CSV and comparison report.
  </behavior>
  <action>Create `app/retrieval/hybrid_retrieval.py` with clear interfaces for lexical BM25/FTS retrieval, dense retrieval over the prepared Qdrant collection, bge-reranker-v2-m3-compatible reranking, and fallback status when embeddings or the reranker are gated. Create `scripts/run_retrieval_spike.py` that evaluates a bounded subset of the golden cases and writes `retrieval-eval.csv`; the scored output must prioritize whether the top candidates are relevant to the user's query, whether weak candidates are rejected with reasons, and whether source family coverage matches the golden expectations. Write `retrieval-comparison.md` with an explicit comparison table for lexical BM25/FTS, Qdrant dense collection, rerank path, local-vs-remote Qdrant config, and exact skipped-evidence behavior when credentials/index artifacts are unavailable. The comparison must keep the full target stack per D-06 rather than declaring dense retrieval optional.</action>
  <acceptance_criteria>`rg -n "lexical|BM25|FTS|dense|bge-reranker-v2-m3|rerank|Qdrant|collection|credential-aware fallback|prepared index|relevance_score|source_family_match" .planning/phases/01-data-architecture-research/retrieval-comparison.md app/retrieval/hybrid_retrieval.py scripts/run_retrieval_spike.py` returns all concepts, `python scripts/run_retrieval_spike.py ...` writes `retrieval-eval.csv`, and the CSV includes relevance/source-family/rejection fields with at least one evaluated row.</acceptance_criteria>
  <verify>
    <automated>rg -n "lexical|BM25|FTS|dense|bge-reranker-v2-m3|rerank|Qdrant|collection|credential-aware fallback|prepared index|relevance_score|source_family_match" .planning/phases/01-data-architecture-research/retrieval-comparison.md app/retrieval/hybrid_retrieval.py scripts/run_retrieval_spike.py && python scripts/run_retrieval_spike.py --goldens .planning/phases/01-data-architecture-research/golden-cases.yaml --index-manifest .planning/phases/01-data-architecture-research/embedding-index-manifest.json --output .planning/phases/01-data-architecture-research/retrieval-eval.csv && python - <<'PY'
import csv
from pathlib import Path
rows=list(csv.DictReader(Path('.planning/phases/01-data-architecture-research/retrieval-eval.csv').open()))
assert rows
cols=set(rows[0])
need={'case_id','retrieval_mode','top_candidate','relevance_score','source_family_match','rejection_reasons','dense_status','rerank_status','index_manifest_status','qdrant_collection'}
assert need.issubset(cols)
assert all(row['qdrant_collection'] for row in rows)
print(len(rows))
PY</automated>
  </verify>
  <done>The repo contains hybrid retrieval over the prepared index contract with explicit evidence for lexical, dense, rerank, and fallback behavior.</done>
</task>

</tasks>

<verification>
Check that the embedding/index manifest exists, records real Qdrant collection build or gated-skip evidence, and retrieval evidence covers lexical BM25/FTS, dense Qdrant search, bge-reranker-compatible rerank, local/server Qdrant config, and fallback states over the prepared index contract.
</verification>

<success_criteria>
The phase contains a durable prepared Qdrant embedding/search collection path and executable evidence for the chosen retrieval stack, not just architecture prose, with deterministic no-data and rejection handling represented explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-03-SUMMARY.md`
</output>
