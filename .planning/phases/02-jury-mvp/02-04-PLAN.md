---
phase: 02-jury-mvp
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - app/data/ckan_adapter.py
  - app/workflow/nodes/scouts.py
  - app/workflow/nodes/coverage.py
  - app/workflow/nodes/extraction_planner.py
  - tests/test_phase2_workflow_nodes.py
autonomous: true
requirements:
  - NLU-02
  - SRCH-03
  - SRCH-04
  - DATA-03
  - DATA-04
  - DATA-05
  - ART-01
  - ART-02
  - ART-03
  - ART-04
  - ART-05
  - ART-06
  - RBST-01
  - RBST-03
  - ENG-03
must_haves:
  truths:
    - "FedStat, World Bank, and CKAN scouts are first-class source paths."
    - "Promoted CKAN resources with bounded supported formats can be extracted deterministically, not only described as metadata."
    - "Coverage preview runs before extraction planning and records periods, geography, unit, frequency, and risks."
    - "Extraction planner emits safe structured operations rather than free-form LLM SQL."
  artifacts:
    - "app/data/ckan_adapter.py compresses, promotes, previews, and extracts supported CKAN resources."
    - "app/workflow/nodes/scouts.py returns selected and rejected source cards."
    - "app/workflow/nodes/coverage.py and extraction_planner.py produce typed workflow artifacts."
  key_links:
    - "scouts.py calls HybridRetriever and ckan_adapter; it does not duplicate retrieval internals."
    - "coverage.py calls fedstat_adapter/world_bank_adapter/ckan_adapter before extraction_planner.py runs."
    - "extraction_planner.py can route CKAN extraction plans to extract_ckan_dataset for supported promoted resources."
---

<objective>
Implement source scout, CKAN promotion, coverage, and extraction-planning nodes that the graph can call.

Purpose: honor D-16, D-17, D-21, D-22, D-23, and D-25 with reusable node functions instead of UI/eval-specific code.
Output: workflow node modules for scouts, coverage, and safe extraction planning.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@.planning/phases/02-jury-mvp/remote-workstream-review.md
@app/retrieval/hybrid_retrieval.py
@app/data/deterministic_tools.py
@app/data/fedstat_adapter.py
@app/data/world_bank_adapter.py
@app/artifacts/workflow_artifacts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded CKAN promotion and extraction adapter</name>
  <files>app/data/ckan_adapter.py, tests/test_phase2_workflow_nodes.py</files>
  <read_first>app/data/deterministic_tools.py, app/data/source_card_builders.py, .planning/codebase/INTEGRATIONS.md</read_first>
  <behavior>
    - `search_ckan_source_cards("57319", rows=5)` calls only CKAN `package_search`.
    - `promote_ckan_package(package_id)` calls only CKAN `package_show` for promoted candidates.
    - Raw CKAN payloads are compressed into source-card dictionaries with no oversized JSON dump in LLM-facing fields.
    - `extract_ckan_dataset(...)` extracts promoted `csv`, `csv.gz`, and `parquet` resources through deterministic code, while unsupported formats return explicit no-data/unsupported evidence.
  </behavior>
  <action>Create `app/data/ckan_adapter.py` with `search_ckan_source_cards(query: str, *, rows: int = 5) -> list[dict[str, Any]]`, `promote_ckan_package(package_id: str, *, cache_dir: Path = Path(".local/dataagent/phase2/ckan-cache")) -> dict[str, Any]`, `preview_ckan_coverage(promoted: dict[str, Any]) -> CoverageReport`, and `extract_ckan_dataset(promoted: dict[str, Any], *, resource_id: str, filters: dict[str, Any], output_dir: Path, artifact_id: str) -> DatasetArtifact | NoDataExplanationArtifact`. Use `ckan_package_search` and `ckan_package_show` from `app.data.deterministic_tools`. Cap search rows at 5 and promoted package resources at 20. Include compressed fields `source_family`, `dataset_id`, `resource_id`, `title`, `formats`, `provenance_url`, `why_matched`, `risk_flags`, and `resource_count`. Support only bounded deterministic formats `csv`, `csv.gz`, and `parquet` for extraction; cache downloaded promoted resources under `.local/dataagent/phase2/ckan-cache`; apply filters through DuckDB/PyArrow; export canonical dataset columns matching FedStat/World Bank adapters. For unsupported resource formats such as raw `xls`, `zip` without a promoted CSV/Parquet member, HTML, or PDF, return `NoDataExplanationArtifact` with reason `ckan_resource_format_not_supported_for_deterministic_extraction`. Cache promoted metadata JSON under `.local/dataagent/phase2/ckan-cache` but do not commit it.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_nodes.py -q</automated></verify>
  <acceptance_criteria>`app/data/ckan_adapter.py` contains `rows = min(rows, 5)` or equivalent cap; contains `def extract_ckan_dataset`; contains `ckan_resource_format_not_supported_for_deterministic_extraction`; tests monkeypatch CKAN calls and assert `resource_count` and supported CSV extraction.</acceptance_criteria>
  <done>CKAN remains bounded trusted catalog access and has a deterministic extraction path for promoted supported resources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement source scout and coverage nodes</name>
  <files>app/workflow/nodes/scouts.py, app/workflow/nodes/coverage.py, tests/test_phase2_workflow_nodes.py</files>
  <read_first>app/workflow/graph_contract.py, app/retrieval/hybrid_retrieval.py, app/data/fedstat_adapter.py, app/data/world_bank_adapter.py, app/data/ckan_adapter.py</read_first>
  <behavior>
    - Scout node returns `EvidenceBundleArtifact` with selected and rejected sources.
    - Coverage node returns at least one `CoverageReport` before extraction planning.
    - Ambiguous cases return selected source-card previews without numeric extraction.
  </behavior>
  <action>Create package directory `app/workflow/nodes/` with `__init__.py`. In `scouts.py`, implement `run_source_scouts(query: str, *, expected_sources: list[str], index_manifest_path: Path) -> EvidenceBundleArtifact` using `HybridRetriever.search` and CKAN bounded discovery when `CKAN` is expected or query contains `ЕМИСС`, `НЦСЭД`, `CKAN`, or an indicator code like `57319`. In `coverage.py`, implement `run_coverage_preview(evidence: EvidenceBundleArtifact, *, intent_fields: dict[str, Any]) -> list[CoverageReport]`; route `source_family` values `fedstat`, `world_bank`, and `ckan` to the adapters. Each coverage report must include checks for `periods`, `geographies`, `unit`, `frequency`, `missing_values`, and `source_specific_risks`.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_nodes.py tests/test_hybrid_retrieval.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/nodes/scouts.py` contains `def run_source_scouts`; `app/workflow/nodes/coverage.py` contains `source_specific_risks`; tests assert selected and rejected source lists are both preserved.</acceptance_criteria>
  <done>Workflow can obtain source evidence and coverage reports without hardcoding a single source family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement safe extraction planner node</name>
  <files>app/workflow/nodes/extraction_planner.py, tests/test_phase2_workflow_nodes.py</files>
  <read_first>app/artifacts/workflow_artifacts.py, app/data/fedstat_adapter.py, app/data/world_bank_adapter.py, app/data/ckan_adapter.py, .planning/ARCHITECTURE_STACK.md</read_first>
  <behavior>
    - Planner refuses to plan extraction when coverage status is `gated` or `skipped_with_reason`.
    - Planner emits operations from a fixed allowlist: `coverage_preview`, `filter_rows`, `join_indicators`, `normalize_index`, `export_dataset`.
    - Planner never includes arbitrary user-provided SQL.
  </behavior>
  <action>Create `app/workflow/nodes/extraction_planner.py` with constant `ALLOWED_OPERATIONS = {"coverage_preview", "filter_rows", "join_indicators", "normalize_index", "export_dataset"}` and function `build_extraction_plan(intent: IntentFrame, coverage_reports: list[CoverageReport]) -> ExtractionPlan`. For ok/enough coverage, set `status="ok"`, `operations` from the allowlist, concrete `filters` from `IntentFrame.known_fields`, and output columns equal to the canonical adapter columns. Include `source_family` in each operation so downstream deterministic tools can dispatch to `extract_fedstat_dataset`, `extract_world_bank_dataset`, or `extract_ckan_dataset`. For insufficient or clarification-required coverage, set `status="needs_clarification"` or `status="skipped_with_reason"` and include `skip_reason`; do not produce `duckdb_sql` unless it is compiled by a deterministic adapter.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_nodes.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/nodes/extraction_planner.py` contains `ALLOWED_OPERATIONS`; contains `extract_ckan_dataset` or a dispatch key for CKAN extraction; tests assert a malicious string like `DROP TABLE` is absent from generated plans.</acceptance_criteria>
  <done>Extraction planning is structured, safe, and ready for graph integration.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_phase2_workflow_nodes.py tests/test_hybrid_retrieval.py -q`.
</verification>

<success_criteria>
All three source families can be scouted, coverage-checked, and converted to safe extraction plans through shared workflow nodes; promoted CKAN CSV/Parquet resources also have deterministic extraction support.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-04-SUMMARY.md`.
</output>
