---
phase: 01-data-architecture-research
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - app/ui/trace_models.py
  - .planning/phases/01-data-architecture-research/trace-ui-demo.md
  - .planning/phases/01-data-architecture-research/implementation-decision-brief.md
  - .planning/phases/01-data-architecture-research/final-recommendation.md
autonomous: true
requirements:
  - NLU-02
  - NLU-03
  - NLU-04
  - SRCH-04
must_haves:
  truths:
    - "The phase shows how Streamlit will expose state, trace, artifacts, and user feedback/fix requests."
    - "The trace contract is concrete enough to demonstrate chosen sources, rejected sources, checkpoint decisions, and no-data outcomes, using the canonical workflow-owned `TraceEvent` rather than a duplicated UI schema."
    - "The final recommendation package clearly distinguishes accepted implementation decisions from remaining risks."
  artifacts:
    - path: "app/ui/trace_models.py"
      provides: "UI view-model mapping plus feedback/fix-request payloads that reuse workflow TraceEvent"
    - path: ".planning/phases/01-data-architecture-research/trace-ui-demo.md"
      provides: "Streamlit state machine and artifacts-panel demo contract"
    - path: ".planning/phases/01-data-architecture-research/final-recommendation.md"
      provides: "Ranked recommendation package"
    - path: ".planning/phases/01-data-architecture-research/implementation-decision-brief.md"
      provides: "Implementation decision brief for the single active Phase 1"
  key_links:
    - from: "app/ui/trace_models.py"
      to: "app/artifacts/workflow_artifacts.py"
      via: "Import canonical TraceEvent into UI view-model mapping"
      pattern: "from app\\.artifacts\\.workflow_artifacts import TraceEvent"
    - from: "app/ui/trace_models.py"
      to: "app/workflow/graph_contract.py"
      via: "Streamlit trace payloads map graph-emitted workflow trace events into UI view models"
      pattern: "TraceEvent|WorkflowTraceViewModel|TraceTimeline"
    - from: "app/ui/trace_models.py"
      to: ".planning/phases/01-data-architecture-research/trace-ui-demo.md"
      via: "Shared feedback and mapped trace payload names"
      pattern: "WorkflowTraceViewModel|FeedbackRequest|FixRequest"
    - from: ".planning/phases/01-data-architecture-research/final-recommendation.md"
      to: ".planning/phases/01-data-architecture-research/implementation-decision-brief.md"
      via: "Recommendation summary and explicit non-acceptance wording"
      pattern: "for discussion|not accepted"
---

<objective>
Package the visible trace/UI contract and the final implementation decisions for the single active Phase 1.

Purpose: Convert the earlier research and spike outputs into the decision-ready artifacts prioritized by D-24 and D-25.
Output: typed trace UI payloads, a Streamlit trace demo artifact, a final recommendation package, and an implementation decision brief.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/ARCHITECTURE_STACK.md
@.planning/phases/01-data-architecture-research/retrieval-comparison.md
@.planning/phases/01-data-architecture-research/extraction-probes.md
@.planning/phases/01-data-architecture-research/langgraph-contract.md
@.planning/phases/01-data-architecture-research/yandex-qwen-spike.md
@app/artifacts/workflow_artifacts.py
@app/workflow/graph_contract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the trace and feedback UI payloads</name>
  <files>app/ui/trace_models.py, .planning/phases/01-data-architecture-research/trace-ui-demo.md</files>
  <read_first>app/ui/trace_models.py, app/artifacts/workflow_artifacts.py, app/workflow/graph_contract.py, .planning/phases/01-data-architecture-research/trace-ui-demo.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/ARCHITECTURE_STACK.md, .planning/phases/01-data-architecture-research/langgraph-contract.md</read_first>
  <behavior>
    - Test 1: UI models import the canonical workflow `TraceEvent` and map it into Streamlit-ready view models carrying state, agent, tools, artifact ids, decision summary, warnings, and timing.
    - Test 2: Feedback/fix-request payloads can route a run back to clarification, source selection, coverage, extraction, or narration.
    - Test 3: The demo artifact describes a Streamlit layout that exposes state machine, trace timeline, artifacts panel, and fix-request loop using trace payloads emitted from `graph_contract.py` through `workflow_artifacts.py`.
  </behavior>
  <action>Create `app/ui/trace_models.py` with typed UI payloads for `FeedbackRequest`, `FixRequest`, and one or more UI view-model wrappers such as `WorkflowTraceViewModel` or equivalent that import and reuse `TraceEvent` from `app/artifacts/workflow_artifacts.py`. Do not redefine `TraceEvent` in `app/ui/trace_models.py`. Add mapping helpers or comments showing how `graph_contract.py` trace payloads feed the Streamlit timeline/artifact panels. Write `trace-ui-demo.md` with sections named exactly `State machine`, `Trace timeline`, `Artifacts panel`, and `Feedback and fix requests`. The document must describe a Streamlit-first demo surface, explicitly mention that the UI should expose state, trace, artifacts, and user feedback/fix requests per project rules, and state that trace payloads originate from the workflow contract and are adapted for display in the UI layer.</action>
  <acceptance_criteria>`rg -n "State machine|Trace timeline|Artifacts panel|Feedback and fix requests" .planning/phases/01-data-architecture-research/trace-ui-demo.md` returns four section headers, `rg -n "from app\\.artifacts\\.workflow_artifacts import TraceEvent|class FeedbackRequest|class FixRequest|class WorkflowTraceViewModel" app/ui/trace_models.py` returns the import plus UI models, and `rg -n "workflow contract|graph_contract|TraceEvent" .planning/phases/01-data-architecture-research/trace-ui-demo.md` confirms the trace data source is explicit.</acceptance_criteria>
  <verify>
    <automated>rg -n "State machine|Trace timeline|Artifacts panel|Feedback and fix requests" .planning/phases/01-data-architecture-research/trace-ui-demo.md && rg -n "from app\\.artifacts\\.workflow_artifacts import TraceEvent|class FeedbackRequest|class FixRequest|class WorkflowTraceViewModel" app/ui/trace_models.py && rg -n "workflow contract|graph_contract|TraceEvent" .planning/phases/01-data-architecture-research/trace-ui-demo.md</automated>
  </verify>
  <done>The repo contains a concrete Streamlit trace/UI contract that reuses the workflow-owned TraceEvent through explicit UI mapping rather than duplicate schema ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the final recommendation package and implementation decision brief</name>
  <files>.planning/phases/01-data-architecture-research/final-recommendation.md, .planning/phases/01-data-architecture-research/implementation-decision-brief.md</files>
  <read_first>.planning/phases/01-data-architecture-research/final-recommendation.md, .planning/phases/01-data-architecture-research/implementation-decision-brief.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/phases/01-data-architecture-research/retrieval-comparison.md, .planning/phases/01-data-architecture-research/extraction-probes.md, .planning/phases/01-data-architecture-research/langgraph-contract.md, .planning/phases/01-data-architecture-research/trace-ui-demo.md</read_first>
  <action>Write `final-recommendation.md` as a ranked recommendation package covering retrieval path, extraction path, orchestration path, Yandex/Qwen path, and trace/UI path. Each recommendation must cite the evidence artifact that supports it and must score options against `demonstration value`, `deterministic reliability`, `implementation risk`, and `demo readiness`. Write `implementation-decision-brief.md` for the single active Phase 1 with an explicit section for accepted implementation decisions, rejected options, remaining risks, and verification still required. Include a short section titled `Open questions before final demo` and keep it focused on remaining trade-offs, not new architecture shopping.</action>
  <acceptance_criteria>`rg -n "demonstration value|deterministic reliability|implementation risk|demo readiness" .planning/phases/01-data-architecture-research/final-recommendation.md` returns four scoring dimensions, and `rg -n "Accepted implementation decisions|Remaining risks|Open questions before final demo" .planning/phases/01-data-architecture-research/implementation-decision-brief.md` returns three matches.</acceptance_criteria>
  <verify>
    <automated>rg -n "demonstration value|deterministic reliability|implementation risk|demo readiness" .planning/phases/01-data-architecture-research/final-recommendation.md && rg -n "Accepted implementation decisions|Remaining risks|Open questions before final demo" .planning/phases/01-data-architecture-research/implementation-decision-brief.md</automated>
  </verify>
  <done>The phase ends with a decision package that is explicit, source-backed, and clear about accepted implementation choices versus remaining risks.</done>
</task>

</tasks>

<verification>
Verify that the trace/UI artifact names the visible Streamlit surfaces and that the decision brief preserves the boundary between accepted implementation choices and remaining risks.
</verification>

<success_criteria>
Phase 1 concludes with a user-visible trace concept and a recommendation package optimized for transparent multi-agent demo value while staying source-bound and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-05-SUMMARY.md`
</output>
