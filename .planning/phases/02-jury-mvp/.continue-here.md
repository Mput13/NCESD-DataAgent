---
context: phase
phase: 02-jury-mvp
task: minimal_real_frontend_prep
total_tasks: 3
status: in_progress
last_updated: 2026-05-10T15:29:59.550Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Background server after interrupted smoke | The first `app.web.server --port 8787` smoke was interrupted after the process started. PID 29180 was stopped before pausing. | advisory | Before starting a new server, check current Python processes and port availability. |

<current_state>
Phase 2 execution itself is complete and committed at `e140e35 feat(02-08): wire Streamlit workflow surface`.

The user then requested a minimal replacement path from Streamlit to a real frontend using JS/HTML/CSS. Work started additively, not as a Streamlit deletion:

- `app/web/server.py` exposes a tiny standard-library HTTP server.
- `app/web/static/index.html`, `styles.css`, and `app.js` implement a minimal browser UI.
- API endpoints are `/api/query`, `/api/continue`, and `/api/feedback`.
- The API calls `app.workflow.service.run_user_query`, `continue_user_query`, and `apply_feedback`.
- `tests/test_web_frontend.py` covers static file/API wiring.
- `README.md` has a new `Run Minimal Web UI` section.
</current_state>

<completed_work>

Completed Tasks:
- Phase 2 plan execution: Done, committed as `e140e35`.
- Minimal web scaffold: In progress, files created but not committed.
- Focused verification: Done, `python -m pytest tests/test_web_frontend.py tests/test_phase2_workflow_service.py -q` passed with 33 tests.
- Interrupted web server cleanup: Done, stopped `python -m app.web.server --port 8787` PID 29180.
</completed_work>

<remaining_work>

- Smoke the minimal web UI:
  - `$env:PYTHONPATH='.'`
  - `python -m app.web.server --port 8787`
  - open `http://127.0.0.1:8787`
  - submit `Kakoy VVP Rossii v 2024 godu`
  - verify message, details tabs, clarification state, and feedback form.
- Decide whether this scaffold is enough for now or whether to introduce a JS build tool later.
- If accepted, commit the scaffold and handoff files.
- Manual Phase 2 UAT in `manual-uat.md` is still pending before full phase completion.
</remaining_work>

<decisions_made>

- Use a zero-new-dependency Python HTTP server plus static HTML/CSS/JS first.
- Keep `app.workflow.service` as the only workflow API boundary.
- Do not remove Streamlit yet; the new frontend is additive until the user approves replacement.
- Use local mode by default in the browser UI so missing live credentials do not block smoke testing.
</decisions_made>

<blockers>

- No technical blockers for the frontend scaffold.
- Human UAT approval remains blocking for claiming Phase 2 complete.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable pause state and exact next action.
1. `.planning/phases/02-jury-mvp/02-08-SUMMARY.md` — what Phase 2 UI/service execution already completed.
1. `app/web/server.py` — API boundary for the new minimal frontend.
1. `app/web/static/app.js` — frontend request/render behavior.
1. `tests/test_web_frontend.py` — expected frontend/API wiring.
1. `app/workflow/service.py` — canonical workflow service; do not bypass it.

## Critical Anti-Patterns (do NOT repeat these)

- Do not replace workflow logic in JavaScript. JS should call the Python workflow API and render `WorkflowResponse`.
- Do not delete Streamlit during this transition unless the user explicitly approves removal.
- Do not treat all-20 acceptance as product quality proof for the new browser UI; it proves service outcomes, not visual/browser behavior.

## Infrastructure State

- Streamlit processes were already running before pause:
  - PID 29972: `streamlit run app/ui/streamlit_app.py --server.headless true`
  - PID 28584: `python -m streamlit run app/ui/streamlit_app.py --server.port 8502 --server.headless true`
- The new web server on port 8787 was stopped after the interrupted smoke.
- Branch: `codex/phase-2-jury-mvp-planning`, ahead of origin by 1 commit before frontend scaffold commit.

## Pre-Execution Critique Required

- Not required. This is a small additive scaffold; first resume action is a smoke test.

<context>
The user wants to move from Streamlit to a real frontend, but asked for minimal preparation, not a full frontend rewrite. The safest continuation is to validate the existing static scaffold, keep it plain, and only introduce React/Vite/etc. if the user explicitly wants that next.
</context>

<next_action>
Start with: run `$env:PYTHONPATH='.'; python -m app.web.server --port 8787`, open `http://127.0.0.1:8787`, and test the selected prompt `Kakoy VVP Rossii v 2024 godu`.
</next_action>
