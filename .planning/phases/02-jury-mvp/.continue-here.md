---
context: phase
phase: 02-jury-mvp
task: null
total_tasks: null
status: paused
last_updated: 2026-05-14T13:54:21Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. Advisory notes in Anti-Patterns section below._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| LLM fallback temptation | When Qwen credentials missing, there is NO fallback path. Do not add keyword matching or rule-based substitutes. | blocking | `gated_skip` → raise RuntimeError, set `finalization_pending=True`, stop. |
| FedStat geo column missed | Wide Parquet files use ОКАТО/ОКСМ column names. Fixed in `fedstat_adapter.py` `_find_column` needles. | advisory | Always check `_find_column` needles when a new geo classifier appears |
| JSON truncation on Russian output | Yandex AI Studio truncates at max_tokens. Research Designer: 1024, Narrator: 2048, Extraction Planner: 1024. | advisory | Use `_repair_truncated_json` fallback + bump max_tokens first |
| has_records without null check | Checking `bool(records)` instead of `any(r.get("value") is not None for r in records)` causes contradictory narrator output for all-null datasets. | advisory | Fixed in narrator.py — do not revert |

**Do not proceed until all boxes are checked.**

<current_state>
Phase 02 (jury-MVP) — active bug-fix iteration. 11 fixes shipped across sessions. Latest commit: a50b8ca (2026-05-14).

This session: deep analysis of extraction/artifact pipeline revealed that too many pipeline nodes had binary fail-fast behaviour (partial data → not_found instead of partial analysis). Fix A (graceful degradation) shipped:
- coverage.py: LLM can_proceed=False → partial (not skipped_with_reason)
- critic.py: needs_repair + ok dataset → passed (not not_found)
- narrator.py: has_records checks non-null values; coverage hint added for not_found prompts
- extraction_planner.py: max_tokens 512 → 1024

Fix B (code-gen extraction agent with schema awareness + retries) agreed with user as next architectural step after running golden eval.
</current_state>

<completed_work>

- fix1–fix10: shipped in previous sessions (see HANDOFF.json)
- fix11 (this session): graceful degradation — partial analysis instead of not_found
  - coverage.py: can_proceed=False → partial
  - critic.py: needs_repair + data exists → passed
  - narrator.py: has_records null-check + coverage hint for not_found
  - extraction_planner.py: max_tokens 1024
</completed_work>

<remaining_work>

**HIGH** — full-20-run: Run all 20 golden cases to measure pass rate after 11 fixes. Entry: `scripts/run_golden_cases.py` or `.planning/phases/02-jury-mvp/batch-runs/`. Target: `unacceptable==0`.

**MEDIUM** — gc011-routing: In `app/workflow/graph.py` `_route_after_intent()`, check `supervisor_route=='no_data'` BEFORE `intent.needs_clarification`.

**MEDIUM** — fedstat-period-filter: `fedstat_adapter.py` line 203 — `period_columns` regex `\d{4}` drops non-year columns (`Янв`, `фев.2023`). Cumulative sources like 61502 return 0 rows. Need to detect and handle non-year period columns.

**LOW** — sse-hang: "Думает" stays on screen after pipeline ends on error paths. Check `server.py _handle_stream` finally block.

**FUTURE** — Variant B: code-gen extraction agent — LLM sees real parquet schema, writes DuckDB query, retries on error.
</remaining_work>

<decisions_made>

- Variant A (graceful degradation) before Variant B (code-gen) — user decision
- coverage can_proceed=False → partial: keeps extraction running with quality warnings
- needs_repair + ok data → passed: surfaces extracted data instead of silence
- has_records checks value is not None: prevents contradictory output for null-only datasets
- extraction_planner max_tokens 512 → 1024: aligns with other nodes
</decisions_made>

<blockers>
None currently.
</blockers>

## Required Reading (in order)
1. `app/workflow/nodes/coverage.py` — coverage degradation logic
2. `app/workflow/nodes/critic.py` — derive_final_outcome
3. `app/workflow/nodes/narrator.py` — has_records, data_signal, coverage_hint
4. `app/data/fedstat_adapter.py` — `_load_normalized_rows`, `period_columns` line 203

## Critical Anti-Patterns (do NOT repeat these)
- **SILENT LLM FALLBACK**: gated_skip → RuntimeError → stop. NEVER keyword/rule-based substitutes.
- **REVERT has_records**: The null-check fix is intentional — do not revert to `bool(records)`.
- **512 max_tokens FOR RUSSIAN**: Always ≥1024 for structured outputs, ≥2048 for narrative.

## Infrastructure State
- Web UI: `uvicorn app.web.server:app --reload --port 8000` (not running, start manually)
- Qdrant: server mode via `QDRANT_URL` env var required for dense retrieval
- Credentials: `YANDEX_AI_STUDIO_QWEN_API_KEY` + `YANDEX_AI_STUDIO_QWEN_MODEL` required

<context>
Session analyzed why correct sources don't produce analysis. Root: pipeline had too many binary fail-fast points. Fix A shipped (graceful degradation, 4 files, commit a50b8ca). Next session should start with 20-case golden eval to quantify improvement, then fix gc011-routing and fedstat-period-filter.
</context>

<next_action>
Start with: run the full 20-case golden eval. Check `.planning/phases/02-jury-mvp/batch-runs/` for the batch runner, or `scripts/run_golden_cases.py`. Review results, count unacceptable outcomes. Then fix `gc011-routing` and `fedstat-period-filter`.
</next_action>
