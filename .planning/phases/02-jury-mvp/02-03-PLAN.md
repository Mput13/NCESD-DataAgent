---
phase: 02-jury-mvp
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/fedstat_adapter.py
  - app/data/world_bank_adapter.py
  - app/data/deterministic_tools.py
  - tests/test_phase2_extraction_adapters.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - ART-04
  - ART-05
  - ART-06
  - RBST-04
  - ENG-02
must_haves:
  truths:
    - "FedStat and World Bank extraction produce DatasetArtifact rows through deterministic code."
    - "Derived metrics such as normalization to base year 100 are computed by code, not LLM narration."
    - "Dataset exports include CSV, optional Parquet, manifest, provenance, and quality flags."
  artifacts:
    - "app/data/fedstat_adapter.py implements FedStat coverage and extraction helpers."
    - "app/data/world_bank_adapter.py implements World Bank coverage and extraction helpers."
    - "tests/test_phase2_extraction_adapters.py proves deterministic rows and exports."
  key_links:
    - "adapters return app.artifacts.workflow_artifacts.DatasetArtifact."
    - "app/data/deterministic_tools.py remains the export and visualization helper layer."
---

<objective>
Promote FedStat and World Bank from probe evidence to answer-grade deterministic adapters.

Purpose: satisfy D-22 through D-26 and keep RBST-04 hard: LLMs never extract table values.
Output: source-specific coverage/extraction helpers with tests over local synthetic Parquet fixtures.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@.planning/ARCHITECTURE_STACK.md
@app/data/deterministic_tools.py
@app/artifacts/workflow_artifacts.py
@.planning/phases/01-data-architecture-research/golden-cases.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FedStat wide Parquet adapter</name>
  <files>app/data/fedstat_adapter.py, tests/test_phase2_extraction_adapters.py</files>
  <read_first>app/data/deterministic_tools.py, app/data/source_card_builders.py, .planning/ARCHITECTURE_STACK.md</read_first>
  <behavior>
    - Synthetic wide Parquet with first-row headers is normalized to canonical long rows.
    - Coverage preview reports `available_periods`, `available_geographies`, `unit`, `frequency`, and `missing_values`.
    - Extraction for a selected indicator/geography/period returns DatasetArtifact with provenance source URL.
  </behavior>
  <action>Create `app/data/fedstat_adapter.py` with functions `preview_fedstat_coverage(source_card: dict[str, Any], *, filters: dict[str, Any]) -> CoverageReport` and `extract_fedstat_dataset(source_card: dict[str, Any], *, filters: dict[str, Any], output_dir: Path, artifact_id: str) -> DatasetArtifact`. The canonical output columns must be exactly `source`, `dataset_id`, `indicator_id`, `indicator_name`, `geo_id`, `geo_name`, `period`, `period_type`, `value`, `unit`, `dimensions`, `source_url`, `retrieved_at`, `quality_flags`. Use PyArrow/DuckDB to read Parquet metadata and rows; treat first row as logical header when FedStat columns are technical (`column00`, `column01`, `column0`). Do not call any LLM, and do not pass free-form SQL from outside the adapter.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_extraction_adapters.py -q</automated></verify>
  <acceptance_criteria>`app/data/fedstat_adapter.py` contains `def preview_fedstat_coverage`; contains `def extract_fedstat_dataset`; test fixture asserts canonical column `value` and `quality_flags`.</acceptance_criteria>
  <done>FedStat wide-table extraction is deterministic, source-bound, and covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement World Bank long Parquet adapter</name>
  <files>app/data/world_bank_adapter.py, tests/test_phase2_extraction_adapters.py</files>
  <read_first>app/data/deterministic_tools.py, app/data/source_card_builders.py, .planning/phases/01-data-architecture-research/golden-cases.yaml</read_first>
  <behavior>
    - Russia/Kazakhstan/China aliases resolve deterministically.
    - BRICS and EAEU country sets are expanded without mixing aggregates as countries.
    - `normalize_first_available_to_100` computes base-year index values by code.
  </behavior>
  <action>Create `app/data/world_bank_adapter.py` with `preview_world_bank_coverage(source_card, *, countries, periods, indicator_id) -> CoverageReport`, `extract_world_bank_dataset(source_card, *, countries, periods, indicator_id, output_dir, artifact_id) -> DatasetArtifact`, and `normalize_first_available_to_100(dataset: DatasetArtifact, *, group_column: str = "geo_id") -> DatasetArtifact`. Include alias maps for `Russia -> RUS`, `Казахстан -> KAZ`, `China -> CHN`, `БРИКС -> [BRA,RUS,IND,CHN,ZAF]`, and `ЕАЭС -> [ARM,BLR,KAZ,KGZ,RUS]`. Reject aggregate rows unless the requested geography explicitly asks for an aggregate. Output the same canonical columns as the FedStat adapter.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_extraction_adapters.py -q</automated></verify>
  <acceptance_criteria>`app/data/world_bank_adapter.py` contains `BRICS`; contains `normalize_first_available_to_100`; tests assert first available value equals `100` for each geography group.</acceptance_criteria>
  <done>World Bank comparative and derived-metric extraction paths are deterministic and testable.</done>
</task>

<task type="auto">
  <name>Task 3: Route deterministic exports through shared helpers</name>
  <files>app/data/deterministic_tools.py, tests/test_phase2_extraction_adapters.py</files>
  <read_first>app/data/deterministic_tools.py, app/artifacts/workflow_artifacts.py, app/data/fedstat_adapter.py, app/data/world_bank_adapter.py</read_first>
  <action>Add helper `export_dataset_with_script(dataset: DatasetArtifact, *, output_dir: Path, script_text: str) -> tuple[DatasetArtifact, dict[str, Any]]` to `app/data/deterministic_tools.py`. It must call `export_csv_parquet_manifest`, write a `.py` reproducibility script beside the CSV using the dataset artifact id as the stem, and return a script metadata dict with keys `artifact_id`, `path`, `language`, `source_dataset_artifact_id`, and `sha256`. The script text must be deterministic and must not include API keys or `.env` values.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_extraction_adapters.py tests/test_deterministic_tools_and_trace.py -q</automated></verify>
  <acceptance_criteria>`app/data/deterministic_tools.py` contains `def export_dataset_with_script`; tests assert returned metadata has `language == "python"` and a 64-character `sha256`.</acceptance_criteria>
  <done>Passed cases can produce downloadable dataset and script artifacts required by ART-04 and ART-05.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_phase2_extraction_adapters.py tests/test_deterministic_tools_and_trace.py -q`.
</verification>

<success_criteria>
FedStat and World Bank data can be covered, extracted, exported, and normalized through deterministic adapters with no LLM numeric path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-03-SUMMARY.md`.
</output>
