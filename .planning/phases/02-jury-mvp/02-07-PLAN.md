---
phase: 02-jury-mvp
plan: "07"
type: execute
wave: 7
depends_on:
  - "02-02"
  - "02-06"
  - "02-10"
files_modified:
  - app/evals/run_eval.py
  - app/demo/run_demo.py
  - scripts/run_phase2_acceptance.py
  - tests/test_phase2_acceptance.py
autonomous: true
requirements:
  - NLU-01
  - NLU-02
  - NLU-03
  - NLU-04
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - ART-01
  - ART-02
  - ART-03
  - ART-04
  - ART-05
  - ART-06
  - RBST-01
  - RBST-02
  - RBST-03
  - RBST-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - ENG-01
  - ENG-02
  - ENG-03
  - ENG-04
must_haves:
  truths:
    - "All 20 golden cases are evaluated through the same workflow service used by UI and CLI."
    - "No case can finish with gated, stale, skipped_with_reason, no_candidate, or ok-with-gated-internals."
    - "All 20 golden cases are checked against the coverage/extraction matrix before final acceptance is scored."
    - "Jury readiness fails when any final response uses test-only LLM fallbacks for intent, research design, critic, or narrator."
    - "Demo readiness is ready only after index, workflow eval, extraction, artifacts, and trace gates pass."
  artifacts:
    - "scripts/run_phase2_acceptance.py writes phase2-golden-results.json and .md."
    - "app/evals/run_eval.py scores WorkflowResponse terminal outcomes."
    - "app/demo/run_demo.py includes Phase 2 readiness inputs."
    - ".planning/phases/02-jury-mvp/golden-coverage-matrix.json is consumed during acceptance."
  key_links:
    - "run_phase2_acceptance.py calls app.workflow.service.run_user_query and validates each case against golden-coverage-matrix.json."
    - "app.demo.run_demo consumes Phase 2 golden results before reporting ready."
---

<objective>
Turn Phase 2 acceptance into a reproducible all-20 golden-case gate and strict readiness package.

Purpose: enforce D-02, D-05 through D-10, D-38A, and the roadmap non-negotiable acceptance target.
Output: acceptance CLI, eval scoring, readiness aggregation, and tests.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-architecture-research/golden-cases.yaml
@.planning/phases/02-jury-mvp/golden-coverage-matrix.json
@app/workflow/service.py
@app/evals/run_eval.py
@app/demo/run_demo.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add all-20 Phase 2 acceptance runner</name>
  <files>scripts/run_phase2_acceptance.py, tests/test_phase2_acceptance.py</files>
  <read_first>app/workflow/service.py, .planning/phases/01-data-architecture-research/golden-cases.yaml, .planning/phases/02-jury-mvp/02-CONTEXT.md</read_first>
  <behavior>
    - Runner loads exactly 20 golden cases unless `--limit` is passed for local debugging.
    - Default run rejects any final outcome outside `passed`, `needs_clarification`, `not_found`.
    - Output includes per-case `case_id`, `query_ru`, `expected_route`, `matrix_expected_terminal_outcome`, `matrix_required_adapter`, `final_outcome`, `sources_count`, `dataset_count`, `script_count`, `trace_count`, `used_test_only_fallbacks`, and `unacceptable_reasons`.
  </behavior>
  <action>Create `scripts/run_phase2_acceptance.py` with CLI args `--goldens`, `--coverage-matrix`, `--json-output`, `--markdown-output`, `--artifact-dir`, optional `--limit`, and optional `--allow-test-fallbacks`. It must iterate all golden cases by default, load `.planning/phases/02-jury-mvp/golden-coverage-matrix.json`, call `run_user_query` with `WorkflowRunConfig.default().model_copy(update={"case_id": case["id"], "artifact_dir": Path(args.artifact_dir)})`, and write `.planning/phases/02-jury-mvp/phase2-golden-results.json` / `.md` in normal usage. Treat final outcomes `gated`, `stale`, `skipped_with_reason`, `no_candidate`, `ok`, or missing `final_outcome` as unacceptable implementation failures. For each case, compare the returned final outcome, selected source family, required adapter, dataset/script expectations, and missing-data evidence against the matrix; mismatches are unacceptable. Unless `--allow-test-fallbacks` is explicitly set for local development, fail any response whose component statuses or trace contain `test_only_intent_fallback`, `test_only_research_design_fallback`, `test_only_critic_fallback`, or `test_only_narrator_fallback` per D-37/D-38.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_acceptance.py -q</automated></verify>
  <acceptance_criteria>`scripts/run_phase2_acceptance.py` contains `run_user_query`; contains `--coverage-matrix`; contains `used_test_only_fallbacks`; contains `unacceptable_reasons`; tests assert default loads `20` cases from golden-cases.yaml.</acceptance_criteria>
  <done>The all-20 acceptance run is executable and machine-readable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend eval scoring for WorkflowResponse outcomes</name>
  <files>app/evals/run_eval.py, tests/test_phase2_acceptance.py</files>
  <read_first>app/evals/run_eval.py, app/artifacts/workflow_artifacts.py, scripts/run_phase2_acceptance.py</read_first>
  <behavior>
    - Passed cases require source, coverage, deterministic dataset, script, answer, trace evidence, and matrix alignment.
    - Clarification cases require a specific question and no extraction.
    - Not-found cases require attempted source/rejection evidence.
    - Test-only intent/research/critic/narrator fallbacks fail jury readiness.
  </behavior>
  <action>Extend `app/evals/run_eval.py` with `score_phase2_results(results_path: Path, *, coverage_matrix_path: Path | None = None) -> dict[str, Any]` and CLI args `--phase2-results`, `--phase2-coverage-matrix`, `--phase2-json-output`, and `--phase2-markdown-output`. Aggregate counts `passed`, `needs_clarification`, `not_found`, `failed`, `unacceptable`, and `test_only_fallback_failures`. For each case, fail if `final_outcome=="passed"` and `dataset_count < 1` or `script_count < 1` or `trace_count < 5`; fail if the case outcome/source/adapter/artifacts do not match the coverage matrix; fail if `needs_clarification` has no clarification question; fail if `not_found` has no rejection/attempted-source evidence; fail if any response contains unsupported numeric claim marker; fail jury readiness when `used_test_only_fallbacks` is non-empty.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_acceptance.py tests/test_eval_runner.py -q</automated></verify>
  <acceptance_criteria>`app/evals/run_eval.py` contains `def score_phase2_results`; contains `--phase2-coverage-matrix`; contains `test_only_fallback_failures`; tests include unacceptable final outcome `gated`.</acceptance_criteria>
  <done>Eval scoring reflects final product behavior instead of Phase 1 probe gates.</done>
</task>

<task type="auto">
  <name>Task 3: Make demo readiness depend on Phase 2 acceptance</name>
  <files>app/demo/run_demo.py, tests/test_phase2_acceptance.py</files>
  <read_first>app/demo/run_demo.py, app/retrieval/readiness.py, app/evals/run_eval.py, .planning/phases/02-jury-mvp/02-RESEARCH.md</read_first>
  <action>Update `app/demo/run_demo.py` so it can accept optional Phase 2 inputs `phase2_results`, `phase2_eval`, and `coverage_matrix` in `DemoInputs` or a new `Phase2DemoInputs`. Readiness `overall_status` may be `ready` only when `assess_phase2_index_readiness(...).ready == true`, the coverage matrix has `total_cases == 20` and no unresolved data gaps, Phase 2 eval has `total_cases == 20`, `failed == 0`, `unacceptable == 0`, `test_only_fallback_failures == 0`, and all cases have final outcomes in `passed`, `needs_clarification`, or `not_found`. Include `phase2_workflow_eval_status`, `phase2_total_cases`, `phase2_unacceptable_count`, `phase2_test_only_fallback_failures`, and `phase2_coverage_matrix_status` in the returned JSON.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_acceptance.py tests/test_demo_readiness.py -q</automated></verify>
  <acceptance_criteria>`app/demo/run_demo.py` contains `phase2_workflow_eval_status`; contains `phase2_coverage_matrix_status`; tests assert readiness is not `ready` when `phase2_total_cases` is `19` or test-only fallbacks are present.</acceptance_criteria>
  <done>Demo readiness cannot claim jury readiness unless all Phase 2 product gates pass.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_phase2_acceptance.py tests/test_eval_runner.py tests/test_demo_readiness.py -q`.
</verification>

<success_criteria>
All-20 golden acceptance is a CLI/eval/readiness gate with coverage-matrix alignment, live-LLM readiness enforcement, and no acceptable stale, gated, skipped, or fake-success final state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-07-SUMMARY.md`.
</output>
