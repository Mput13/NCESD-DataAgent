---
phase: 02-jury-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app/artifacts/workflow_artifacts.py
  - app/workflow/service.py
  - tests/test_phase2_contracts.py
autonomous: true
requirements:
  - NLU-01
  - NLU-02
  - NLU-03
  - NLU-04
  - ART-01
  - ART-02
  - ART-03
  - ART-04
  - ART-05
  - ART-06
  - RBST-01
  - RBST-02
  - RBST-03
  - RBST-04
  - UI-02
  - UI-03
  - ENG-02
  - ENG-03
must_haves:
  truths:
    - "Every workflow response has final_outcome equal to passed, needs_clarification, or not_found."
    - "Internal gated/stale/skipped/no_candidate statuses cannot be represented as final user outcomes."
    - "UI, eval, and CLI callers have one shared WorkflowResponse contract."
  artifacts:
    - "app/artifacts/workflow_artifacts.py defines TerminalOutcome, WorkflowResponse, ScriptArtifact, and NoDataExplanationArtifact."
    - "app/workflow/service.py defines WorkflowRunConfig and run_user_query."
    - "tests/test_phase2_contracts.py validates the Phase 2 response contract."
  key_links:
    - "app/workflow/service.py imports WorkflowResponse from app.artifacts.workflow_artifacts."
    - "requirements.txt contains langgraph>=1.1.10."
---

<objective>
Create the Phase 2 response/status/artifact contract that all later workflow, eval, and Streamlit work must use.

Purpose: enforce D-06 through D-10, D-31 through D-33, and D-37 before any implementation can claim jury readiness.
Output: dependency baseline, strict final-outcome models, and one service entrypoint signature.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@.planning/phases/02-jury-mvp/02-RESEARCH.md
@.planning/ARCHITECTURE_STACK.md
@app/artifacts/workflow_artifacts.py
@app/workflow/graph_contract.py

<interfaces>
Existing contracts to extend:
```python
WorkflowStatus = Literal["ok", "gated", "skipped_with_reason", "needs_clarification", "no_data"]
class IntentFrame(BaseModel): ...
class CoverageReport(BaseModel): ...
class ExtractionPlan(BaseModel): ...
class DatasetArtifact(BaseModel): ...
class VisualizationSpec(BaseModel): ...
class CritiqueReport(BaseModel): ...
class FinalAnswer(BaseModel): ...
class TraceEvent(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare LangGraph runtime dependency</name>
  <files>requirements.txt, tests/test_phase2_contracts.py</files>
  <read_first>requirements.txt, .planning/phases/02-jury-mvp/02-RESEARCH.md</read_first>
  <action>Add the exact dependency line `langgraph>=1.1.10` to `requirements.txt` if absent. In `tests/test_phase2_contracts.py`, add `test_phase2_runtime_imports_declared_dependencies` that imports `langgraph`, `pydantic`, `streamlit`, `qdrant_client`, `duckdb`, and `pyarrow`. Do not add any test that reads `.env` or prints secret values.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_contracts.py -q</automated></verify>
  <acceptance_criteria>`requirements.txt` contains `langgraph>=1.1.10`; `tests/test_phase2_contracts.py` contains `import langgraph`; pytest exits 0.</acceptance_criteria>
  <done>LangGraph is declared and the Phase 2 import sanity test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add strict Phase 2 response models</name>
  <files>app/artifacts/workflow_artifacts.py, tests/test_phase2_contracts.py</files>
  <read_first>app/artifacts/workflow_artifacts.py, tests/test_workflow_graph.py, .planning/phases/02-jury-mvp/02-CONTEXT.md</read_first>
  <behavior>
    - `WorkflowResponse(final_outcome="gated", ...)` fails validation.
    - `WorkflowResponse(final_outcome="passed", dataset_artifacts=[], script_artifacts=[])` fails validation.
    - `WorkflowResponse(final_outcome="needs_clarification", clarification_questions=["..."])` validates.
    - `WorkflowResponse(final_outcome="not_found", not_found_evidence=...)` validates.
  </behavior>
  <action>Extend `app/artifacts/workflow_artifacts.py` with `TerminalOutcome = Literal["passed", "needs_clarification", "not_found"]`, `FeedbackAction`, `ScriptArtifact`, `NoDataExplanationArtifact`, and `WorkflowResponse`. `WorkflowResponse` must include these concrete fields: `run_id`, `final_outcome`, `message`, `answer_blocks`, `citations`, `selected_sources`, `rejected_sources`, `coverage`, `extraction_plan`, `dataset_artifacts`, `script_artifacts`, `visualization`, `trace_events`, `limitations`, `clarification_questions`, `not_found_evidence`, `feedback_actions`, and `component_statuses`. Add Pydantic validators enforcing D-06/D-07/D-08/D-09/D-10: `passed` requires at least one dataset artifact and one script artifact; `needs_clarification` requires at least one clarification question; `not_found` requires `NoDataExplanationArtifact`; no final outcome may be `ok`, `gated`, `stale`, `skipped_with_reason`, or `no_candidate`.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_contracts.py tests/test_workflow_graph.py -q</automated></verify>
  <acceptance_criteria>`app/artifacts/workflow_artifacts.py` contains `TerminalOutcome = Literal["passed", "needs_clarification", "not_found"]`; contains `class WorkflowResponse`; contains `class ScriptArtifact`; tests contain the string `final_outcome="gated"` inside a failing-validation assertion.</acceptance_criteria>
  <done>Strict final outcome and frontend response contracts are available to workflow, eval, and UI code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create shared workflow service entrypoint contract</name>
  <files>app/workflow/service.py, tests/test_phase2_contracts.py</files>
  <read_first>app/workflow/graph_contract.py, app/workflow/run_graph.py, app/artifacts/workflow_artifacts.py</read_first>
  <behavior>
    - `WorkflowRunConfig.default()` points Phase 2 artifacts to `.planning/phases/02-jury-mvp`.
    - `run_user_query("...", run_config=WorkflowRunConfig(...))` returns `WorkflowResponse` or raises `NotImplementedError` with message `Phase 2 final WorkflowResponse implementation is provided by plan 02-06`.
  </behavior>
  <action>Create `app/workflow/service.py` with `WorkflowRunConfig(BaseModel)` fields: `goldens_path`, `phase1_index_manifest`, `phase1_source_catalog_manifest`, `artifact_dir`, `live_llm_required`, `live_embeddings_required`, and `case_id`. Add `WorkflowRunConfig.default()` using `.planning/phases/01-data-architecture-research/golden-cases.yaml`, `.planning/phases/01-data-architecture-research/embedding-index-manifest.json`, `.planning/phases/01-data-architecture-research/source-catalog-manifest.json`, and `.planning/phases/02-jury-mvp/workflow-runs`. Add function `run_user_query(query: str, *, run_config: WorkflowRunConfig | None = None) -> WorkflowResponse` as the single API for Streamlit/evals/CLI; until plan 02-06 implements finalization, it must raise `NotImplementedError("Phase 2 final WorkflowResponse implementation is provided by plan 02-06")` rather than returning a fake success. Plan 02-05 may add a non-final internal helper for graph execution through extraction, but it must not make `run_user_query` return a complete `WorkflowResponse` before critic/visualization/narrator exist.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_contracts.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/service.py` contains `def run_user_query(`, `class WorkflowRunConfig`, `.planning/phases/02-jury-mvp/workflow-runs`, and `NotImplementedError("Phase 2 final WorkflowResponse implementation is provided by plan 02-06")`.</acceptance_criteria>
  <done>Downstream plans have a concrete service import path without any UI-only or eval-only execution path.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_phase2_contracts.py tests/test_workflow_graph.py -q`.
</verification>

<success_criteria>
The repo has a strict Phase 2 `WorkflowResponse` model and a shared `run_user_query` entrypoint that cannot silently return a gated or fake final answer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-01-SUMMARY.md`.
</output>
