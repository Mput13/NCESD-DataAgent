---
phase: 01-data-architecture-research
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/retrieval/hybrid_retrieval.py
  - scripts/run_retrieval_spike.py
  - .planning/phases/01-data-architecture-research/retrieval-comparison.md
  - .planning/phases/01-data-architecture-research/retrieval-eval.csv
  - .planning/phases/01-data-architecture-research/extraction-probes.md
  - scripts/run_extraction_probes.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - NLU-04
must_haves:
  truths:
    - "The phase contains evidence for a full hybrid retrieval path: lexical plus dense plus rerank, or a documented credential-aware fallback when dense/rerank cannot run."
    - "Retrieval outputs are scored against the golden set and include rejection reasons and metadata-rich source cards."
    - "FedStat, World Bank, and CKAN all have deterministic coverage/extraction probes with no numbers sourced from LLM memory."
  artifacts:
    - path: "app/retrieval/hybrid_retrieval.py"
      provides: "Hybrid retrieval interfaces and provider-aware fallback"
    - path: ".planning/phases/01-data-architecture-research/retrieval-comparison.md"
      provides: "Trade-off and fallback evidence for lexical, dense, and rerank"
    - path: ".planning/phases/01-data-architecture-research/retrieval-eval.csv"
      provides: "Case-by-case retrieval evidence"
    - path: ".planning/phases/01-data-architecture-research/extraction-probes.md"
      provides: "Coverage and extraction probe results for three source paths"
    - path: "scripts/run_extraction_probes.py"
      provides: "Deterministic extraction probe entrypoint"
  key_links:
    - from: "scripts/run_retrieval_spike.py"
      to: "app/retrieval/hybrid_retrieval.py"
      via: "Retrieval spike runner"
      pattern: "hybrid_retrieval"
    - from: "scripts/run_extraction_probes.py"
      to: "app/data/source_card_builders.py"
      via: "Probe target selection"
      pattern: "source_card_builders|FedStat|World Bank|CKAN"
---

<objective>
Validate the hybrid retrieval path and run deterministic source probes for coverage and extraction.

Purpose: Satisfy D-06 through D-12 with runnable evidence, not prose-only comparison.
Output: hybrid retrieval module, retrieval spike runner, comparison artifacts, retrieval eval CSV, extraction probes report, and deterministic probe runner.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/phases/01-data-architecture-research/01-01-PLAN.md
@.planning/phases/01-data-architecture-research/01-02-PLAN.md
@.planning/phases/01-data-architecture-research/golden-cases.yaml
@app/artifacts/source_cards.py
@app/data/source_card_builders.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the hybrid retrieval spike and comparison artifact</name>
  <files>app/retrieval/hybrid_retrieval.py, scripts/run_retrieval_spike.py, .planning/phases/01-data-architecture-research/retrieval-comparison.md, .planning/phases/01-data-architecture-research/retrieval-eval.csv</files>
  <read_first>app/retrieval/hybrid_retrieval.py, scripts/run_retrieval_spike.py, .planning/phases/01-data-architecture-research/retrieval-comparison.md, .planning/phases/01-data-architecture-research/retrieval-eval.csv, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/phases/01-data-architecture-research/golden-cases.yaml, app/artifacts/source_cards.py, app/data/source_card_builders.py</read_first>
  <behavior>
    - Test 1: Lexical retrieval returns source cards with exact/title/code and RU/EN keyword evidence.
    - Test 2: Dense retrieval is attempted through the chosen provider path, or the run records a credential-aware fallback artifact without silently dropping the dense requirement.
    - Test 3: Rerank decisions and rejected candidates are captured in the eval CSV and comparison report.
  </behavior>
  <action>Create `app/retrieval/hybrid_retrieval.py` with clear interfaces for lexical retrieval, dense retrieval, reranking, and a fallback status object when Yandex embeddings or other dense credentials are absent. Create `scripts/run_retrieval_spike.py` that evaluates a bounded subset of the golden cases and writes `retrieval-eval.csv`. Write `retrieval-comparison.md` with an explicit comparison table for lexical BM25/FTS, dense path, rerank path, and the exact skipped-evidence behavior when a path cannot run. The comparison must keep the full target stack per D-06 rather than declaring dense retrieval optional.</action>
  <acceptance_criteria>`rg -n "lexical|dense|rerank|credential-aware fallback" .planning/phases/01-data-architecture-research/retrieval-comparison.md` returns all four concepts, and `python - <<'PY'\nimport csv\nfrom pathlib import Path\nrows=list(csv.DictReader(Path('.planning/phases/01-data-architecture-research/retrieval-eval.csv').open()))\nassert rows\ncols=set(rows[0])\nneed={'case_id','retrieval_mode','top_candidate','rejection_reasons','dense_status'}\nassert need.issubset(cols)\nprint(len(rows))\nPY` exits 0.</acceptance_criteria>
  <verify>
    <automated>rg -n "lexical|dense|rerank|credential-aware fallback" .planning/phases/01-data-architecture-research/retrieval-comparison.md && python - <<'PY'
import csv
from pathlib import Path
rows=list(csv.DictReader(Path('.planning/phases/01-data-architecture-research/retrieval-eval.csv').open()))
assert rows
cols=set(rows[0])
need={'case_id','retrieval_mode','top_candidate','rejection_reasons','dense_status'}
assert need.issubset(cols)
print(len(rows))
PY</automated>
  </verify>
  <done>The repo contains a hybrid retrieval spike with explicit evidence for lexical, dense, rerank, and fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run deterministic coverage and extraction probes for FedStat, World Bank, and CKAN</name>
  <files>.planning/phases/01-data-architecture-research/extraction-probes.md, scripts/run_extraction_probes.py</files>
  <read_first>.planning/phases/01-data-architecture-research/extraction-probes.md, scripts/run_extraction_probes.py, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/DATA_REPORT.md, .planning/phases/01-data-architecture-research/golden-cases.yaml, app/data/source_card_builders.py</read_first>
  <behavior>
    - Test 1: FedStat probe shows wide-table normalization and coverage preview fields before any final extraction claim.
    - Test 2: World Bank probe shows indicator/country/period path and canonical long-format expectations.
    - Test 3: CKAN probe shows resource-level access path or explicit bounded skip evidence when a resource is unsuitable.
  </behavior>
  <action>Create `scripts/run_extraction_probes.py` to inspect one representative FedStat wide parquet path, one World Bank parquet path, and one CKAN resource path chosen from the source cards. Write `extraction-probes.md` with sections named exactly `FedStat wide Parquet probe`, `World Bank parquet probe`, and `CKAN resource path probe`. Each section must report coverage-preview fields, extraction strategy, and rejection/no-data conditions without copying numeric facts from memory or from the LLM.</action>
  <acceptance_criteria>`rg -n "^## FedStat wide Parquet probe$|^## World Bank parquet probe$|^## CKAN resource path probe$" .planning/phases/01-data-architecture-research/extraction-probes.md` returns three section headers.</acceptance_criteria>
  <verify>
    <automated>rg -n "^## FedStat wide Parquet probe$|^## World Bank parquet probe$|^## CKAN resource path probe$" .planning/phases/01-data-architecture-research/extraction-probes.md</automated>
  </verify>
  <done>All three source families have deterministic probe evidence for coverage and extraction strategy, including no-data/rejection paths.</done>
</task>

</tasks>

<verification>
Check that retrieval evidence covers lexical, dense, rerank, and fallback states, and that the extraction probes explicitly cover FedStat, World Bank, and CKAN resource access.
</verification>

<success_criteria>
The phase contains executable evidence for the chosen search and extraction stack, not just architecture prose, with deterministic no-data and rejection handling represented explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-03-SUMMARY.md`
</output>
