---
phase: 02-jury-mvp
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-05-11T11:38:42.784Z
---

<current_state>
Paused on branch `codex/phase-2-jury-mvp-planning` while adding a clean headless observability layer for the Phase 2 data-agent workflow.

Current user priority: no Streamlit/UI work. The goal is to run many requests into the data agent from a coding-agent/headless path and preserve enough trace data to analyze how the workflow behaves, especially what goes into retrieval, what each LLM agent receives/returns, what deterministic tools execute, and how clarification cases are handled.

The new headless batch runner was already committed in `9445fa8 feat: add headless workflow batch runner`. The current uncommitted work adds `WorkflowAuditSession` and wires it into `scripts/run_workflow_batch.py`.
</current_state>

<completed_work>

- Graph-aware retrieval/runtime handoff - Done. Correct branch is `codex/phase-2-jury-mvp-planning`; Qdrant collection `phase1_source_cards` was verified green with `36321` vectors; dense and graph retrieval modes reported ready; live Yandex/Qwen smoke succeeded.
- Headless batch runner - Done and committed as `9445fa8`. It writes per-item `request.json`, `response.json`, summaries, workflow artifacts, and supports optional clarification follow-up.
- Observability approach - Decided. Do not make workflow agents write self-reports, do not modify prompts, and do not replace LLM work with handwritten logic. Use an external runtime audit layer around boundary calls.
- Audit implementation - Implemented but not committed. `app/observability/workflow_audit.py` patches boundary functions only inside a context manager and records JSONL streams for node events, LLM calls, retrieval calls, and deterministic tool calls.
- Batch-run integration - Implemented but not committed. `scripts/run_workflow_batch.py` wraps `run_user_query` and `continue_user_query` in `WorkflowAuditSession`, and summary items now include `audit_dir`.
- Verification - `py_compile` passed for `app/observability/workflow_audit.py` and `scripts/run_workflow_batch.py`.
- Audited smoke query - Done. Ran `"ВВП россии за 2011"` into the batch runner at `.planning/phases/02-jury-mvp/batch-runs/vvp-2011-audit`.
</completed_work>

<remaining_work>

- Review and commit the observability code: `app/observability/workflow_audit.py` and `scripts/run_workflow_batch.py`.
- Decide what to do with generated audit/run artifacts. Some JSONL evidence is ignored by repo-wide `*.jsonl`; either keep local, force-add selected evidence, or create a smaller committed summary.
- Run a broader batch over golden/realistic prompts and analyze recurring failure patterns.
- Fix workflow defects based on audit evidence, while preserving the rule that LLM reasoning stays live Yandex/Qwen and deterministic code only handles retrieval/extraction/validation/provenance.
</remaining_work>

<decisions_made>

- Do not touch Streamlit/UI in this workstream. User explicitly said UI is not interesting right now.
- Runtime query understanding and reasoning must go through live Yandex/Qwen. Offline/no-response fallback logic is a bug, not a feature.
- Observability must be external. The audit layer must capture inputs/outputs automatically and must not ask LLM agents to produce reports about their own work.
- Clarification in batch mode should be preserved as a first-class outcome. If a query has a `follow_up`, the runner should continue and save both initial and final responses.
</decisions_made>

<blockers>

- Commit/evidence policy is unresolved. The code is ready for review; generated batch/audit artifacts need an explicit keep/discard/summarize decision.
- Audit JSONL files are ignored by `.gitignore` via `*.jsonl`, even though they exist locally.
- The first audited query exposed workflow issues that still need fixes: `research_designer` overcomplicates a simple query, `extraction_planner` emits source `40570` despite coverage/retrieval pointing elsewhere, extraction returns `0 rows`, and critic returned `repair_plan` in the wrong type.
</blockers>

<context>
Current dirty tree:

- Modified: `scripts/run_workflow_batch.py`
- Untracked code: `app/observability/workflow_audit.py`
- Untracked run evidence: `.planning/phases/02-jury-mvp/batch-runs/vvp-2011-audit/`
- Untracked workflow artifacts: `.planning/phases/02-jury-mvp/workflow-runs/phase2-fb135f1cca03-q001/`
- Ignored but important local audit evidence:
  - `.planning/phases/02-jury-mvp/batch-runs/vvp-2011-audit/items/001-q001/audit/node-events.jsonl`
  - `.planning/phases/02-jury-mvp/batch-runs/vvp-2011-audit/items/001-q001/audit/llm-calls.jsonl`
  - `.planning/phases/02-jury-mvp/batch-runs/vvp-2011-audit/items/001-q001/audit/retrieval.jsonl`
  - `.planning/phases/02-jury-mvp/batch-runs/vvp-2011-audit/items/001-q001/audit/tool-calls.jsonl`

Audited query result:

- Query: `ВВП россии за 2011`
- Batch dir: `.planning/phases/02-jury-mvp/batch-runs/vvp-2011-audit`
- Workflow run id: `phase2-fb135f1cca03-q001`
- Final outcome: `not_found`
- Selected sources: `5`
- Rejected sources: `25`
- Audit counts: `28` LLM events, `20` node events, `16` retrieval events, `6` tool events

Important object-contract observation: workflow nodes pass a shared `Phase2State`, not separate per-agent DTOs. The audit layer captures each `node_start.input_state` and `node_end.output_state`, so it shows the exact object shape one layer leaves for the next.
</context>

<next_action>
Start with: run `git status --short --ignored app/observability .planning/phases/02-jury-mvp/batch-runs/vvp-2011-audit/items/001-q001/audit`, inspect the observability diff, then either commit the code-only audit layer or first decide how to preserve/summarize the generated audit evidence.
</next_action>
