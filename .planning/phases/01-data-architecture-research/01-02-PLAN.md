---
phase: 01-data-architecture-research
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-data-architecture-research/data-inventory.md
  - .planning/phases/01-data-architecture-research/embedding-corpus-contract.md
  - app/artifacts/source_cards.py
  - app/catalog/source_catalog.py
  - app/data/source_card_builders.py
  - scripts/build_source_cards.py
  - scripts/build_source_catalog.py
  - scripts/build_embedding_corpus.py
  - .planning/phases/01-data-architecture-research/source-cards-manifest.json
  - .planning/phases/01-data-architecture-research/source-catalog-manifest.json
  - .planning/phases/01-data-architecture-research/embedding-corpus-manifest.json
autonomous: true
requirements:
  - SRCH-03
  - SRCH-04
must_haves:
  truths:
    - "Phase 1 has a deterministic inventory of local dumps and bounded CKAN checks with no raw numeric claims from the LLM."
    - "FedStat, World Bank, and CKAN produce the same source-card and evidence-bundle shape."
    - "Source cards expose period, geography, units, provenance, and rejection-ready metadata for later retrieval work."
    - "Source cards expose a stable embedding document/chunk contract before dense retrieval or indexing starts."
    - "Source cards, schemas, coverage hints, embedding chunk ids, and rejection-ready metadata are materialized into a local SQLite/DuckDB catalog interface."
    - "The embedding corpus format is durable enough that later phases can consume the prepared index without reprocessing all sources by default."
    - "Verification runs the source-card, catalog, and embedding-corpus builders and validates generated manifests, not just code markers."
  artifacts:
    - path: ".planning/phases/01-data-architecture-research/data-inventory.md"
      provides: "Bounded inventory and verification notes"
    - path: ".planning/phases/01-data-architecture-research/embedding-corpus-contract.md"
      provides: "Stable embedding document/chunk format and index manifest contract"
    - path: "app/artifacts/source_cards.py"
      provides: "Typed SourceCandidateCard, EmbeddingDocument, and EvidenceBundle schemas"
    - path: "app/catalog/source_catalog.py"
      provides: "Local SQLite/DuckDB catalog interface for source cards and embedding chunks"
    - path: "app/data/source_card_builders.py"
      provides: "Per-source builders for FedStat, World Bank, and CKAN"
    - path: "scripts/build_source_cards.py"
      provides: "Deterministic source-card generation entrypoint"
    - path: "scripts/build_source_catalog.py"
      provides: "Deterministic local catalog build entrypoint"
    - path: "scripts/build_embedding_corpus.py"
      provides: "Deterministic embedding-corpus generation entrypoint"
    - path: ".planning/phases/01-data-architecture-research/source-cards-manifest.json"
      provides: "Committed manifest for generated source-card corpus counts, hashes, source families, and local artifact paths"
    - path: ".planning/phases/01-data-architecture-research/source-catalog-manifest.json"
      provides: "Committed manifest for local SQLite/DuckDB catalog path, tables, row counts, and queryability checks"
    - path: ".planning/phases/01-data-architecture-research/embedding-corpus-manifest.json"
      provides: "Committed manifest for generated embedding corpus chunks, hashes, metadata version, provider hints, and local artifact paths"
  key_links:
    - from: "scripts/build_source_cards.py"
      to: "app/data/source_card_builders.py"
      via: "Builder imports"
      pattern: "from app\\.data\\.source_card_builders import"
    - from: "app/data/source_card_builders.py"
      to: "app/artifacts/source_cards.py"
      via: "Typed return values"
      pattern: "SourceCandidateCard|EmbeddingDocument|EvidenceBundle"
    - from: "scripts/build_embedding_corpus.py"
      to: "app/artifacts/source_cards.py"
      via: "Embedding corpus serialization contract"
      pattern: "EmbeddingDocument|embedding_text|content_hash|metadata_version"
    - from: "scripts/build_source_catalog.py"
      to: "app/catalog/source_catalog.py"
      via: "Catalog build uses shared local catalog interface"
      pattern: "SourceCatalog|SQLite|DuckDB|source_cards|embedding_chunks"
---

<objective>
Create the deterministic source inventory, shared source-card contracts, and stable embedding-corpus format that retrieval, coverage preview, indexing, and orchestration will consume.

Purpose: Operationalize D-03 through D-12 and D-08A through D-08E by making FedStat, World Bank, and CKAN first-class, typed, bounded, and ready for durable embedding/index construction from the start.
Output: `data-inventory.md`, `embedding-corpus-contract.md`, typed source-card/embedding schemas, local catalog interface, per-source builders, and deterministic builder scripts.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/ARCHITECTURE_STACK.md
@.planning/DATA_REPORT.md
@requirements.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define source-card, evidence-bundle, and embedding document contracts</name>
  <files>app/artifacts/source_cards.py, .planning/phases/01-data-architecture-research/embedding-corpus-contract.md</files>
  <read_first>app/artifacts/source_cards.py, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/ARCHITECTURE_STACK.md, .planning/DATA_REPORT.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: `SourceCandidateCard` includes source, dataset/resource identity, title, match mode, units, geography, period coverage, provenance, availability flags, and quality flags.
    - Test 2: `EvidenceBundle` contains selected candidates, rejected candidates, coverage intent, and rejection reasons without raw numeric answer text.
    - Test 3: `EmbeddingDocument` or equivalent contains card id/source id, chunk id, source family, language, metadata version, content hash, embedding text, provenance fields, and provider hints for Yandex `text-search-doc`.
  </behavior>
  <action>Create `app/artifacts/source_cards.py` with Pydantic v2 models for `SourceCandidateCard`, `CoverageHint`, `RejectedCandidate`, `EmbeddingDocument`, `EmbeddingCorpusManifest`, and `EvidenceBundle`. Include match modes required by D-08: exact, lexical, semantic, proxy, ckan_discovery, methodology_match. Include a field for `builder_source` or equivalent to preserve deterministic provenance. `EmbeddingDocument.embedding_text` must be built from stable metadata fields: title/name, source family, dataset/indicator/resource codes, geography, period coverage, units, dimensions, source URL/resource URL, and descriptive metadata. It must not include raw numeric series or generated answer text. Write `embedding-corpus-contract.md` with the provider target, document/query split, chunk identity, hashing/versioning, local artifact boundaries, and manifest fields. Use ASCII-only identifiers and docstrings in code. Do not put deprecated old-phase paths into defaults or comments.</action>
  <acceptance_criteria>`rg -n "class SourceCandidateCard|class EvidenceBundle|class EmbeddingDocument|class EmbeddingCorpusManifest|ckan_discovery|methodology_match|content_hash|metadata_version" app/artifacts/source_cards.py` returns matches for all required schema elements, and `rg -n "text-search-doc|text-search-query|content_hash|metadata_version|source-card chunks|not raw numeric" .planning/phases/01-data-architecture-research/embedding-corpus-contract.md` returns the embedding contract markers.</acceptance_criteria>
  <verify>
    <automated>rg -n "class SourceCandidateCard|class EvidenceBundle|class EmbeddingDocument|class EmbeddingCorpusManifest|ckan_discovery|methodology_match|content_hash|metadata_version" app/artifacts/source_cards.py && rg -n "text-search-doc|text-search-query|content_hash|metadata_version|source-card chunks|not raw numeric" .planning/phases/01-data-architecture-research/embedding-corpus-contract.md</automated>
  </verify>
  <done>The repository has typed artifact contracts that can represent FedStat, World Bank, and CKAN candidates uniformly and serialize stable embedding documents for durable indexing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic inventory, source-card builders, local catalog, and embedding corpus generator</name>
  <files>.planning/phases/01-data-architecture-research/data-inventory.md, app/catalog/source_catalog.py, app/data/source_card_builders.py, scripts/build_source_cards.py, scripts/build_source_catalog.py, scripts/build_embedding_corpus.py</files>
  <read_first>.planning/phases/01-data-architecture-research/data-inventory.md, app/catalog/source_catalog.py, app/data/source_card_builders.py, scripts/build_source_cards.py, scripts/build_source_catalog.py, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/DATA_REPORT.md, .planning/STATE.md, app/artifacts/source_cards.py</read_first>
  <behavior>
    - Test 1: FedStat builder emits cards from verified metadata and flags wide-parquet normalization requirements.
    - Test 2: World Bank builder emits cards with indicator metadata, country/aggregate hints, and canonical provenance.
    - Test 3: CKAN builder uses bounded package/resource checks and records when resource inspection is skipped or truncated.
    - Test 4: The local catalog records source cards, schema/coverage hints, rejection-ready fields, and embedding chunk ids using SQLite or DuckDB.
    - Test 5: The embedding corpus generator emits deterministic source-card chunks plus a manifest with provider/model, chunk counts, hashes, metadata version, and local artifact paths.
    - Test 6: Builder verification fails if generated manifests are absent, empty, missing FedStat/World Bank/CKAN family coverage, or not queryable through the catalog interface.
  </behavior>
  <action>Create `app/data/source_card_builders.py` with functions for FedStat, World Bank, and CKAN source-card creation returning the typed models from `app/artifacts/source_cards.py`. Create `scripts/build_source_cards.py` as a deterministic CLI entrypoint that reads local metadata and performs bounded CKAN API calls only. Create `app/catalog/source_catalog.py` and `scripts/build_source_catalog.py` to materialize cards, schema/coverage hints, rejection-ready metadata, and embedding chunk ids into a local SQLite or DuckDB catalog. Create `scripts/build_embedding_corpus.py` to transform source cards into deterministic embedding documents and a manifest without calling the embedding API yet. The scripts must write local generated artifacts under `.local/dataagent/phase1/` and committed manifests under the phase directory: `source-cards-manifest.json`, `source-catalog-manifest.json`, and `embedding-corpus-manifest.json`. Write `data-inventory.md` summarizing: verified local dump paths, local metadata files used, bounded CKAN endpoints, inspected limits, known gaps such as incomplete `metadata.jsonl`, local catalog path/schema, generated artifact paths, manifest hashes/counts, and the handoff to the embedding corpus. Include the exact phrase `CKAN is first-class for discovery and data access` per D-04.</action>
  <acceptance_criteria>`rg -n "CKAN is first-class for discovery and data access|metadata.jsonl|bounded CKAN|embedding corpus|SQLite|DuckDB catalog|source-cards-manifest|embedding-corpus-manifest" .planning/phases/01-data-architecture-research/data-inventory.md` returns matches, `rg -n "def build_fedstat|def build_world_bank|def build_ckan" app/data/source_card_builders.py` returns three builder functions, `rg -n "SourceCatalog|SQLite|DuckDB|source_cards|embedding_chunks|rejection" app/catalog/source_catalog.py scripts/build_source_catalog.py` returns catalog markers, `rg -n "EmbeddingDocument|EmbeddingCorpusManifest|content_hash|metadata_version" scripts/build_embedding_corpus.py` returns corpus-generation markers, and the automated verification commands generate and validate the three manifests.</acceptance_criteria>
  <verify>
    <automated>rg -n "CKAN is first-class for discovery and data access|metadata.jsonl|bounded CKAN|embedding corpus|SQLite|DuckDB catalog|source-cards-manifest|embedding-corpus-manifest" .planning/phases/01-data-architecture-research/data-inventory.md && rg -n "def build_fedstat|def build_world_bank|def build_ckan" app/data/source_card_builders.py && rg -n "SourceCatalog|SQLite|DuckDB|source_cards|embedding_chunks|rejection" app/catalog/source_catalog.py scripts/build_source_catalog.py && rg -n "EmbeddingDocument|EmbeddingCorpusManifest|content_hash|metadata_version" scripts/build_embedding_corpus.py && python scripts/build_source_cards.py --manifest .planning/phases/01-data-architecture-research/source-cards-manifest.json && python scripts/build_source_catalog.py --source-cards-manifest .planning/phases/01-data-architecture-research/source-cards-manifest.json --manifest .planning/phases/01-data-architecture-research/source-catalog-manifest.json && python scripts/build_embedding_corpus.py --source-cards-manifest .planning/phases/01-data-architecture-research/source-cards-manifest.json --manifest .planning/phases/01-data-architecture-research/embedding-corpus-manifest.json && python - <<'PY'
import json
from pathlib import Path
base = Path('.planning/phases/01-data-architecture-research')
cards = json.loads((base / 'source-cards-manifest.json').read_text())
catalog = json.loads((base / 'source-catalog-manifest.json').read_text())
corpus = json.loads((base / 'embedding-corpus-manifest.json').read_text())
families = set(cards.get('source_families', []))
assert {'FedStat', 'World Bank', 'CKAN'}.issubset(families), families
assert cards.get('card_count', 0) > 0
assert catalog.get('source_cards_count', 0) == cards.get('card_count')
assert catalog.get('queryability_check') == 'passed'
assert corpus.get('chunk_count', 0) > 0
assert corpus.get('content_hash')
assert corpus.get('metadata_version')
print(cards['card_count'], corpus['chunk_count'])
PY</automated>
  </verify>
  <done>Deterministic inventory findings, three per-source builders, a local catalog, and a source-card embedding corpus generator exist and share the same typed card/chunk contract.</done>
</task>

</tasks>

<verification>
Confirm the inventory document cites only verified dump/API facts, the builder module exposes FedStat, World Bank, and CKAN functions returning shared typed cards, the local SQLite/DuckDB catalog stores queryable source metadata, and the embedding corpus generator creates stable chunks and a manifest without embedding raw numeric values.
</verification>

<success_criteria>
Phase 1 has a source-contract layer, local catalog, deterministic inventory artifact, and embedding corpus contract that later plans can use without re-reading raw dump structures, CKAN payloads, or redesigning dense retrieval inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-02-SUMMARY.md`
</output>
