---
phase: 02-jury-mvp
plan: "06"
type: execute
wave: 6
depends_on:
  - "02-03"
  - "02-05"
files_modified:
  - app/workflow/nodes/critic.py
  - app/workflow/nodes/visualization.py
  - app/workflow/nodes/narrator.py
  - app/workflow/graph.py
  - app/workflow/service.py
  - tests/test_phase2_finalization.py
autonomous: true
requirements:
  - DATA-05
  - ART-06
  - RBST-02
  - RBST-03
  - RBST-04
  - UI-02
must_haves:
  truths:
    - "Methodology Critic blocks passed outcomes when coverage, extraction, units, or provenance are insufficient."
    - "Narrator cannot introduce numeric values missing from DatasetArtifact records."
    - "Visualization is generated from DatasetArtifact, never from free-form LLM text."
    - "This plan is the first plan where run_user_query returns a complete WorkflowResponse."
    - "Passed WorkflowResponse objects include at least one downloadable ScriptArtifact."
    - "Clarification follow-up preserves pending state, merges the user's answer into intent, and can change the terminal outcome."
  artifacts:
    - "app/workflow/nodes/critic.py implements final-outcome guardrails."
    - "app/workflow/nodes/visualization.py creates VisualizationSpec from datasets."
    - "app/workflow/nodes/narrator.py creates answer blocks and messages."
    - "app/workflow/service.py implements run_user_query and continue_user_query over finalization nodes."
  key_links:
    - "graph.py routes critic verdicts to repair, clarification, not_found, or narrator."
    - "narrator.py reads DatasetArtifact records before emitting numeric text."
    - "build_workflow_response maps Phase2State.script_artifacts into WorkflowResponse.script_artifacts without lossy dict conversion."
    - "continue_user_query reads a pending clarification run artifact and reinvokes the graph with merged intent."
---

<objective>
Implement the critic, visualization, and narrator enforcement layer that turns internal workflow evidence into safe final responses.

Purpose: satisfy D-27 through D-30 and prevent attractive but unsupported final prose.
Output: tested finalization nodes, complete `run_user_query`, clarification follow-up, and graph wiring.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@app/workflow/graph.py
@app/workflow/service.py
@app/artifacts/workflow_artifacts.py
@app/data/deterministic_tools.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Methodology Critic final-outcome guard</name>
  <files>app/workflow/nodes/critic.py, app/workflow/graph.py, tests/test_phase2_finalization.py</files>
  <read_first>app/workflow/graph.py, app/artifacts/workflow_artifacts.py, .planning/phases/02-jury-mvp/02-CONTEXT.md</read_first>
  <behavior>
    - `passed` is impossible when any coverage report has `status != "ok"`.
    - `passed` is impossible when no DatasetArtifact has `status == "ok"`.
    - No-data cases require attempted selected/rejected source evidence before `not_found`.
    - Qwen/Yandex structured output is the target path for Methodology Critic; deterministic fallback is explicitly marked `test_only_critic_fallback`.
  </behavior>
  <action>Create `app/workflow/nodes/critic.py` with `run_methodology_critic(state: Phase2State, *, live_llm_required: bool) -> CritiqueReport` and `derive_final_outcome(state: Phase2State, critique: CritiqueReport) -> TerminalOutcome`. The target path must call `YandexAIStudioClient.structured_chat` for Methodology Critic per D-37, passing compact evidence from coverage, extraction, selected/rejected sources, and artifact manifests; tests may monkeypatch the client. If `live_llm_required=False`, allow deterministic fallback only with component status/open reasoning marker `test_only_critic_fallback`; plan 02-07 must exclude this marker from jury readiness. Enforce concrete checks after the LLM critique: coverage statuses all `ok` for `passed`; at least one dataset artifact with `status="ok"` and `rows > 0`; every dataset artifact has non-empty `provenance`; unit/frequency warnings become `pass_with_warnings`; unsupported source or missing coverage becomes `needs_repair`; ambiguous missing critical fields becomes `needs_user_clarification`; bounded checked/rejected sources with no usable coverage becomes `not_found`. Update `graph.py` to call these functions before visualization/narrator.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_finalization.py tests/test_phase2_workflow_service.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/nodes/critic.py` contains `def derive_final_outcome`; contains `passed outcome requires`; contains `test_only_critic_fallback`; tests assert a gated coverage report raises or maps away from `passed`.</acceptance_criteria>
  <done>Final status now derives from evidence truth, not from optimistic graph completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement visualization from DatasetArtifact</name>
  <files>app/workflow/nodes/visualization.py, tests/test_phase2_finalization.py</files>
  <read_first>app/data/deterministic_tools.py, app/artifacts/workflow_artifacts.py, .planning/ARCHITECTURE_STACK.md</read_first>
  <behavior>
    - Time-series datasets select `line`.
    - Comparative country/category datasets select `bar` or `grouped_line`.
    - Empty or clarification/not-found responses return skipped visualization with a reason.
  </behavior>
  <action>Create `app/workflow/nodes/visualization.py` with `build_visualization(dataset: DatasetArtifact | None, *, query_category: str) -> VisualizationSpec`. Use `render_visualization_from_dataset_artifact` for deterministic rendering, then set `chart_type` to `line` when columns include `period` and one geography, `grouped_line` when columns include `period` and multiple `geo_id` values, `bar` for category comparison without time, and `table` otherwise. Do not parse numbers from narrator text.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_finalization.py tests/test_deterministic_tools_and_trace.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/nodes/visualization.py` contains `render_visualization_from_dataset_artifact`; tests assert `chart_type == "grouped_line"` for multi-country time series.</acceptance_criteria>
  <done>Visualization is a deterministic artifact derived from dataset rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement source-bound narrator, service finalization, and clarification follow-up</name>
  <files>app/workflow/nodes/narrator.py, app/workflow/graph.py, app/workflow/service.py, tests/test_phase2_finalization.py</files>
  <read_first>app/artifacts/workflow_artifacts.py, app/workflow/nodes/critic.py, app/workflow/graph.py, app/workflow/service.py</read_first>
  <behavior>
    - Any number in `WorkflowResponse.message` must appear in DatasetArtifact records or provenance.
    - Passed responses include at least one downloadable script artifact from `Phase2State.script_artifacts`.
    - `needs_clarification` response includes concrete clarification questions and no dataset artifacts.
    - `not_found` response includes checked/rejected source evidence and no invented value.
    - Qwen/Yandex structured output is the target path for Narrator; deterministic fallback is explicitly marked `test_only_narrator_fallback`.
    - `continue_user_query(run_id, clarification_answer, ...)` loads pending clarification state, merges the user's answer into intent, reruns from the relevant state, and can change the final outcome from `needs_clarification` to `passed` or `not_found`.
  </behavior>
  <action>Create `app/workflow/nodes/narrator.py` with `build_workflow_response(state: Phase2State, *, final_outcome: TerminalOutcome, critique: CritiqueReport, visualization: VisualizationSpec | None, live_llm_required: bool) -> WorkflowResponse`. The target path must call `YandexAIStudioClient.structured_chat` for Narrator per D-37 with compact, source-bound context only: dataset summaries, script artifact summaries, provenance, critique, visualization spec, selected/rejected sources, and limitations. If `live_llm_required=False`, allow deterministic narrator fallback only with component status/open reasoning marker `test_only_narrator_fallback`; plan 02-07 must exclude this marker from jury readiness. Add helper `assert_message_numbers_are_supported(message: str, datasets: list[DatasetArtifact]) -> None` using regex for integers/decimals and a ledger built from dataset record values plus years/periods. Build `WorkflowResponse.dataset_artifacts` from `Phase2State.dataset_artifacts` and `WorkflowResponse.script_artifacts` from `Phase2State.script_artifacts` preserving `ScriptArtifact` fields from 02-03; do not down-convert them to incompatible dicts. For `passed`, validate at least one dataset artifact with rows and at least one script artifact whose path exists and is downloadable before constructing `WorkflowResponse`; failing that must raise or map away from `passed`. For direct lookup/comparison/research responses, include answer blocks `summary`, `methodology`, `limitations`, and `how_found`. For clarification responses, include concrete questions from `IntentFrame.missing_fields`, persist pending clarification state under `WorkflowRunConfig.artifact_dir`, and include feedback action `answer_clarification`. For not-found responses, include `NoDataExplanationArtifact` from attempted source and rejection evidence. Update `app/workflow/service.py` so `run_user_query` invokes the 02-05 graph through extraction and then critic -> visualization -> narrator, returning complete `WorkflowResponse`. Add `continue_user_query(run_id: str, clarification_answer: str, *, run_config: WorkflowRunConfig | None = None) -> WorkflowResponse`: load the pending run state, merge the user's answer into `IntentFrame.known_fields`/`missing_fields`, append a trace event, rerun from coverage/extraction when possible, and return a new complete response. Do not start over with no memory of selected/rejected sources unless the clarification changes the source family or indicator.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_finalization.py tests/test_phase2_contracts.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/nodes/narrator.py` contains `assert_message_numbers_are_supported`; contains `test_only_narrator_fallback`; contains `script_artifacts`; `app/workflow/service.py` contains `def continue_user_query`; tests assert unsupported number `999999` fails, clarification follow-up changes the outcome after merging an answer, and a `passed` response contains at least one script artifact with an existing downloadable `.py` path.</acceptance_criteria>
  <done>Narration is useful to users while preserving source-bound numeric guarantees, and clarification follow-up is a real service path.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_phase2_finalization.py tests/test_phase2_workflow_service.py tests/test_phase2_contracts.py -q`.
</verification>

<success_criteria>
Final response construction blocks bad outputs, creates deterministic visualization specs, forbids unsupported numeric claims, implements the first complete `run_user_query`, and supports clarification follow-up without losing pending state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-06-SUMMARY.md`.
</output>
