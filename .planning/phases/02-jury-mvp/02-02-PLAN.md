---
phase: 02-jury-mvp
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/retrieval/readiness.py
  - app/retrieval/hybrid_retrieval.py
  - scripts/run_retrieval_spike.py
  - tests/test_phase2_retrieval.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - RBST-03
  - ENG-03
must_haves:
  truths:
    - "The promoted Qdrant index is checked for current corpus hash and 36,321 vectors before readiness is claimed."
    - "Direct indicator queries rank direct metric cards above weak contextual matches."
    - "Selected and rejected source cards include source metadata and rejection reasons."
  artifacts:
    - "app/retrieval/readiness.py provides phase2 index readiness checks."
    - "app/retrieval/hybrid_retrieval.py applies domain-aware ranking and rejection reasons."
    - "scripts/run_retrieval_spike.py can emit Phase 2 retrieval evidence for all 20 golden cases."
  key_links:
    - "HybridRetriever reads .planning/phases/01-data-architecture-research/embedding-index-manifest.json."
    - "Retrieval eval output feeds app.evals.run_eval in plan 02-07."
---

<objective>
Promote retrieval readiness and ranking quality without rebuilding or re-embedding by default.

Purpose: honor D-18, D-19A, D-20, D-21, D-37, and the research finding that the 36,321-vector index is ready when manifest checks pass.
Output: strict readiness evidence and improved direct-indicator ranking over existing Qdrant/source-card artifacts.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@.planning/phases/02-jury-mvp/02-RESEARCH.md
@.planning/phases/01-data-architecture-research/golden-cases.yaml
@app/retrieval/embedding_index.py
@app/retrieval/hybrid_retrieval.py
@scripts/run_retrieval_spike.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 2 index readiness probe</name>
  <files>app/retrieval/readiness.py, tests/test_phase2_retrieval.py</files>
  <read_first>app/retrieval/embedding_index.py, app/demo/run_demo.py, .planning/phases/01-data-architecture-research/embedding-corpus-manifest.json, .planning/phases/01-data-architecture-research/embedding-index-manifest.json</read_first>
  <behavior>
    - A matching manifest with `status=ready`, `dense_status=ready`, and `vector_count=36321` returns `ready=true`.
    - A stale corpus hash returns `ready=false` and reason containing `corpus_hash_mismatch`.
    - A zero vector count returns `ready=false` and reason containing `vector_count_mismatch`.
  </behavior>
  <action>Create `app/retrieval/readiness.py` with `assess_phase2_index_readiness(index_manifest_path: Path, corpus_manifest_path: Path) -> dict[str, Any]`. It must compare `corpus_hash`, `chunk_count`, and `vector_count`; require `status == "ready"` and `dense_status == "ready"`; require `vector_count == chunk_count == 36321` for the current Phase 2 baseline; return keys `ready`, `status`, `dense_status`, `chunk_count`, `vector_count`, `qdrant_collection`, and `reasons`. Do not start `scripts/build_embedding_index.py` from this function; rebuilding is exceptional per D-04/D-19.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_retrieval.py -q</automated></verify>
  <acceptance_criteria>`app/retrieval/readiness.py` contains `def assess_phase2_index_readiness`; tests assert `36321`; tests assert `corpus_hash_mismatch` and `vector_count_mismatch`.</acceptance_criteria>
  <done>Executors can freeze/promote current Qdrant evidence without accidentally rebuilding the full corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Improve direct-indicator retrieval ranking</name>
  <files>app/retrieval/hybrid_retrieval.py, tests/test_phase2_retrieval.py</files>
  <read_first>app/retrieval/hybrid_retrieval.py, .planning/phases/01-data-architecture-research/phase1-test-acceptance.md, .planning/phases/02-jury-mvp/02-CONTEXT.md</read_first>
  <behavior>
    - Query `Какой ВВП России в 2024 году?` ranks direct GDP cards above a title containing `Удельный вес ... в валовом внутреннем продукте`.
    - A rejected weak contextual card includes a rejection reason containing `contextual_match_not_direct_indicator`.
    - Explicit source preference `World Bank` rejects FedStat candidates with reason `source_preference_mismatch`.
  </behavior>
  <action>In `app/retrieval/hybrid_retrieval.py`, add a domain-aware rerank helper used by `BGERerankerCompatible.rerank`: exact indicator/code/title match bonus `+2.5`, expected source-family bonus `+0.75`, direct GDP/CPI/population keyword bonus `+1.0`, and contextual penalty `-1.5` when the title contains Russian/English share/context phrases such as `удельный вес`, `доля`, `share`, `percent of GDP` and the query does not ask for a share. Ensure `split_rejections` adds concrete rejection reasons `contextual_match_not_direct_indicator` and `source_preference_mismatch` where applicable. Preserve `RetrievalCandidate` fields and Qdrant status fields.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_retrieval.py tests/test_hybrid_retrieval.py -q</automated></verify>
  <acceptance_criteria>`app/retrieval/hybrid_retrieval.py` contains `contextual_match_not_direct_indicator`; contains `source_preference_mismatch`; tests contain the exact weak title `Удельный вес бюджетных расходов на фундаментальные исследования в валовом внутреннем продукте`.</acceptance_criteria>
  <done>Known GC-001 source-ranking blocker is covered by a regression test and domain-aware ranking code.</done>
</task>

<task type="auto">
  <name>Task 3: Emit Phase 2 retrieval evidence fields</name>
  <files>scripts/run_retrieval_spike.py, tests/test_phase2_retrieval.py</files>
  <read_first>scripts/run_retrieval_spike.py, app/retrieval/hybrid_retrieval.py, .planning/phases/01-data-architecture-research/golden-cases.yaml</read_first>
  <action>Update `scripts/run_retrieval_spike.py` output CSV/Markdown so each row includes `case_id`, `expected_route`, `top_candidate`, `top_title`, `top_source_family`, `source_family_match`, `dense_status`, `index_manifest_status`, `qdrant_collection`, `selected_count`, `rejected_count`, and `rejection_reasons`. Add a CLI flag `--phase2-output-json` that writes a JSON object with `total_cases`, `ready_index`, `cases`, and `unacceptable_no_candidate_cases`. Cases may have no candidate only when expected terminal behavior is `not_found`; do not special-case a small demo subset per D-02.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_retrieval.py tests/test_hybrid_retrieval.py -q</automated></verify>
  <acceptance_criteria>`scripts/run_retrieval_spike.py` contains `--phase2-output-json`; CSV field list contains `rejected_count`; JSON output contains `unacceptable_no_candidate_cases`.</acceptance_criteria>
  <done>Retrieval evidence can feed the all-20 Phase 2 eval gate with selected/rejected source-card details.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_phase2_retrieval.py tests/test_hybrid_retrieval.py -q`.
</verification>

<success_criteria>
Current Qdrant readiness is checked strictly and retrieval has a tested path for direct-indicator intent beating weak contextual matches.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-02-SUMMARY.md`.
</output>
