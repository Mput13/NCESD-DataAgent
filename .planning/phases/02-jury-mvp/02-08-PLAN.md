---
phase: 02-jury-mvp
plan: "08"
type: execute
wave: 6
depends_on:
  - "02-05"
  - "02-07"
files_modified:
  - app/ui/streamlit_app.py
  - app/ui/trace_models.py
  - README.md
  - docs/PROJECT_WORKFLOW.md
  - .planning/phases/02-jury-mvp/manual-uat.md
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - ART-04
  - ART-05
  - ENG-01
  - ENG-02
  - ENG-03
  - ENG-04
  - RBST-01
must_haves:
  truths:
    - "A non-programmer can enter a query in Streamlit and the real workflow service runs."
    - "The UI renders message, sources, selected/rejected cards, coverage, extraction plan, dataset/script downloads, visualization, trace, and feedback/fix requests."
    - "Manual UAT records user feedback before Phase 2 readiness is claimed."
  artifacts:
    - "app/ui/streamlit_app.py calls app.workflow.service.run_user_query."
    - "README.md documents setup, run, eval, and architecture commands."
    - ".planning/phases/02-jury-mvp/manual-uat.md records manual testing evidence."
  key_links:
    - "Streamlit -> run_user_query -> WorkflowResponse -> rendered blocks."
    - "manual-uat.md references phase2-golden-results.json and Streamlit URL."
---

<objective>
Wire Streamlit to the real workflow, document reproducible run commands, and perform the required manual UAT checkpoint.

Purpose: satisfy D-31 through D-34 and D-38B/D-38C without building a polished custom frontend.
Output: simple jury test surface, docs, and manual feedback artifact.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@app/ui/streamlit_app.py
@app/ui/trace_models.py
@app/workflow/service.py
@app/artifacts/workflow_artifacts.py
@docs/PROJECT_WORKFLOW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Streamlit chat input to workflow service</name>
  <files>app/ui/streamlit_app.py, app/ui/trace_models.py</files>
  <read_first>app/ui/streamlit_app.py, app/ui/trace_models.py, app/workflow/service.py, app/artifacts/workflow_artifacts.py</read_first>
  <behavior>
    - Streamlit stores `messages`, `latest_response`, `latest_run_id`, and `feedback` in `st.session_state`.
    - Query submission calls `run_user_query`, not `assess_demo_readiness`.
    - The rendered response includes trace, selected sources, rejected sources, coverage, extraction plan, dataset/script artifacts, visualization, limitations, clarification/not-found blocks, and feedback actions.
  </behavior>
  <action>Update `app/ui/streamlit_app.py` so `st.chat_input("Введите экономический запрос")` invokes `run_user_query(prompt, run_config=WorkflowRunConfig.default())`. Keep readiness display available as a sidebar/status block, but product answer rendering must consume `WorkflowResponse`. Add render helpers for `answer_blocks`, `citations`, `selected_sources`, `rejected_sources`, `coverage`, `extraction_plan`, `dataset_artifacts`, `script_artifacts`, `visualization`, `trace_events`, `limitations`, `clarification_questions`, `not_found_evidence`, and `feedback_actions`. Use `st.download_button` for CSV/script paths when files exist. Do not add decorative frontend polish or a custom frontend.</action>
  <verify><automated>python3 -m pytest tests/test_demo_readiness.py tests/test_phase2_contracts.py -q</automated></verify>
  <acceptance_criteria>`app/ui/streamlit_app.py` contains `run_user_query`; contains `WorkflowRunConfig.default`; contains `st.download_button`; no code path uses chat input only to set `active_query` without running workflow.</acceptance_criteria>
  <done>Streamlit is a simple test surface for the same workflow used by evals.</done>
</task>

<task type="auto">
  <name>Task 2: Document run, eval, and architecture commands</name>
  <files>README.md, docs/PROJECT_WORKFLOW.md, .planning/phases/02-jury-mvp/manual-uat.md</files>
  <read_first>README.md (new file if absent), docs/PROJECT_WORKFLOW.md, .planning/ROADMAP.md, .planning/phases/02-jury-mvp/02-CONTEXT.md</read_first>
  <action>Update `README.md` with sections `## Setup`, `## Run Streamlit Jury UI`, `## Run All-20 Golden Acceptance`, `## Source-Bound Architecture`, and `## Secrets`. Include exact commands: `python3 -m pip install -r requirements.txt`, `PYTHONPATH=. python3 -m streamlit run app/ui/streamlit_app.py --server.port 8501`, `python3 scripts/run_phase2_acceptance.py --goldens .planning/phases/01-data-architecture-research/golden-cases.yaml --json-output .planning/phases/02-jury-mvp/phase2-golden-results.json --markdown-output .planning/phases/02-jury-mvp/phase2-golden-results.md --artifact-dir .planning/phases/02-jury-mvp/workflow-runs`, and `python3 -m pytest -q`. Update `docs/PROJECT_WORKFLOW.md` only to point Phase 2 users at those commands. Create `.planning/phases/02-jury-mvp/manual-uat.md` with checklist headings for pipeline traversal, trace readability, source selection/rejection, deterministic provenance, dataset/script downloads, frontend response format, clarification behavior, not-found honesty, and feedback/fix-request flow.</action>
  <verify><automated>python3 -m pytest -q</automated></verify>
  <acceptance_criteria>`README.md` contains `Run All-20 Golden Acceptance`; contains `streamlit run app/ui/streamlit_app.py`; `manual-uat.md` contains `feedback/fix-request flow`.</acceptance_criteria>
  <done>Jury/demo setup and acceptance commands are reproducible from committed docs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual Streamlit UAT and feedback incorporation</name>
  <files>.planning/phases/02-jury-mvp/manual-uat.md</files>
  <read_first>.planning/phases/02-jury-mvp/manual-uat.md, README.md, app/ui/streamlit_app.py, .planning/phases/01-data-architecture-research/golden-cases.yaml</read_first>
  <action>Start the UI with `PYTHONPATH=. python3 -m streamlit run app/ui/streamlit_app.py --server.port 8501`. Manually test at least these prompts: GC-001 `Какой ВВП России в 2024 году?`, GC-003 `Сравни динамику ВВП стран БРИКС за 2015-2024 годы.`, GC-009 `Дай данные по инфляции.`, GC-011 `Найди официальную инфляцию в Северной Корее за 2024 год.`, and GC-013 `Найди показатель ЕМИСС 57319 и покажи доступные ресурсы.` Record observations in `.planning/phases/02-jury-mvp/manual-uat.md` under each checklist heading. Fix any implementation issue that blocks the valid outcomes `passed`, `needs_clarification`, or `not_found`; document any user-requested follow-up that is outside Phase 2 only if it does not lower D-01/D-02 acceptance.</action>
  <verify><automated>python3 scripts/run_phase2_acceptance.py --goldens .planning/phases/01-data-architecture-research/golden-cases.yaml --json-output .planning/phases/02-jury-mvp/phase2-golden-results.json --markdown-output .planning/phases/02-jury-mvp/phase2-golden-results.md --artifact-dir .planning/phases/02-jury-mvp/workflow-runs</automated></verify>
  <acceptance_criteria>`manual-uat.md` contains dated entries for GC-001, GC-003, GC-009, GC-011, and GC-013; `phase2-golden-results.json` contains `"total_cases": 20`; no case has final outcome `gated`, `stale`, `skipped_with_reason`, `no_candidate`, or `ok`.</acceptance_criteria>
  <done>User has manually verified the quick UI/workflow surface and feedback is incorporated or explicitly recorded before readiness is claimed.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest -q`, run the all-20 acceptance command, then perform the manual UAT checkpoint at `http://localhost:8501`.
</verification>

<success_criteria>
The Streamlit surface runs the evaluated workflow, documentation is reproducible, and manual UAT evidence exists before Phase 2 can be marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-08-SUMMARY.md`.
</output>
