---
phase: 01-data-architecture-research
task: 7
total_tasks: 7
status: paused_with_background_embedding_running
last_updated: 2026-05-10T07:34:18.858Z
---

<current_state>
Phase 1 plans were completed, but the user correctly challenged the data-preparation result: the accepted Plan 05 readiness docs still described an 11-card demo subset, while the phase goal requires all available sources to be prepared for embeddings. The recovery work rebuilt the full local source-card/catalog/corpus artifacts for FedStat and World Bank.

Current prepared-data truth:

- Source cards: `.planning/phases/01-data-architecture-research/source-cards-manifest.json` -> `.local/dataagent/phase1/source-cards.json`, `36321` cards.
- SQLite catalog: `.planning/phases/01-data-architecture-research/source-catalog-manifest.json` -> `.local/dataagent/phase1/source-catalog.sqlite`, `36321` source cards.
- Embedding corpus: `.planning/phases/01-data-architecture-research/embedding-corpus-manifest.json` -> `.local/dataagent/phase1/embedding-corpus.jsonl`, `36321` chunks.
- Partial dev index: `.planning/phases/01-data-architecture-research/partial-embedding-index-manifest.json`, collection `phase1_source_cards_partial`, `5748` vectors at the time it was built, FedStat-only coverage.

Full embedding build is still running. At handoff, `.local/dataagent/phase1/embedding-cache.jsonl` had `9630 / 36321` rows. PID file `.local/dataagent/phase1/embedding-build.pid` points to `77528`; command uses `batch-size 64` and `workers 3`.

Do not stop the regular embedding check. The heartbeat automation `monitor-full-embedding-build` is ACTIVE every five minutes and must stay active until the full build finishes.
</current_state>

<completed_work>

- Plan 01: Requirements map, golden cases, and eval rubric - Done.
- Plan 02: Prepared-data contracts/builders/catalog/corpus - Done originally, then superseded by the full corpus rebuild.
- Plan 03: Qdrant embedding/search path and retrieval eval - Done originally, then superseded by the full vector build now in progress.
- Plan 04: Qwen/Yandex gates, runnable graph smoke, deterministic extraction probes, and trace UI models - Done.
- Plan 05: Demo readiness package and Streamlit diagnostic shell - Done, but readiness artifacts must be refreshed after the full rebuild because they still describe the old 11-card corpus.
- Full local source-card rebuild - Done for FedStat and World Bank: `36321` cards/chunks after deterministic duplicate handling.
- Long FedStat metadata embedding issue - Fixed by bounding `SourceCandidateCard.to_embedding_text()` in `app/artifacts/source_cards.py`.
- Yandex embedding environment and Qdrant client compatibility - Fixed in `app/retrieval/embedding_index.py`, including `.env` loading, document/query model defaults, current qdrant-client search compatibility, retry/backoff, and collection recreation support.
- Resumable embedding build - Implemented in `scripts/build_embedding_index.py` with append-only cache, `--batch-size`, and `--workers`.
- Partial snapshot tooling - Added `scripts/build_partial_embedding_snapshot.py`; partial FedStat source-discovery smokes returned meaningful hits.
- Embedding monitor tooling - Added `scripts/monitor_embedding_build.py`; heartbeat automation also monitors/restarts from the current thread.
- Targeted tests passed: `python3 -m pytest tests/test_embedding_index.py tests/test_source_catalog_and_corpus.py tests/test_hybrid_retrieval.py -q` -> `7 passed`.
</completed_work>

<remaining_work>

- Let full embedding build complete, or let the heartbeat restart it if the PID stops before cache count reaches `36321`.
- Verify `.planning/phases/01-data-architecture-research/embedding-index-manifest.json` after completion: status should be `ready`, corpus hash should match the current `embedding-corpus-manifest.json`, and vector count should be `36321`.
- Refresh `.planning/phases/01-data-architecture-research/prepared-data-readiness.md` and `demo-readiness.json`; both still reflect the old 11-card gated demo state.
- Run source-discovery/agent smokes on the full Qdrant collection for FedStat and World Bank queries.
- Keep CKAN cases explicit as gated unless bounded CKAN/NSED preparation is added.
- Promote 2-3 golden cases into deterministic extraction smokes after retrieval returns relevant source cards.
- Run `$gsd-verify-work 1` only after full-index readiness and refreshed evidence are available.
</remaining_work>

<decisions_made>

- Prepare all available local FedStat and World Bank sources, not only the old 11-card demo subset, because the user clarified the phase goal is full data preparation.
- Keep CKAN as a trusted catalog API but gated for now; it was not part of the local full rebuild.
- Use Qdrant for vector storage and Yandex embeddings for document/query vectors; do not introduce a custom vector index.
- Use `workers=3` for Yandex embedding calls. `workers=6` hit rate limits.
- Use append-only `.local/dataagent/phase1/embedding-cache.jsonl` as the progress source of truth.
- Keep partial index work isolated in `.local/qdrant-partial-dev` / `phase1_source_cards_partial` so development can continue without disturbing the full collection.
- Increase Yandex embedding retries to survive transient DNS/provider instability during long builds.
</decisions_made>

<blockers>

- Full embedding build is still in progress and depends on Yandex API/network stability. Workaround: resumable cache plus heartbeat restarts.
- Readiness docs are stale relative to the full manifests. Workaround: treat full manifests as truth until docs are regenerated.
- CKAN is not yet fully prepared in the current full corpus. Workaround: keep CKAN demo/eval cases gated or add bounded CKAN preparation explicitly.
- Deterministic numeric extraction is still probe-level for the demo; source-bound numeric answers need promoted golden cases.
</blockers>

<background_processes>

- Full embedding build: PID `77528` at handoff, PID file `.local/dataagent/phase1/embedding-build.pid`, cache progress `9630/36321`.
- Heartbeat automation: `monitor-full-embedding-build`, status `ACTIVE`, schedule `FREQ=MINUTELY;INTERVAL=5`. Do not pause/delete it until completion.
</background_processes>

<modified_files>

- `.planning/phases/01-data-architecture-research/source-cards-manifest.json`
- `.planning/phases/01-data-architecture-research/source-catalog-manifest.json`
- `.planning/phases/01-data-architecture-research/embedding-corpus-manifest.json`
- `.planning/phases/01-data-architecture-research/partial-embedding-index-manifest.json`
- `app/artifacts/source_cards.py`
- `app/retrieval/embedding_index.py`
- `scripts/build_embedding_index.py`
- `scripts/build_partial_embedding_snapshot.py`
- `scripts/build_source_cards.py`
- `scripts/monitor_embedding_build.py`
- `tests/test_embedding_index.py`
- `tests/test_source_catalog_and_corpus.py`
</modified_files>

<context>
The important mental model: Phase 1 is not ready merely because plans were checked off. It becomes ready when the data product is honest: full prepared corpus, full embedding index, refreshed readiness evidence, source discovery working over actual vectors, and deterministic extraction boundaries visible. The partial FedStat vector snapshot is good enough for development/smoke work, but not enough to claim full readiness.
</context>

<next_action>
Start with: `wc -l .local/dataagent/phase1/embedding-cache.jsonl .local/dataagent/phase1/embedding-corpus.jsonl` and `ps -p $(cat .local/dataagent/phase1/embedding-build.pid) -o pid=,etime=,command=`. If the process is alive and cache is growing, leave it alone. If the cache reaches `36321`, verify the manifest/Qdrant vector count and regenerate readiness artifacts.
</next_action>
