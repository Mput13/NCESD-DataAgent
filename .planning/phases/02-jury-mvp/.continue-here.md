---
context: phase
phase: 02-jury-mvp
task: null
total_tasks: null
status: paused
last_updated: 2026-05-15T09:13:30Z
---

# Resume Point — Phase 02 Jury MVP

## Current State

Phase 02 is in the bug-fix and acceptance-eval loop. Variant A fixes and Variant B codegen extraction are now consolidated on branch `feat/fix-after-matmod`.

The old feature worktree path `/Users/a/MAI/matmod/.claude/worktrees/feat+variant-b-codegen-extraction` is being retired. Future work should happen from the primary checkout `/Users/a/MAI/matmod` on branch `feat/fix-after-matmod`.

## Completed Work

- Variant A fixes: graceful degradation, UI/trace labels, prompt and pipeline fixes.
- Import cleanup: `ArtifactStore` export and source catalog import path cleanup.
- Variant B: codegen extraction fallback in deterministic tools.
- Source-family fix: World Bank IDs with underscores/lowercase dots resolve as `world_bank`, and fallback prefers `source_card.source_family`.
- Post-Claude functional fixes: shared source-family routing, public parquet resolver aliases, codegen `FROM source` contract, value-column normalization, critic/narrator semantics.

## Remaining Work

**HIGH** — Rerun targeted tests after the branch refresh.

**HIGH** — Diagnose remaining Phase 2 failures before claiming all-20 acceptance. Start with GC-011/GC-015 codegen behavior, `ds=1` but `not_found` cases, and narrator/API error paths.

**MEDIUM** — Rerun all 20 golden cases from `/Users/a/MAI/matmod` once `.env`, `.local`, and Qdrant are available.

## Critical Constraints

- No offline/no-response LLM fallback paths. Runtime LLM failures must surface as explicit gated/error artifacts.
- Numeric values must come from deterministic tools or trusted source adapters, not LLM memory.
- Variant B remains a fallback inside deterministic tools, not a new graph node.
- Generated `batch-runs/` and `workflow-runs/` artifacts from the retired worktree are not canonical code state.

## Next Action

1. Use `/Users/a/MAI/matmod` on branch `feat/fix-after-matmod`.
2. Ensure `.env`, `.local`, and Qdrant are available.
3. Rerun targeted tests.
4. Diagnose remaining Phase 2 batch failures.
5. Run the all-20 golden acceptance batch only after the targeted fixes are understood.
