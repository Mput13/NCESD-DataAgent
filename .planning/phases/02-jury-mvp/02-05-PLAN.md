---
phase: 02-jury-mvp
plan: "05"
type: execute
wave: 5
depends_on:
  - "02-01"
  - "02-03"
  - "02-04"
  - "02-10"
files_modified:
  - app/workflow/state.py
  - app/workflow/graph.py
  - app/workflow/nodes/deterministic_tools.py
  - app/workflow/service.py
  - app/workflow/run_graph.py
  - tests/test_phase2_workflow_service.py
autonomous: true
requirements:
  - NLU-01
  - NLU-02
  - NLU-03
  - NLU-04
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - ART-01
  - ART-02
  - ART-03
  - ART-04
  - ART-05
  - ART-06
  - RBST-01
  - RBST-02
  - RBST-03
  - RBST-04
  - ENG-02
  - ENG-03
must_haves:
  truths:
    - "A user query can run through the shared graph/service path up to deterministic extraction with explicit finalization_pending status."
    - "The graph exposes Supervisor, Intent Analyst, live Research Designer/direct path, scouts, coverage, extraction planner, and deterministic tools states."
    - "The deterministic_tools node consumes ExtractionPlan operations and produces DatasetArtifact plus ScriptArtifact objects."
    - "Plan 02-05 does not return fake final WorkflowResponse objects before critic, visualization, and narrator are implemented in plan 02-06."
  artifacts:
    - "app/workflow/state.py defines the Phase 2 typed state."
    - "app/workflow/graph.py builds a LangGraph StateGraph."
    - "app/workflow/nodes/deterministic_tools.py dispatches extraction plans to source-specific adapters."
    - "app/workflow/service.py implements a non-final graph execution helper and keeps run_user_query finalization pending."
    - "app/workflow/run_graph.py delegates to the service."
  key_links:
    - "service.py calls graph.py."
    - "graph.py calls app.workflow.nodes.* from plan 02-04, deterministic adapters/export helpers from plan 02-03, and reads golden-coverage-matrix.json from plan 02-10 for case hints."
    - "run_graph.py and Streamlit/evals use service.py, not separate logic."
---

<objective>
Build the real Phase 2 workflow runtime through deterministic extraction over the previously defined contracts, matrix, and nodes.

Purpose: replace the Phase 1 smoke graph per D-11/D-12/D-13/D-14/D-15A while preserving trace and source-bound invariants.
Output: LangGraph-backed extraction-ready graph, live Qwen intent/research target path, explicit `finalization_pending`, and updated CLI path.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@.planning/phases/02-jury-mvp/remote-workstream-review.md
@app/workflow/service.py
@app/workflow/graph_contract.py
@app/workflow/run_graph.py
@app/workflow/nodes/scouts.py
@app/workflow/nodes/coverage.py
@app/workflow/nodes/extraction_planner.py
@app/workflow/nodes/deterministic_tools.py
@app/data/fedstat_adapter.py
@app/data/world_bank_adapter.py
@app/data/ckan_adapter.py
@app/llm/yandex_ai_studio.py
@.planning/phases/02-jury-mvp/golden-coverage-matrix.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 2 graph state and live intent/research services</name>
  <files>app/workflow/state.py, tests/test_phase2_workflow_service.py</files>
  <read_first>app/workflow/graph_contract.py, app/artifacts/workflow_artifacts.py, app/llm/yandex_ai_studio.py</read_first>
  <behavior>
    - Phase 2 state includes `run_id`, `query`, `intent`, `research_design`, `evidence`, `coverage_reports`, `extraction_plan`, `dataset_artifacts`, `script_artifacts`, `final_outcome`, `finalization_pending`, `trace_events`, and `component_statuses`.
    - Ambiguous golden cases GC-001, GC-006, GC-009, and GC-010 set `needs_clarification=True`.
    - Qwen/Yandex structured output is the target path; deterministic classifier fallback is explicitly marked `test_only`.
    - Research Designer uses Qwen/Yandex structured output in the target path; deterministic fallback is explicitly marked `test_only_research_design_fallback`.
  </behavior>
  <action>Create `app/workflow/state.py` with `Phase2State(TypedDict, total=False)` containing the fields above and helper `new_run_id() -> str` returning prefix `phase2-`. Type `dataset_artifacts` as `list[DatasetArtifact]` and `script_artifacts` as `list[ScriptArtifact]` imported from `app.artifacts.workflow_artifacts` so the 02-01 response contract remains compatible through graph/finalization. Add `class IntentAnalysisResult(BaseModel)` if needed, but reuse `IntentFrame` as the durable artifact. Implement `analyze_intent(query: str, *, live_llm_required: bool) -> IntentFrame`: target path calls `YandexAIStudioClient.structured_chat` using the verified `app.llm.yandex_ai_studio` client; tests may monkeypatch it. Implement `design_research(intent: IntentFrame, *, live_llm_required: bool, matrix_hint: dict[str, Any] | None = None) -> ResearchDesignArtifact` using Qwen/Yandex structured output for the target Research Designer role per D-37. If `live_llm_required=False`, allow deterministic fallback with `open_reasoning` containing `test_only_intent_fallback` or `test_only_research_design_fallback`; component statuses must mark those fallbacks as `test_only` so plan 02-07 can exclude them from jury readiness. Never return a fake live Qwen success when credentials are missing per D-37/D-38.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_service.py tests/test_yandex_ai_studio.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/state.py` contains `class Phase2State`; contains `phase2-`; contains `script_artifacts`; contains `test_only_intent_fallback`; contains `test_only_research_design_fallback`; tests monkeypatch `YandexAIStudioClient`.</acceptance_criteria>
  <done>Graph state, intent analysis, and Research Designer have real-Qwen target paths and explicit test fallbacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build LangGraph workflow through extraction with finalization_pending</name>
  <files>app/workflow/graph.py, app/workflow/nodes/deterministic_tools.py, app/workflow/service.py, tests/test_phase2_workflow_service.py</files>
  <read_first>app/workflow/service.py, app/workflow/state.py, app/workflow/nodes/scouts.py, app/workflow/nodes/coverage.py, app/workflow/nodes/extraction_planner.py, app/artifacts/workflow_artifacts.py, app/data/fedstat_adapter.py, app/data/world_bank_adapter.py, app/data/ckan_adapter.py, app/data/deterministic_tools.py</read_first>
  <behavior>
    - `build_phase2_graph()` returns a compiled LangGraph graph with named nodes through deterministic tools.
    - `run_user_query_to_pending_finalization()` returns `Phase2State` with `finalization_pending=True`.
    - `run_deterministic_tools(state)` consumes `ExtractionPlan`, dispatches by `source_family`, persists dataset/script artifacts, appends TraceEvent/status, and a matrix-backed passed case produces extracted rows.
    - `run_user_query()` still raises the plan 02-06 `NotImplementedError` rather than returning a fake `WorkflowResponse`.
  </behavior>
  <action>Create `app/workflow/graph.py` using `langgraph.graph.StateGraph`, `START`, and `END`. Add nodes named exactly `supervisor`, `intent_analyst`, `research_designer`, `source_scouts`, `coverage_schema`, `extraction_planner`, `deterministic_tools`, and `finalization_pending`. Create `app/workflow/nodes/deterministic_tools.py` with `run_deterministic_tools(state: Phase2State, *, output_dir: Path) -> Phase2State`. This node must consume `state["extraction_plan"]` as an `ExtractionPlan`, dispatch each extractable operation by `source_family`: `fedstat` -> `extract_fedstat_dataset`, `world_bank` -> `extract_world_bank_dataset`, `ckan` -> `extract_ckan_dataset`; call `export_dataset_with_script` for successful datasets; persist DatasetArtifact, ScriptArtifact, generated script, manifest, and extraction-log artifacts under the run artifact directory; append a TraceEvent with tool name, source family, artifact ids, row count, and status; and update `component_statuses["deterministic_tools"]`. Unsupported or skipped operations must append explicit reasons and must not fabricate rows. Add a test that a passed matrix-backed case reaches the deterministic_tools node and produces at least one DatasetArtifact with rows and one ScriptArtifact with an existing `.py` path. Implement conditional routing: ambiguous intent -> `finalization_pending` with pending reason `needs_clarification_finalization_pending`; no selected sources after bounded search -> `finalization_pending` with pending reason `not_found_finalization_pending`; otherwise coverage -> extraction_planner -> deterministic_tools -> `finalization_pending`. Use `.planning/phases/02-jury-mvp/golden-coverage-matrix.json` as optional case hints for source family, source_id/card_id, filters, and expected terminal outcome; do not hardcode a small demo subset. Update `app/workflow/service.py` with `run_user_query_to_pending_finalization(query: str, *, run_config: WorkflowRunConfig | None = None) -> Phase2State` that builds/invokes this graph and writes state artifacts under `WorkflowRunConfig.artifact_dir`. Keep `run_user_query` raising `NotImplementedError("Phase 2 final WorkflowResponse implementation is provided by plan 02-06")` until plan 02-06 wires critic, visualization, and narrator.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_service.py tests/test_phase2_workflow_nodes.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/graph.py` contains `StateGraph`; contains `finalization_pending`; `app/workflow/nodes/deterministic_tools.py` contains `def run_deterministic_tools`; contains `extract_fedstat_dataset`, `extract_world_bank_dataset`, `extract_ckan_dataset`, and `export_dataset_with_script`; `app/workflow/service.py` contains `def run_user_query_to_pending_finalization`; `app/workflow/service.py` still contains `NotImplementedError("Phase 2 final WorkflowResponse implementation is provided by plan 02-06")`.</acceptance_criteria>
  <done>The product workflow has one executable graph path through extraction, with explicit pending finalization instead of fake final output.</done>
</task>

<task type="auto">
  <name>Task 3: Update graph CLI to use the service path</name>
  <files>app/workflow/run_graph.py, tests/test_phase2_workflow_service.py</files>
  <read_first>app/workflow/run_graph.py, app/workflow/service.py, .planning/phases/01-data-architecture-research/golden-cases.yaml</read_first>
  <action>Refactor `app/workflow/run_graph.py` so the CLI supports both `--case-index` and `--query`. It must load a golden case and matrix hint when `--case-index` is provided, call `run_user_query_to_pending_finalization(query, run_config=WorkflowRunConfig.default().model_copy(update={"case_id": case_id}))`, and write serialized state JSON to `--json-output` with `status="finalization_pending"`. Preserve module invocation `python3 -m app.workflow.run_graph`; do not keep a separate Phase 1-only success path for user queries. Plan 02-06 will change the CLI to emit final `WorkflowResponse` once finalization nodes exist.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_workflow_service.py tests/test_workflow_graph.py -q</automated></verify>
  <acceptance_criteria>`app/workflow/run_graph.py` contains `run_user_query_to_pending_finalization`; parser contains `--query`; output JSON contains `finalization_pending` in tests.</acceptance_criteria>
  <done>Workflow CLI, future evals, and future Streamlit share the same pre-finalization service execution path.</done>
</task>

</tasks>

<verification>
Run `python3 -m pytest tests/test_phase2_workflow_service.py tests/test_phase2_workflow_nodes.py tests/test_workflow_graph.py -q`.
</verification>

<success_criteria>
The Phase 2 service can execute architecture-stack roles through deterministic extraction and persist a `finalization_pending` state for plan 02-06 to turn into strict response outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-05-SUMMARY.md`.
</output>
