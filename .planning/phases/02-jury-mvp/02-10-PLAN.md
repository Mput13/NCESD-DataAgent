---
phase: 02-jury-mvp
plan: "10"
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
  - "02-04"
files_modified:
  - scripts/build_phase2_coverage_matrix.py
  - .planning/phases/02-jury-mvp/golden-coverage-matrix.json
  - .planning/phases/02-jury-mvp/golden-coverage-matrix.md
  - tests/test_phase2_coverage_matrix.py
autonomous: true
requirements:
  - NLU-01
  - NLU-02
  - NLU-03
  - NLU-04
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - ART-01
  - ART-02
  - ART-03
  - ART-04
  - ART-05
  - ART-06
  - RBST-01
  - RBST-02
  - RBST-03
  - RBST-04
must_haves:
  truths:
    - "Every one of the 20 golden cases has a planned source family, source/card id, filters, expected terminal outcome, required adapter, and artifact expectation."
    - "Cases expected to be `not_found` or `needs_clarification` include explicit missing-data or ambiguity evidence instead of empty candidate placeholders."
    - "Final acceptance cannot run against an unknown data-coverage surface."
  artifacts:
    - "scripts/build_phase2_coverage_matrix.py creates the all-20 matrix from golden cases, Phase 1 source cards, local dumps, and retrieval evidence."
    - ".planning/phases/02-jury-mvp/golden-coverage-matrix.json stores machine-readable per-case coverage and extraction expectations."
    - ".planning/phases/02-jury-mvp/golden-coverage-matrix.md stores human-readable evidence for jury/debug review."
    - "tests/test_phase2_coverage_matrix.py enforces exactly 20 mapped cases and no missing required adapter fields."
  key_links:
    - "build_phase2_coverage_matrix.py reads .planning/phases/01-data-architecture-research/golden-cases.yaml and Phase 1 source-card/catalog manifests."
    - "Matrix source families map directly to fedstat_adapter, world_bank_adapter, or ckan_adapter extraction paths."
    - "Plan 02-05 workflow and plan 02-07 acceptance runner consume golden-coverage-matrix.json."
---

<objective>
Create a real all-20 golden coverage/extraction matrix before final workflow acceptance.

Purpose: close the gap between a gated all-20 eval and actual data coverage by forcing each case to map to a source family, source/card id, filters, expected terminal outcome, required adapter, artifact expectations, and missing-data evidence.
Output: machine-readable and human-readable coverage matrix plus tests.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@.planning/phases/01-data-architecture-research/golden-cases.yaml
@.planning/phases/01-data-architecture-research/source-catalog-manifest.json
@.planning/phases/01-data-architecture-research/source-cards-manifest.json
@.planning/phases/02-jury-mvp/qdrant-server-manifest.json
@app/data/fedstat_adapter.py
@app/data/world_bank_adapter.py
@app/data/ckan_adapter.py
@app/workflow/nodes/coverage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build all-20 golden coverage matrix generator</name>
  <files>scripts/build_phase2_coverage_matrix.py, tests/test_phase2_coverage_matrix.py</files>
  <read_first>.planning/phases/01-data-architecture-research/golden-cases.yaml, .planning/phases/01-data-architecture-research/source-catalog-manifest.json, .planning/phases/01-data-architecture-research/source-cards-manifest.json, app/workflow/nodes/coverage.py, app/data/fedstat_adapter.py, app/data/world_bank_adapter.py, app/data/ckan_adapter.py</read_first>
  <behavior>
    - The generator loads exactly 20 golden cases by default.
    - Every case row includes `case_id`, `source_family`, `source_id`, `card_id`, `filters`, `expected_terminal_outcome`, `required_adapter`, `artifact_expectations`, and `missing_data_evidence`.
    - `passed` rows require an extractable adapter and dataset/script expectations; `needs_clarification` rows require clarification fields; `not_found` rows require checked/rejected source evidence.
  </behavior>
  <action>Create `scripts/build_phase2_coverage_matrix.py` with CLI args `--goldens`, `--source-catalog-manifest`, `--source-cards-manifest`, `--retrieval-evidence-json`, `--json-output`, and `--markdown-output`. It must join golden cases to Phase 1 source cards/local dump metadata using stable `source_id`/`card_id` where present, route each case to `fedstat`, `world_bank`, or `ckan`, derive filters from case metadata/query fields, and record `expected_terminal_outcome` as one of `passed`, `needs_clarification`, or `not_found`. For cases where CKAN is only metadata/not-found, require a specific `missing_data_evidence` entry naming the checked CKAN package/resource and the reason extraction is unsupported or unavailable. Do not allow placeholder values like `todo`, `unknown`, `TBD`, or empty `required_adapter`.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_coverage_matrix.py -q</automated></verify>
  <acceptance_criteria>`scripts/build_phase2_coverage_matrix.py` contains `expected_terminal_outcome`; contains `required_adapter`; tests assert exactly `20` matrix rows and reject `unknown` adapter values.</acceptance_criteria>
  <done>The project has an executable generator for all-20 data coverage planning.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and verify the matrix artifacts</name>
  <files>.planning/phases/02-jury-mvp/golden-coverage-matrix.json, .planning/phases/02-jury-mvp/golden-coverage-matrix.md, tests/test_phase2_coverage_matrix.py</files>
  <read_first>scripts/build_phase2_coverage_matrix.py, .planning/phases/01-data-architecture-research/golden-cases.yaml, .planning/phases/02-jury-mvp/qdrant-server-manifest.json</read_first>
  <action>Run `python3 scripts/build_phase2_coverage_matrix.py --goldens .planning/phases/01-data-architecture-research/golden-cases.yaml --source-catalog-manifest .planning/phases/01-data-architecture-research/source-catalog-manifest.json --source-cards-manifest .planning/phases/01-data-architecture-research/source-cards-manifest.json --json-output .planning/phases/02-jury-mvp/golden-coverage-matrix.json --markdown-output .planning/phases/02-jury-mvp/golden-coverage-matrix.md`. The JSON must include top-level keys `total_cases`, `generated_at`, `qdrant_server_manifest`, `cases`, and `unresolved_data_gaps`. The Markdown must include one row per GC case with source family, source/card id, filters summary, expected terminal outcome, required adapter, expected artifacts, and missing-data evidence. If any case lacks a real mapping, record it in `unresolved_data_gaps` and fail verification; do not silently allow final acceptance to proceed.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_coverage_matrix.py -q</automated></verify>
  <acceptance_criteria>`golden-coverage-matrix.json` contains `"total_cases": 20`; contains no `"expected_terminal_outcome": "gated"`; `golden-coverage-matrix.md` contains `GC-001` and `GC-020`.</acceptance_criteria>
  <done>All 20 golden cases have concrete data/extraction expectations before workflow finalization and acceptance plans run.</done>
</task>

</tasks>

<verification>
Run `python3 scripts/build_phase2_coverage_matrix.py --goldens .planning/phases/01-data-architecture-research/golden-cases.yaml --source-catalog-manifest .planning/phases/01-data-architecture-research/source-catalog-manifest.json --source-cards-manifest .planning/phases/01-data-architecture-research/source-cards-manifest.json --json-output .planning/phases/02-jury-mvp/golden-coverage-matrix.json --markdown-output .planning/phases/02-jury-mvp/golden-coverage-matrix.md`, then `python3 -m pytest tests/test_phase2_coverage_matrix.py -q`.
</verification>

<success_criteria>
The all-20 acceptance target is backed by a concrete source/adapter/filter/artifact matrix, and any missing data gap blocks final acceptance rather than surfacing late as a gated eval.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-10-SUMMARY.md`.
</output>
