---
context: phase
phase: 02-jury-mvp
task: null
total_tasks: null
status: paused
last_updated: 2026-05-14T20:50:21Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree без gitignored файлов | Worktree не наследует .env и .local/ — скауты и extraction gated | blocking | Перед батчем: `ln -sf /Users/a/MAI/matmod/.env .env` и `ln -sf /Users/a/MAI/matmod/.local .local` |
| LLM fallback temptation | Нет Qwen credentials → gated/stopped, никакого keyword-matching | blocking | gated_skip → RuntimeError, finalization_pending=True |
| Codegen на unknown source_family | _resolve_source_family возвращает unknown для WB IDs с подчёркиваниями/lowercase.dot | advisory | Исправлено в 1e08b07 — не откатывать |

**Do not proceed until all boxes are checked.**

<current_state>
Phase 02 (jury-MVP) — bug-fix + Variant B iteration.

Variant A (fixes 1–11): 5/20 passed.
Variant B (codegen extraction): реализован, первый прогон 5/20 (нейтрально).
Фикс WB source_family detection закоммичен (1e08b07) в ветке worktree-feat+variant-b-codegen-extraction.
Нужен повторный батч чтобы проверить улучшение для GC-011 и GC-015.
</current_state>

<completed_work>

- Fixes 1–11: graceful degradation (a50b8ca и ранее)
- Import cleanup: ArtifactStore + SourceCatalog (c8644fd)
- 20-case golden eval Variant A: 5/20 passed (batch-20260514T140840Z)
- Variant B: codegen extraction agent (797d6a1) — app/data/codegen_extractor.py + fallback в deterministic_tools.py + 22 теста
- Fix WB source_family (1e08b07): SH_UHC_FH40_LARGE и fin30.2 теперь распознаются как world_bank
- 20-case golden eval Variant B первый прогон: 5/20 (codegen сработал в GC-007)
</completed_work>

<remaining_work>

**HIGH** — rerun-after-family-fix: батч в worktree после 1e08b07. Ожидаем codegen в GC-011 и GC-015.

**HIGH** — sel-zero-root-cause: 10 кейсов (GC-006,009,010,012,014,016,017,018,019,020) sel=0. Upstream retrieval/routing проблема.

**MEDIUM** — ds1-not-found: GC-003, GC-005, GC-008 — ds=1 но not_found. Critic/narrator убивает.

**MEDIUM** — gc011-routing: graph.py _route_after_intent() — no_data ДО needs_clarification.

**MEDIUM** — fedstat-period-filter: fedstat_adapter.py ~203 — месячные/кумулятивные колонки.

**LOW** — sse-hang, merge-to-main.
</remaining_work>

<decisions_made>

- Variant B — fallback в deterministic_tools, не отдельный граф-узел
- Codegen только для fedstat и world_bank (Parquet-backed)
- source_family из source_card напрямую (надёжнее парсинга source_id)
- Worktree требует симлинков .env и .local/ перед батчем
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `app/data/codegen_extractor.py` — Variant B реализация
2. `app/workflow/nodes/deterministic_tools.py` — `_maybe_codegen_fallback`, `_resolve_source_family`
3. `.planning/phases/02-jury-mvp/batch-runs/batch-20260514T140840Z/summary.json` — Variant A
4. `.planning/phases/02-jury-mvp/batch-runs/batch-variantB-20260514T150223Z/summary.json` — Variant B первый прогон

## Critical Anti-Patterns (do NOT repeat these)
- **WORKTREE SYMLINKS**: `.env` и `.local/` симлинки обязательны перед батчем
- **CODEGEN НА unknown FAMILY**: проверить source_card.source_family если codegen не активируется

## Infrastructure State
- Qdrant: `docker compose -f docker-compose.qdrant.yml up -d`, порт 6333, collection `phase1_source_cards`
- Worktree: `/Users/a/MAI/matmod/.claude/worktrees/feat+variant-b-codegen-extraction`, ветка `worktree-feat+variant-b-codegen-extraction`
- Симлинки в worktree: `.env → /Users/a/MAI/matmod/.env`, `.local → /Users/a/MAI/matmod/.local`

<context>
Variant B реализован. Нейтральный первый прогон (5/20). После фикса WB source_family ожидаем +2. Основной потолок — 10 кейсов sel=0 (upstream retrieval). 3 кейса ds=1 но not_found (critic/narrator). Главный вопрос следующей сессии: почему 10 кейсов не находят источники вообще.
</context>

<next_action>
1. Запустить Qdrant: `docker compose -f docker-compose.qdrant.yml up -d`
2. В worktree убедиться в симлинках: `.env` и `.local/`
3. Запустить батч: `python3 -m scripts.run_workflow_batch .planning/phases/01-data-architecture-research/golden-cases.yaml --output-dir .planning/phases/02-jury-mvp/batch-runs/batch-variantB-TIMESTAMP`
4. Сравнить с batch-20260514T140840Z — ожидаем 7/20
</next_action>
