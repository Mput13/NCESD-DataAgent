---
phase: 01-data-architecture-research
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - requirements.txt
  - app/llm/yandex_ai_studio.py
  - app/artifacts/workflow_artifacts.py
  - app/workflow/graph_contract.py
  - .planning/phases/01-data-architecture-research/yandex-qwen-spike.md
  - .planning/phases/01-data-architecture-research/langgraph-contract.md
autonomous: true
requirements:
  - NLU-01
  - NLU-02
  - NLU-03
  - NLU-04
must_haves:
  truths:
    - "Phase 1 has a Qwen-targeted Yandex AI Studio client path with structured output and tool-call hardening, or explicit gated skip evidence if credentials are absent."
    - "The LangGraph architecture contract names the supervisor, scouts, coverage, extraction planner, critic, narrator, and visualization roles from the locked decisions."
    - "Typed artifacts define how intent, evidence, coverage, extraction plans, critique, and the canonical workflow-owned `TraceEvent` move through the graph and into the Streamlit trace payloads."
  artifacts:
    - path: "app/llm/yandex_ai_studio.py"
      provides: "Hardened Yandex AI Studio client for the Qwen target"
    - path: "app/artifacts/workflow_artifacts.py"
      provides: "Typed workflow artifact contracts"
    - path: "app/workflow/graph_contract.py"
      provides: "LangGraph skeleton or contract for supervisor and workers"
    - path: ".planning/phases/01-data-architecture-research/yandex-qwen-spike.md"
      provides: "Qwen/Yandex structured-output spike evidence"
    - path: ".planning/phases/01-data-architecture-research/langgraph-contract.md"
      provides: "Graph-role, routing, checkpoint, and trace contract"
  key_links:
    - from: "app/llm/yandex_ai_studio.py"
      to: ".planning/YANDEX_AI_STUDIO_RESEARCH.md"
      via: "Endpoint/model/auth alignment"
      pattern: "llm\\.api\\.cloud\\.yandex\\.net|Api-Key|Qwen"
    - from: "app/workflow/graph_contract.py"
      to: "app/artifacts/workflow_artifacts.py"
      via: "Typed state, canonical TraceEvent ownership, and node I/O"
      pattern: "IntentFrame|ResearchDesign|CoverageReport|TraceEvent"
    - from: "app/workflow/graph_contract.py"
      to: "app/ui/trace_models.py"
      via: "UI trace payloads reuse workflow TraceEvent through import/view-model mapping"
      pattern: "from app\\.artifacts\\.workflow_artifacts import TraceEvent|WorkflowTraceViewModel|TraceTimeline"
---

<objective>
Harden the Qwen/Yandex path and define the typed LangGraph orchestration contract.

Purpose: Validate D-13 through D-18 with code and artifacts that the remaining Phase 1 implementation can build on directly.
Output: updated dependencies, a hardened Yandex client, workflow artifact types, a graph contract/skeleton, and two research artifacts documenting the spike results.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/YANDEX_AI_STUDIO_RESEARCH.md
@.planning/ARCHITECTURE_STACK.md
@requirements.txt
@app/llm/yandex_ai_studio.py

<interfaces>
From app/llm/yandex_ai_studio.py:
```python
@dataclass(frozen=True)
class YandexAIStudioConfig:
    api_key: str
    model: str
    base_url: str = DEFAULT_BASE_URL

class YandexAIStudioClient:
    def chat(
        self,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.1,
        max_tokens: int = 512,
        timeout: int = 60,
    ) -> str: ...

def smoke_prompt(profile: str = "QWEN") -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the Yandex AI Studio client for Qwen structured output</name>
  <files>requirements.txt, app/llm/yandex_ai_studio.py, .planning/phases/01-data-architecture-research/yandex-qwen-spike.md</files>
  <read_first>requirements.txt, app/llm/yandex_ai_studio.py, .planning/phases/01-data-architecture-research/yandex-qwen-spike.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/YANDEX_AI_STUDIO_RESEARCH.md, .planning/STATE.md</read_first>
  <behavior>
    - Test 1: The client aligns with the verified OpenAI-compatible base URL and auth header for Yandex AI Studio.
    - Test 2: The client exposes a structured-output or schema-driven request path for intent artifacts.
    - Test 3: Missing credentials produce explicit gated-skip evidence in the spike report rather than silent pass/fail ambiguity.
  </behavior>
  <action>Update `requirements.txt` with the minimum packages needed for this spike, including `openai` and `pydantic` if absent. Refactor `app/llm/yandex_ai_studio.py` to target Qwen per D-16, align with the verified `https://llm.api.cloud.yandex.net/v1` host, use `Authorization: Api-Key ...`, and expose a structured-output helper for typed intent or artifact schemas. Write `yandex-qwen-spike.md` documenting: exact env vars, model URI expectations, structured-output test outcome, tool-call readiness, and a `DeepSeek historical fallback only` note so DeepSeek is not treated as the default target.</action>
  <acceptance_criteria>`rg -n "llm\\.api\\.cloud\\.yandex\\.net|Api-Key|Qwen|DeepSeek historical fallback only" app/llm/yandex_ai_studio.py .planning/phases/01-data-architecture-research/yandex-qwen-spike.md` returns matches for all four requirements.</acceptance_criteria>
  <verify>
    <automated>rg -n "llm\\.api\\.cloud\\.yandex\\.net|Api-Key|Qwen|DeepSeek historical fallback only" app/llm/yandex_ai_studio.py .planning/phases/01-data-architecture-research/yandex-qwen-spike.md</automated>
  </verify>
  <done>The repo has a Qwen-targeted Yandex client path and a spike artifact that records either successful validation or explicit gated skip evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define typed workflow artifacts and the LangGraph contract</name>
  <files>app/artifacts/workflow_artifacts.py, app/workflow/graph_contract.py, .planning/phases/01-data-architecture-research/langgraph-contract.md</files>
  <read_first>app/artifacts/workflow_artifacts.py, app/workflow/graph_contract.py, .planning/phases/01-data-architecture-research/langgraph-contract.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/ARCHITECTURE_STACK.md, .planning/phases/01-data-architecture-research/01-RESEARCH.md, app/artifacts/source_cards.py</read_first>
  <behavior>
    - Test 1: Typed artifacts cover intent/triage, research design, evidence bundle, coverage, extraction plan, critique, narration, visualization payload, and trace event.
    - Test 2: The graph contract names the supervisor, the three scouts, coverage/schema, extraction planner, critic, narrator, and visualization roles from D-14.
    - Test 3: The contract describes when direct lookups may skip nodes and when complex queries fan out to parallel scouts per D-15, while keeping `TraceEvent` canonical in `app/artifacts/workflow_artifacts.py` for downstream UI reuse.
  </behavior>
  <action>Create `app/artifacts/workflow_artifacts.py` with Pydantic models or typed structures for `IntentFrame`, `ResearchDesignArtifact`, `CoverageReport`, `ExtractionPlan`, `CritiqueReport`, and the canonical `TraceEvent`. `TraceEvent` ownership must stay in `app/artifacts/workflow_artifacts.py`; do not redefine it in UI modules. Create `app/workflow/graph_contract.py` as a LangGraph skeleton or graph contract module with node names, state shape, routing comments matching D-13 through D-15, and explicit import/usage of the workflow-owned `TraceEvent` for emitted trace payloads. Write `langgraph-contract.md` with sections named exactly `Supervisor`, `Parallel scouts`, `Coverage and extraction`, `Critic and narrator`, and `Checkpoint and trace rules`. The document must state that this is a validation artifact for the single active Phase 1, not a bypass around plan verification, and it must note that Streamlit trace payloads are fed from `graph_contract.py` through `workflow_artifacts.py` rather than a duplicate UI-local trace schema.</action>
  <acceptance_criteria>`rg -n "Supervisor|Parallel scouts|Coverage and extraction|Critic and narrator|Checkpoint and trace rules" .planning/phases/01-data-architecture-research/langgraph-contract.md` returns five section headers, `rg -n "class TraceEvent|IntentFrame|CoverageReport|ExtractionPlan" app/artifacts/workflow_artifacts.py` returns matches, and `rg -n "from app\\.artifacts\\.workflow_artifacts import .*TraceEvent|TraceEvent" app/workflow/graph_contract.py` confirms the graph contract consumes the canonical trace model.</acceptance_criteria>
  <verify>
    <automated>rg -n "Supervisor|Parallel scouts|Coverage and extraction|Critic and narrator|Checkpoint and trace rules" .planning/phases/01-data-architecture-research/langgraph-contract.md && rg -n "class TraceEvent|IntentFrame|CoverageReport|ExtractionPlan" app/artifacts/workflow_artifacts.py && rg -n "from app\\.artifacts\\.workflow_artifacts import .*TraceEvent|TraceEvent" app/workflow/graph_contract.py</automated>
  </verify>
  <done>The phase contains a typed LangGraph contract with a single canonical TraceEvent owner that downstream UI work can reuse without contract drift.</done>
</task>

</tasks>

<verification>
Check that the Yandex client matches the verified endpoint/auth path, and that the graph contract names the full locked role set with explicit routing and trace rules.
</verification>

<success_criteria>
Phase 1 has validated the LLM/orchestration layer deeply enough that remaining implementation can use the chosen graph and Qwen path without renewed architecture shopping.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-04-SUMMARY.md`
</output>
