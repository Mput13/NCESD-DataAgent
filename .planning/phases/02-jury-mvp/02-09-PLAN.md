---
phase: 02-jury-mvp
plan: "09"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.qdrant.yml
  - .env.example
  - app/retrieval/readiness.py
  - app/retrieval/embedding_index.py
  - scripts/promote_qdrant_server.py
  - .planning/phases/02-jury-mvp/qdrant-server-manifest.json
  - tests/test_phase2_qdrant_server.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - ENG-01
  - ENG-03
must_haves:
  truths:
    - "Qdrant server mode is started, populated, and verified before Phase 2 retrieval/eval/UI work claims readiness."
    - "The promoted server collection contains 36,321 vectors with the current Phase 1 corpus hash."
    - "An empty `.local/qdrant-server` directory cannot be mistaken for a ready jury runtime."
  artifacts:
    - "docker-compose.qdrant.yml defines the shared Qdrant server."
    - "scripts/promote_qdrant_server.py promotes cached Phase 1 embeddings into QDRANT_URL when needed."
    - ".planning/phases/02-jury-mvp/qdrant-server-manifest.json records collection, vector count, corpus hash, URL, and verification command."
    - "app/retrieval/readiness.py rejects server manifests with missing collection, wrong vector count, or corpus hash mismatch."
  key_links:
    - "promote_qdrant_server.py reads Phase 1 embedding cache/corpus manifests and writes the same collection configured by QDRANT_URL and QDRANT_COLLECTION."
    - "app.retrieval.embedding_index selects URL mode whenever QDRANT_URL is non-empty."
    - "Plan 02-02 retrieval evidence reads qdrant-server-manifest.json before ranking/eval output."
---

<objective>
Make Qdrant Docker/server mode operational for Phase 2 rather than merely configured.

Purpose: satisfy D-18A through D-18D and D-19A by promoting the ready 36,321-vector corpus into a shared server collection with reproducible evidence.
Output: server compose config, promotion script, strict readiness probe, and committed manifest path for downstream retrieval/eval/UI plans.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-jury-mvp/02-CONTEXT.md
@.planning/phases/02-jury-mvp/02-RESEARCH.md
@.planning/phases/01-data-architecture-research/embedding-corpus-manifest.json
@.planning/phases/01-data-architecture-research/embedding-index-manifest.json
@app/retrieval/embedding_index.py
@.env.example
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure Qdrant server as the Phase 2 runtime</name>
  <files>docker-compose.qdrant.yml, .env.example, app/retrieval/embedding_index.py, tests/test_phase2_qdrant_server.py</files>
  <read_first>app/retrieval/embedding_index.py, .env.example, .planning/phases/02-jury-mvp/02-CONTEXT.md, .planning/codebase/INTEGRATIONS.md</read_first>
  <behavior>
    - `QDRANT_URL=http://localhost:6333` is documented as the Phase 2 runtime default for workflow/eval/UI concurrency.
    - `docker-compose.qdrant.yml` starts one Qdrant service exposing ports `6333` and `6334` and persists data outside the embedded `.local/qdrant` path.
    - Embedded `QDRANT_MODE=local` remains available only for small tests or explicit fallback, and readiness output warns when local mode is used with `vector_count > 20000`.
  </behavior>
  <action>Create `docker-compose.qdrant.yml` with service `qdrant`, image `qdrant/qdrant:latest`, ports `"6333:6333"` and `"6334:6334"`, and volume `.local/qdrant-server:/qdrant/storage`. Update `.env.example` to include `QDRANT_URL=http://localhost:6333`, `QDRANT_COLLECTION=phase1_source_cards`, and a comment that Phase 2 jury/eval/UI must use Qdrant server mode; keep `QDRANT_MODE=local` documented only as small-test fallback. Update `app/retrieval/embedding_index.py` so URL mode is selected whenever `QDRANT_URL` is non-empty and embedded path mode is not opened. Add tests in `tests/test_phase2_qdrant_server.py` that assert server-mode env selects URL mode and that local mode with `vector_count=36321` produces a warning reason containing `embedded_local_qdrant_not_recommended_for_phase2`.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_qdrant_server.py tests/test_embedding_index.py -q</automated></verify>
  <acceptance_criteria>`docker-compose.qdrant.yml` contains `qdrant/qdrant:latest`; `.env.example` contains `QDRANT_URL=http://localhost:6333`; tests contain `embedded_local_qdrant_not_recommended_for_phase2`; tests verify `QDRANT_URL` is preferred over embedded path mode.</acceptance_criteria>
  <done>Phase 2 has a shared Qdrant server path suitable for parallel scouts, evals, workflow smoke, and Streamlit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote the Phase 1 embedding cache into server Qdrant</name>
  <files>scripts/promote_qdrant_server.py, tests/test_phase2_qdrant_server.py</files>
  <read_first>app/retrieval/embedding_index.py, scripts/build_embedding_index.py, .planning/phases/01-data-architecture-research/embedding-corpus-manifest.json, .planning/phases/01-data-architecture-research/embedding-index-manifest.json, docker-compose.qdrant.yml</read_first>
  <behavior>
    - The script starts or validates the server with `docker compose -f docker-compose.qdrant.yml up -d qdrant`.
    - If `QDRANT_URL` already has collection `phase1_source_cards` with `36321` vectors and matching corpus hash, the script skips reload and writes evidence.
    - If the collection is missing or stale, the script rebuilds/upserts from the Phase 1 embedding cache without making new embedding calls unless `--allow-reembed` is explicitly passed.
  </behavior>
  <action>Create `scripts/promote_qdrant_server.py` with CLI args `--index-manifest`, `--corpus-manifest`, `--embedding-cache`, `--qdrant-url`, `--collection`, `--manifest-output`, `--start-server`, and `--allow-reembed`. Default paths must point to `.planning/phases/01-data-architecture-research/embedding-index-manifest.json`, `.planning/phases/01-data-architecture-research/embedding-corpus-manifest.json`, `.local/dataagent/phase1/embedding-cache.jsonl`, `http://localhost:6333`, `phase1_source_cards`, and `.planning/phases/02-jury-mvp/qdrant-server-manifest.json`. Implement a Python-client promotion path: inspect collection vector count and payload corpus hash; when missing/stale, recreate/upsert points from cached embeddings and source-card payload metadata; only call existing embedding build code when `--allow-reembed` is set and cache coverage is insufficient. Do not leave an empty `.local/qdrant-server` directory as success evidence.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_qdrant_server.py -q</automated></verify>
  <acceptance_criteria>`scripts/promote_qdrant_server.py` contains `--start-server`; contains `--allow-reembed`; contains `.local/dataagent/phase1/embedding-cache.jsonl`; tests assert missing cache without `--allow-reembed` fails with `cache_incomplete_for_server_promotion`.</acceptance_criteria>
  <done>Server Qdrant can be populated from existing Phase 1 vectors or explicitly rebuilt with fresh embeddings only when necessary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify and freeze server-mode readiness evidence</name>
  <files>app/retrieval/readiness.py, .planning/phases/02-jury-mvp/qdrant-server-manifest.json, tests/test_phase2_qdrant_server.py</files>
  <read_first>app/demo/run_demo.py, app/retrieval/embedding_index.py, .planning/phases/01-data-architecture-research/embedding-corpus-manifest.json, .planning/phases/01-data-architecture-research/embedding-index-manifest.json, scripts/promote_qdrant_server.py</read_first>
  <behavior>
    - A server manifest with `status=ready`, `qdrant_url=http://localhost:6333`, matching `corpus_hash`, and `vector_count=36321` returns `ready=true`.
    - A stale corpus hash returns `ready=false` and reason containing `corpus_hash_mismatch`.
    - A zero or missing server vector count returns `ready=false` and reason containing `server_vector_count_mismatch`.
    - Readiness includes the exact command used to reproduce server promotion and verification.
  </behavior>
  <action>Create or update `app/retrieval/readiness.py` with `assess_phase2_index_readiness(index_manifest_path: Path, corpus_manifest_path: Path, server_manifest_path: Path | None = None) -> dict[str, Any]`. It must compare `corpus_hash`, `chunk_count`, and `vector_count`; require `status == "ready"` and `dense_status == "ready"`; require `vector_count == chunk_count == 36321` for the current Phase 2 baseline; require server mode with non-empty `qdrant_url` and matching collection evidence before jury readiness is true; return keys `ready`, `status`, `dense_status`, `chunk_count`, `vector_count`, `qdrant_collection`, `qdrant_url`, `qdrant_mode`, `server_manifest_status`, `reproduce_command`, `reasons`, and `warnings`. Generate `.planning/phases/02-jury-mvp/qdrant-server-manifest.json` only from a real server check or a test fixture path; it must contain `status`, `qdrant_url`, `collection`, `vector_count`, `corpus_hash`, `verified_at`, and `reproduce_command`. Embedded local mode with 36,321 vectors must append warning `embedded_local_qdrant_not_recommended_for_phase2` and keep jury readiness false.</action>
  <verify><automated>python3 -m pytest tests/test_phase2_qdrant_server.py -q</automated></verify>
  <acceptance_criteria>`app/retrieval/readiness.py` contains `server_vector_count_mismatch`; `qdrant-server-manifest.json` contains `reproduce_command`; tests assert `36321`, `corpus_hash_mismatch`, and `embedded_local_qdrant_not_recommended_for_phase2`.</acceptance_criteria>
  <done>Downstream plans can verify Qdrant server readiness with vector count, corpus hash, and reproduction command evidence.</done>
</task>

</tasks>

<verification>
Run `docker compose -f docker-compose.qdrant.yml up -d qdrant`, then `python3 scripts/promote_qdrant_server.py --start-server --manifest-output .planning/phases/02-jury-mvp/qdrant-server-manifest.json`, then `python3 -m pytest tests/test_phase2_qdrant_server.py tests/test_embedding_index.py -q`.
</verification>

<success_criteria>
Qdrant server mode is operational, populated with the promoted 36,321-vector collection, verified against corpus hash, and documented through a manifest that later plans must consume.
</success_criteria>

<output>
After completion, create `.planning/phases/02-jury-mvp/02-09-SUMMARY.md`.
</output>
