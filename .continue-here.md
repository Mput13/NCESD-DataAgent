---
workstream: feat/fix-after-matmod
status: paused
last_updated: 2026-05-14T21:59:17.807Z
---

<current_state>
Working branch/worktree:

- Branch: feat/fix-after-matmod
- Worktree: /Users/a/MAI/matmod/.claude/worktrees/feat+variant-b-codegen-extraction

This pause is about the post-Claude bugfix pass, not the historical planning files. Do not start by reading `.planning/phases/*`; that context is stale for this handoff.
</current_state>

<completed_work>

- Added shared source-family detection in `app/data/source_family.py`.
- Routed workflow and codegen through the shared detector.
- Removed the broad dotted-ID heuristic that made lowercase dotted IDs look like World Bank IDs by default.
- Added public parquet path resolver functions in FedStat and World Bank adapters while keeping `_parquet_path` aliases.
- Changed codegen prompt/execution toward the functional `FROM source` contract.
- Added result normalization so `amount`, `obs_value`, `gdp`, `metric_value`, or one unambiguous numeric candidate becomes canonical `value`.
- Reworked codegen retry behavior for missing/ambiguous value columns.
- Removed critic misuse of `repair_plan` as a technical audit field.
- Removed narrator fake audit filler and mandatory expert-verification section.
- Added evidence-backed narrator verification context only for passed responses.
- Added tests for source routing, codegen `FROM source`, value normalization, path resolver compatibility, and narrator/critic prompt regressions.
</completed_work>

<modified_files>

- `app/data/source_family.py`
- `app/data/codegen_extractor.py`
- `app/data/fedstat_adapter.py`
- `app/data/world_bank_adapter.py`
- `app/workflow/nodes/deterministic_tools.py`
- `app/workflow/nodes/critic.py`
- `app/workflow/nodes/narrator.py`
- `tests/test_codegen_extractor.py`
- `tests/test_parquet_path_resolution.py`
- `tests/test_phase2_finalization.py`
</modified_files>

<verification>

Passed:

- `python3 -m py_compile app/data/source_family.py app/data/codegen_extractor.py app/workflow/nodes/critic.py app/workflow/nodes/narrator.py app/workflow/nodes/deterministic_tools.py app/data/fedstat_adapter.py app/data/world_bank_adapter.py`
- `python3 -m pytest tests/test_codegen_extractor.py -q` -> 33 passed
- `python3 -m pytest tests/test_parquet_path_resolution.py -q` -> 8 passed
- `python3 -m pytest tests/test_phase2_finalization.py -q` -> 53 passed
- `python3 -m pytest tests/test_schema_validators.py::TestCritiqueSchema -q` -> 5 passed
- Combined targeted run: `tests/test_codegen_extractor.py tests/test_parquet_path_resolution.py tests/test_phase2_finalization.py tests/test_schema_validators.py::TestCritiqueSchema -q` -> 99 passed, 1 warning

Known unrelated/old diagnostic failures observed but not fixed in this scope:

- `tests/test_agent_diagnostics.py` has existing failures around World Bank coverage call inspection and no-LLM trace expectations.
- Full `tests/test_schema_validators.py` has existing conflict where some tests expect narrator fallback with `live_llm_required=False`, while the active D-37 contract makes narrator raise.
</verification>

<do_not_include>

- Do not include `.local`.
- Do not include `.planning/phases/*/workflow-runs`.
- Do not include `.planning/phases/*/batch-runs`.
- Do not include `.claude/settings.local.json`; it was already dirty and unrelated.
- Do not use historical phase artifacts as primary context for this pause.
</do_not_include>

<next_action>
Start by reviewing the diff in the modified app/test files listed above. Then run the combined targeted pytest command again. If green, commit only the relevant app/test files plus this handoff and `.planning/HANDOFF.json`.
</next_action>
