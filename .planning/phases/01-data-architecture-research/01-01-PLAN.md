---
phase: 01-data-architecture-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-data-architecture-research/requirements-map.md
  - .planning/phases/01-data-architecture-research/golden-cases.yaml
  - .planning/phases/01-data-architecture-research/eval-rubric.md
autonomous: true
requirements:
  - NLU-01
  - NLU-02
  - NLU-03
  - NLU-04
must_haves:
  truths:
    - "Phase 1 has a written map from Phase 1 requirements to concrete research outputs and verification artifacts."
    - "The repo contains 15-20 goldens covering simple, comparative, research, derived metric, ambiguous, and no-data flows."
    - "The eval pack explicitly checks clarification behavior, no-data honesty, retrieval evidence, extraction evidence, and trace completeness."
  artifacts:
    - path: ".planning/phases/01-data-architecture-research/requirements-map.md"
      provides: "Requirement-to-artifact traceability for Phase 1"
    - path: ".planning/phases/01-data-architecture-research/golden-cases.yaml"
      provides: "15-20 structured test cases"
    - path: ".planning/phases/01-data-architecture-research/eval-rubric.md"
      provides: "Deterministic scoring rules for retrieval, extraction, no-data, and trace"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/01-data-architecture-research/requirements-map.md"
      via: "Explicit per-requirement mapping"
      pattern: "NLU-0[1-4]|SRCH-0[1-4]"
    - from: ".planning/phases/01-data-architecture-research/golden-cases.yaml"
      to: ".planning/phases/01-data-architecture-research/eval-rubric.md"
      via: "Case categories and expected evidence fields"
      pattern: "category:|expected_"
---

<objective>
Build the Phase 1 foundation pack: requirement traceability, a 15-20 case golden set, and a deterministic evaluation rubric.

Purpose: Lock the scope of research and spikes around the user-approved query types and evidence rules from D-19, D-20, and D-21.
Output: `requirements-map.md`, `golden-cases.yaml`, and `eval-rubric.md` under the current phase directory.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/ARCHITECTURE_STACK.md
@.planning/DATA_REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 1 requirements map</name>
  <files>.planning/phases/01-data-architecture-research/requirements-map.md</files>
  <read_first>.planning/phases/01-data-architecture-research/requirements-map.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/ARCHITECTURE_STACK.md</read_first>
  <action>Create `requirements-map.md` as a table covering NLU-01..04 and SRCH-01..04. For each requirement, include: requirement text, which locked decisions it depends on (cite D-xx), Phase 1 evidence artifact(s), the deterministic proof expected in Phase 1, and what remains deferred to Phase 2. Use the exact phrase `Recommendation only — not an accepted Phase 2 decision` in the document header to preserve D-23. Do not map any requirement to the old deprecated phase directory.</action>
  <acceptance_criteria>`rg -n "NLU-01|NLU-02|NLU-03|NLU-04|SRCH-01|SRCH-02|SRCH-03|SRCH-04" .planning/phases/01-data-architecture-research/requirements-map.md` returns all eight IDs and `rg -n "Recommendation only — not an accepted Phase 2 decision" .planning/phases/01-data-architecture-research/requirements-map.md` returns one match.</acceptance_criteria>
  <verify>
    <automated>rg -n "NLU-01|NLU-02|NLU-03|NLU-04|SRCH-01|SRCH-02|SRCH-03|SRCH-04" .planning/phases/01-data-architecture-research/requirements-map.md && rg -n "Recommendation only — not an accepted Phase 2 decision" .planning/phases/01-data-architecture-research/requirements-map.md</automated>
  </verify>
  <done>All eight Phase 1 requirement IDs are mapped to explicit research artifacts and deferred boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 15-20 case golden set</name>
  <files>.planning/phases/01-data-architecture-research/golden-cases.yaml</files>
  <read_first>.planning/phases/01-data-architecture-research/golden-cases.yaml, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/phases/01-data-architecture-research/01-RESEARCH.md, .planning/DATA_REPORT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `golden-cases.yaml` with 15-20 cases per D-19. Each case must include: `id`, `category`, `query_ru`, `expected_route`, `expected_sources`, `needs_clarification`, `expected_artifacts`, `expected_rejection_or_no_data`, and `trace_expectations`. Ensure the set covers simple, comparative, research, derived_metric, ambiguous, no_data, FedStat-specific, World-Bank-specific, CKAN-first discovery, rejection-reason visibility, and trace completeness. Keep numbers descriptive only; do not embed factual numeric answers from memory.</action>
  <acceptance_criteria>`python - <<'PY'\nimport yaml, pathlib\np = pathlib.Path('.planning/phases/01-data-architecture-research/golden-cases.yaml')\ndata = yaml.safe_load(p.read_text())\nassert isinstance(data, list)\nassert 15 <= len(data) <= 20\ncats = {item['category'] for item in data}\nneed = {'simple','comparative','research','derived_metric','ambiguous','no_data'}\nassert need.issubset(cats)\nassert any('CKAN' in str(item.get('expected_sources')) for item in data)\nassert any('FedStat' in str(item.get('expected_sources')) for item in data)\nassert any('World Bank' in str(item.get('expected_sources')) for item in data)\nprint(len(data), sorted(cats))\nPY` exits 0.</acceptance_criteria>
  <verify>
    <automated>python - <<'PY'
import yaml, pathlib
p = pathlib.Path('.planning/phases/01-data-architecture-research/golden-cases.yaml')
data = yaml.safe_load(p.read_text())
assert isinstance(data, list)
assert 15 <= len(data) <= 20
cats = {item['category'] for item in data}
need = {'simple','comparative','research','derived_metric','ambiguous','no_data'}
assert need.issubset(cats)
assert any('CKAN' in str(item.get('expected_sources')) for item in data)
assert any('FedStat' in str(item.get('expected_sources')) for item in data)
assert any('World Bank' in str(item.get('expected_sources')) for item in data)
print(len(data), sorted(cats))
PY</automated>
  </verify>
  <done>The golden set contains 15-20 cases and covers all required query and source behaviors.</done>
</task>

<task type="auto">
  <name>Task 3: Define the evaluation rubric</name>
  <files>.planning/phases/01-data-architecture-research/eval-rubric.md</files>
  <read_first>.planning/phases/01-data-architecture-research/eval-rubric.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/phases/01-data-architecture-research/golden-cases.yaml, .planning/REQUIREMENTS.md, .planning/ARCHITECTURE_STACK.md</read_first>
  <action>Create `eval-rubric.md` that scores each golden case on: intent classification, research-definition fields, clarification trigger correctness, candidate-source quality, coverage-preview evidence, deterministic extraction evidence, rejection/no-data honesty, and trace completeness. Include an explicit rule that a case fails if the response contains an unsupported numeric claim, per D-12. Include a short section describing how skipped Yandex-dependent checks are recorded when credentials are absent, without dropping the Qwen target.</action>
  <acceptance_criteria>`rg -n "unsupported numeric claim|trace completeness|coverage-preview evidence|skipped Yandex-dependent checks" .planning/phases/01-data-architecture-research/eval-rubric.md` returns four matches or more.</acceptance_criteria>
  <verify>
    <automated>rg -n "unsupported numeric claim|trace completeness|coverage-preview evidence|skipped Yandex-dependent checks" .planning/phases/01-data-architecture-research/eval-rubric.md</automated>
  </verify>
  <done>The evaluation rubric can be applied to every golden case without relying on unstated human judgment.</done>
</task>

</tasks>

<verification>
Verify that the requirements map names all eight Phase 1 IDs, the golden set contains 15-20 cases, and the rubric enforces deterministic evidence and no unsupported numbers.
</verification>

<success_criteria>
Phase 1 has a reusable evaluation pack that downstream spikes can target directly, with D-19 through D-21 reflected explicitly in the artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-01-SUMMARY.md`
</output>
