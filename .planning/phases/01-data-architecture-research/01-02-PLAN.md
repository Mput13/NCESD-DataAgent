---
phase: 01-data-architecture-research
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-data-architecture-research/data-inventory.md
  - app/artifacts/source_cards.py
  - app/data/source_card_builders.py
  - scripts/build_source_cards.py
autonomous: true
requirements:
  - SRCH-03
  - SRCH-04
must_haves:
  truths:
    - "Phase 1 has a deterministic inventory of local dumps and bounded CKAN checks with no raw numeric claims from the LLM."
    - "FedStat, World Bank, and CKAN produce the same source-card and evidence-bundle shape."
    - "Source cards expose period, geography, units, provenance, and rejection-ready metadata for later retrieval work."
  artifacts:
    - path: ".planning/phases/01-data-architecture-research/data-inventory.md"
      provides: "Bounded inventory and verification notes"
    - path: "app/artifacts/source_cards.py"
      provides: "Typed SourceCandidateCard and EvidenceBundle schemas"
    - path: "app/data/source_card_builders.py"
      provides: "Per-source builders for FedStat, World Bank, and CKAN"
    - path: "scripts/build_source_cards.py"
      provides: "Deterministic source-card generation entrypoint"
  key_links:
    - from: "scripts/build_source_cards.py"
      to: "app/data/source_card_builders.py"
      via: "Builder imports"
      pattern: "from app\\.data\\.source_card_builders import"
    - from: "app/data/source_card_builders.py"
      to: "app/artifacts/source_cards.py"
      via: "Typed return values"
      pattern: "SourceCandidateCard|EvidenceBundle"
---

<objective>
Create the deterministic source inventory and the shared source-card contracts that retrieval, coverage preview, and orchestration will consume.

Purpose: Operationalize D-03 through D-12 by making FedStat, World Bank, and CKAN first-class, typed, and bounded from the start.
Output: `data-inventory.md`, typed source-card schemas, per-source builders, and a deterministic builder script.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/phases/01-data-architecture-research/01-main-artifact-comparison.md
@.planning/ARCHITECTURE_STACK.md
@.planning/DATA_REPORT.md
@requirements.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define source-card and evidence-bundle contracts</name>
  <files>app/artifacts/source_cards.py</files>
  <read_first>app/artifacts/source_cards.py, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/ARCHITECTURE_STACK.md, .planning/DATA_REPORT.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: `SourceCandidateCard` includes source, dataset/resource identity, title, match mode, units, geography, period coverage, provenance, availability flags, and quality flags.
    - Test 2: `EvidenceBundle` contains selected candidates, rejected candidates, coverage intent, and rejection reasons without raw numeric answer text.
  </behavior>
  <action>Create `app/artifacts/source_cards.py` with Pydantic v2 models for `SourceCandidateCard`, `CoverageHint`, `RejectedCandidate`, and `EvidenceBundle`. Include match modes required by D-08: exact, lexical, semantic, proxy, ckan_discovery, methodology_match. Include a field for `builder_source` or equivalent to preserve deterministic provenance. Use ASCII-only identifiers and docstrings. Do not put deprecated old-phase paths into any defaults or comments.</action>
  <acceptance_criteria>`rg -n "class SourceCandidateCard|class EvidenceBundle|ckan_discovery|methodology_match" app/artifacts/source_cards.py` returns matches for all required schema elements.</acceptance_criteria>
  <verify>
    <automated>rg -n "class SourceCandidateCard|class EvidenceBundle|ckan_discovery|methodology_match" app/artifacts/source_cards.py</automated>
  </verify>
  <done>The repository has a typed artifact contract that can represent FedStat, World Bank, and CKAN candidates uniformly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic inventory and source-card builders</name>
  <files>.planning/phases/01-data-architecture-research/data-inventory.md, app/data/source_card_builders.py, scripts/build_source_cards.py</files>
  <read_first>.planning/phases/01-data-architecture-research/data-inventory.md, app/data/source_card_builders.py, scripts/build_source_cards.py, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/DATA_REPORT.md, .planning/STATE.md, app/artifacts/source_cards.py</read_first>
  <behavior>
    - Test 1: FedStat builder emits cards from verified metadata and flags wide-parquet normalization requirements.
    - Test 2: World Bank builder emits cards with indicator metadata, country/aggregate hints, and canonical provenance.
    - Test 3: CKAN builder uses bounded package/resource checks and records when resource inspection is skipped or truncated.
  </behavior>
  <action>Create `app/data/source_card_builders.py` with functions for FedStat, World Bank, and CKAN source-card creation returning the typed models from `app/artifacts/source_cards.py`. Create `scripts/build_source_cards.py` as a deterministic CLI entrypoint that reads local metadata and performs bounded CKAN API calls only. Write `data-inventory.md` summarizing: verified local dump paths, local metadata files used, bounded CKAN endpoints, inspected limits, and known gaps such as incomplete `metadata.jsonl`. Include the exact phrase `CKAN is first-class for discovery and data access` per D-04.</action>
  <acceptance_criteria>`rg -n "CKAN is first-class for discovery and data access|metadata.jsonl|bounded CKAN" .planning/phases/01-data-architecture-research/data-inventory.md` returns matches, and `rg -n "def build_fedstat|def build_world_bank|def build_ckan" app/data/source_card_builders.py` returns three builder functions.</acceptance_criteria>
  <verify>
    <automated>rg -n "CKAN is first-class for discovery and data access|metadata.jsonl|bounded CKAN" .planning/phases/01-data-architecture-research/data-inventory.md && rg -n "def build_fedstat|def build_world_bank|def build_ckan" app/data/source_card_builders.py</automated>
  </verify>
  <done>Deterministic inventory findings and three per-source builders exist and share the same typed card contract.</done>
</task>

</tasks>

<verification>
Confirm the inventory document cites only verified dump/API facts, and the builder module exposes FedStat, World Bank, and CKAN functions returning shared typed cards.
</verification>

<success_criteria>
Phase 1 has a source-contract layer and a deterministic inventory artifact that later plans can use without re-reading raw dump structures or CKAN payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-02-SUMMARY.md`
</output>
