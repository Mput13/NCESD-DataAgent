---
phase: 01-data-architecture-research
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/retrieval/embedding_index.py
  - app/retrieval/hybrid_retrieval.py
  - scripts/build_embedding_index.py
  - scripts/run_retrieval_spike.py
  - .planning/phases/01-data-architecture-research/embedding-index-build.md
  - .planning/phases/01-data-architecture-research/embedding-index-manifest.json
  - .planning/phases/01-data-architecture-research/retrieval-comparison.md
  - .planning/phases/01-data-architecture-research/retrieval-eval.csv
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - NLU-04
must_haves:
  truths:
    - "The phase materializes the source-card embedding/search index as a durable local artifact, or records a credential-aware gated skip when credentials are absent."
    - "The index manifest records provider/model URI, document/query split, dimensions, chunk count, corpus hash, metadata version, local artifact paths, and rebuild command."
    - "Retrieval outputs are scored against the golden set and include lexical, dense, rerank/fallback status, rejection reasons, and metadata-rich source cards."
  artifacts:
    - path: "app/retrieval/embedding_index.py"
      provides: "Embedding provider and local index interfaces"
    - path: "app/retrieval/hybrid_retrieval.py"
      provides: "Hybrid retrieval over the prepared corpus/index"
    - path: "scripts/build_embedding_index.py"
      provides: "Long-running embedding/index build entrypoint"
    - path: ".planning/phases/01-data-architecture-research/embedding-index-build.md"
      provides: "Embedding/index build log, credential status, and operational notes"
    - path: ".planning/phases/01-data-architecture-research/embedding-index-manifest.json"
      provides: "Committed manifest for the local prepared index artifact"
    - path: ".planning/phases/01-data-architecture-research/retrieval-comparison.md"
      provides: "Trade-off and fallback evidence for lexical, dense, and rerank"
    - path: ".planning/phases/01-data-architecture-research/retrieval-eval.csv"
      provides: "Case-by-case retrieval evidence"
  key_links:
    - from: "scripts/build_embedding_index.py"
      to: ".planning/phases/01-data-architecture-research/embedding-index-manifest.json"
      via: "Materialized index manifest"
      pattern: "provider|model_uri|chunk_count|dimensions|corpus_hash|metadata_version"
    - from: "scripts/build_embedding_index.py"
      to: "app/retrieval/embedding_index.py"
      via: "Embedding provider and index writer imports"
      pattern: "embedding_index"
    - from: "scripts/run_retrieval_spike.py"
      to: "app/retrieval/hybrid_retrieval.py"
      via: "Retrieval spike runner"
      pattern: "hybrid_retrieval"
---

<objective>
Materialize the prepared source-card embedding/search index and validate hybrid retrieval over it.

Purpose: Satisfy D-06 through D-08E with a durable prepared data/index artifact, not a throwaway dense-retrieval spike.
Output: embedding provider/index module, long-running index builder, embedding-index manifest/build log, hybrid retrieval module, retrieval spike runner, comparison artifact, and retrieval eval CSV.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/phases/01-data-architecture-research/01-01-PLAN.md
@.planning/phases/01-data-architecture-research/01-02-PLAN.md
@.planning/phases/01-data-architecture-research/golden-cases.yaml
@app/artifacts/source_cards.py
@app/data/source_card_builders.py
@.planning/phases/01-data-architecture-research/embedding-corpus-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the embedding provider and materialized index builder</name>
  <files>app/retrieval/embedding_index.py, scripts/build_embedding_index.py, .planning/phases/01-data-architecture-research/embedding-index-build.md, .planning/phases/01-data-architecture-research/embedding-index-manifest.json</files>
  <read_first>app/retrieval/embedding_index.py, scripts/build_embedding_index.py, .planning/phases/01-data-architecture-research/embedding-index-build.md, .planning/phases/01-data-architecture-research/embedding-index-manifest.json, .planning/phases/01-data-architecture-research/embedding-corpus-contract.md, app/artifacts/source_cards.py, scripts/build_embedding_corpus.py, .planning/phases/01-data-architecture-research/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: The builder reads the embedding corpus from Plan 01-02 and writes a manifest with provider/model URI, document/query split, dimensions, chunk count, corpus hash, metadata version, and local artifact paths.
    - Test 2: Yandex AI Studio embeddings use `text-search-doc` for corpus chunks and reserve `text-search-query` for query vectors.
    - Test 3: Missing credentials create an explicit gated-skip manifest/build log, not a silent pass or a fake dense index.
  </behavior>
  <action>Create `app/retrieval/embedding_index.py` with interfaces for Yandex embedding requests, credential checks, local vector/index storage, cosine search, and a gated-skip status object. Create `scripts/build_embedding_index.py` as the long-running build entrypoint: it must build or load the Plan 01-02 embedding corpus, call Yandex embeddings when credentials are available, persist local index artifacts outside git, and write `embedding-index-manifest.json` plus `embedding-index-build.md`. The manifest must be committed but large vector artifacts must stay local/ignored. If credentials are absent, write a manifest with `status: gated_skip`, exact missing env vars, and the same expected artifact schema.</action>
  <acceptance_criteria>`rg -n "text-search-doc|text-search-query|gated_skip|chunk_count|corpus_hash|metadata_version" app/retrieval/embedding_index.py scripts/build_embedding_index.py .planning/phases/01-data-architecture-research/embedding-index-build.md .planning/phases/01-data-architecture-research/embedding-index-manifest.json` returns all required markers.</acceptance_criteria>
  <verify>
    <automated>rg -n "text-search-doc|text-search-query|gated_skip|chunk_count|corpus_hash|metadata_version" app/retrieval/embedding_index.py scripts/build_embedding_index.py .planning/phases/01-data-architecture-research/embedding-index-build.md .planning/phases/01-data-architecture-research/embedding-index-manifest.json</automated>
  </verify>
  <done>The repo contains a materialized embedding/search index build path and a durable manifest, or explicit gated-skip evidence with the same contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hybrid retrieval over the prepared index and evaluate it</name>
  <files>app/retrieval/hybrid_retrieval.py, scripts/run_retrieval_spike.py, .planning/phases/01-data-architecture-research/retrieval-comparison.md, .planning/phases/01-data-architecture-research/retrieval-eval.csv</files>
  <read_first>app/retrieval/hybrid_retrieval.py, scripts/run_retrieval_spike.py, .planning/phases/01-data-architecture-research/retrieval-comparison.md, .planning/phases/01-data-architecture-research/retrieval-eval.csv, .planning/phases/01-data-architecture-research/embedding-index-manifest.json, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/phases/01-data-architecture-research/golden-cases.yaml, app/artifacts/source_cards.py, app/data/source_card_builders.py, app/retrieval/embedding_index.py</read_first>
  <behavior>
    - Test 1: Lexical retrieval returns source cards with exact/title/code and RU/EN keyword evidence.
    - Test 2: Dense retrieval reads the prepared index manifest/artifact when available, or records the gated-skip status without silently dropping the dense requirement.
    - Test 3: Rerank decisions and rejected candidates are captured in the eval CSV and comparison report.
  </behavior>
  <action>Create `app/retrieval/hybrid_retrieval.py` with clear interfaces for lexical retrieval, dense retrieval over the prepared index, reranking, and fallback status when the index is gated. Create `scripts/run_retrieval_spike.py` that evaluates a bounded subset of the golden cases and writes `retrieval-eval.csv`. Write `retrieval-comparison.md` with an explicit comparison table for lexical BM25/FTS, prepared dense index, rerank path, and exact skipped-evidence behavior when credentials/index artifacts are unavailable. The comparison must keep the full target stack per D-06 rather than declaring dense retrieval optional.</action>
  <acceptance_criteria>`rg -n "lexical|dense|rerank|credential-aware fallback|prepared index" .planning/phases/01-data-architecture-research/retrieval-comparison.md` returns all concepts, and `python - <<'PY'\nimport csv\nfrom pathlib import Path\nrows=list(csv.DictReader(Path('.planning/phases/01-data-architecture-research/retrieval-eval.csv').open()))\nassert rows\ncols=set(rows[0])\nneed={'case_id','retrieval_mode','top_candidate','rejection_reasons','dense_status','index_manifest_status'}\nassert need.issubset(cols)\nprint(len(rows))\nPY` exits 0.</acceptance_criteria>
  <verify>
    <automated>rg -n "lexical|dense|rerank|credential-aware fallback|prepared index" .planning/phases/01-data-architecture-research/retrieval-comparison.md && python - <<'PY'
import csv
from pathlib import Path
rows=list(csv.DictReader(Path('.planning/phases/01-data-architecture-research/retrieval-eval.csv').open()))
assert rows
cols=set(rows[0])
need={'case_id','retrieval_mode','top_candidate','rejection_reasons','dense_status','index_manifest_status'}
assert need.issubset(cols)
print(len(rows))
PY</automated>
  </verify>
  <done>The repo contains hybrid retrieval over the prepared index contract with explicit evidence for lexical, dense, rerank, and fallback behavior.</done>
</task>

</tasks>

<verification>
Check that the embedding/index manifest exists, records real build or gated-skip evidence, and retrieval evidence covers lexical, dense, rerank, and fallback states over the prepared index contract.
</verification>

<success_criteria>
The phase contains a durable prepared embedding/search index path and executable evidence for the chosen retrieval stack, not just architecture prose, with deterministic no-data and rejection handling represented explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-03-SUMMARY.md`
</output>
