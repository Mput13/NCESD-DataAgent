---
phase: 01-data-architecture-research
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - requirements.txt
  - app/llm/yandex_ai_studio.py
  - app/artifacts/workflow_artifacts.py
  - app/workflow/graph_contract.py
  - app/workflow/run_graph.py
  - app/data/deterministic_tools.py
  - app/evals/run_eval.py
  - app/ui/trace_models.py
  - scripts/run_extraction_probes.py
  - .planning/phases/01-data-architecture-research/yandex-qwen-spike.md
  - .planning/phases/01-data-architecture-research/langgraph-contract.md
  - .planning/phases/01-data-architecture-research/extraction-probes.md
  - .planning/phases/01-data-architecture-research/data-relevance-eval.json
  - .planning/phases/01-data-architecture-research/data-relevance-eval.md
  - .planning/phases/01-data-architecture-research/trace-ui-demo.md
autonomous: true
requirements:
  - NLU-01
  - NLU-02
  - NLU-03
  - NLU-04
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - RBST-04
must_haves:
  truths:
    - "Phase 1 has a Qwen-targeted Yandex AI Studio client path with structured output and tool-call hardening, or explicit gated skip evidence if credentials are absent."
    - "The LangGraph implementation names the supervisor, scouts, coverage, extraction planner, critic, narrator, and visualization roles from the locked decisions, with budgets and tool scopes."
    - "The phase contains a runnable narrow LangGraph path that moves typed artifacts through triage, retrieval, coverage/extraction planning, deterministic tool or gated evidence, critic/narrator, and trace emission."
    - "Typed artifacts define how intent, evidence, coverage, extraction plans, DatasetArtifact, MethodologyNote, VisualizationSpec, critique, FinalAnswer, FeedbackArtifact, and the canonical workflow-owned `TraceEvent` move through the graph and into the Streamlit trace payloads."
    - "FedStat, World Bank, and CKAN all have deterministic DuckDB SQL-first coverage/extraction probes prepared while the embedding/index build can run independently."
    - "Deterministic tool contracts expose safe operations for coverage preview, DuckDB query execution, dataset artifact export, CSV/Parquet/manifest output, and visualization rendering from DatasetArtifact."
    - "The UI trace contract can consume retrieval/index status, including long-running build, ready, and gated-skip states."
    - "The phase includes an executable golden-case evaluation harness focused on data relevance, source rejection, coverage evidence, deterministic extraction evidence, and trace completeness."
    - "UI trace models remain diagnostic and minimal; visual polish is deferred until source relevance and extraction correctness are verified."
  artifacts:
    - path: "app/llm/yandex_ai_studio.py"
      provides: "Hardened Yandex AI Studio client for the Qwen target"
    - path: "app/artifacts/workflow_artifacts.py"
      provides: "Typed workflow artifact contracts"
    - path: "app/workflow/graph_contract.py"
      provides: "LangGraph node/state contract for supervisor and workers"
    - path: "app/workflow/run_graph.py"
      provides: "Runnable narrow LangGraph entrypoint for representative demo cases"
    - path: "app/data/deterministic_tools.py"
      provides: "Safe deterministic data tool contracts for coverage, extraction, dataset export, and visualization"
    - path: "app/evals/run_eval.py"
      provides: "Executable golden-case evaluation harness for data relevance, coverage, extraction, rejection, and trace evidence"
    - path: "app/ui/trace_models.py"
      provides: "UI trace and feedback view-models consuming workflow TraceEvent"
    - path: ".planning/phases/01-data-architecture-research/extraction-probes.md"
      provides: "DuckDB SQL-first coverage and extraction probe results for three source paths"
    - path: ".planning/phases/01-data-architecture-research/data-relevance-eval.json"
      provides: "Machine-readable golden-case evaluation results"
    - path: ".planning/phases/01-data-architecture-research/data-relevance-eval.md"
      provides: "Human-readable data relevance and extraction evaluation report"
    - path: "scripts/run_extraction_probes.py"
      provides: "DuckDB SQL-first deterministic extraction probe entrypoint"
    - path: ".planning/phases/01-data-architecture-research/trace-ui-demo.md"
      provides: "Streamlit state, trace, artifact, and feedback demo contract"
    - path: ".planning/phases/01-data-architecture-research/yandex-qwen-spike.md"
      provides: "Qwen/Yandex structured-output spike evidence"
    - path: ".planning/phases/01-data-architecture-research/langgraph-contract.md"
      provides: "Graph-role, routing, checkpoint, and trace contract"
  key_links:
    - from: "app/llm/yandex_ai_studio.py"
      to: ".planning/YANDEX_AI_STUDIO_RESEARCH.md"
      via: "Endpoint/model/auth alignment"
      pattern: "llm\\.api\\.cloud\\.yandex\\.net|Api-Key|Qwen"
    - from: "app/workflow/graph_contract.py"
      to: "app/artifacts/workflow_artifacts.py"
      via: "Typed state, canonical TraceEvent ownership, and node I/O"
      pattern: "IntentFrame|ResearchDesign|CoverageReport|DatasetArtifact|VisualizationSpec|FeedbackArtifact|TraceEvent"
    - from: "app/workflow/run_graph.py"
      to: "app/workflow/graph_contract.py"
      via: "Runnable graph consumes node contracts and emits trace events"
      pattern: "build_graph|invoke|TraceEvent|IntentFrame|DatasetArtifact"
    - from: "app/data/deterministic_tools.py"
      to: "app/artifacts/workflow_artifacts.py"
      via: "Deterministic tools produce typed dataset and visualization artifacts"
      pattern: "DatasetArtifact|VisualizationSpec|run_duckdb_query|export_csv_parquet_manifest"
    - from: "app/workflow/graph_contract.py"
      to: "app/ui/trace_models.py"
      via: "UI trace payloads reuse workflow TraceEvent through import/view-model mapping"
      pattern: "from app\\.artifacts\\.workflow_artifacts import TraceEvent|WorkflowTraceViewModel|TraceTimeline"
    - from: "scripts/run_extraction_probes.py"
      to: "app/data/source_card_builders.py"
      via: "Probe target selection and adapter handoff"
      pattern: "source_card_builders|build_fedstat|build_world_bank|build_ckan"
    - from: "scripts/run_extraction_probes.py"
      to: ".planning/phases/01-data-architecture-research/extraction-probes.md"
      via: "Recorded DuckDB SQL-first probe evidence and adapter rationale"
      pattern: "duckdb|pyarrow|polars|FedStat normalizer|World Bank canonical long-format"
---

<objective>
Harden the Qwen/Yandex path and implement the typed narrow LangGraph, extraction, and UI contracts while the embedding/index build can run independently.

Purpose: Validate D-09 through D-18 with code and artifacts that are independent of the long-running embedding job and can consume the prepared index once it is ready.
Output: updated dependencies, a hardened Yandex client, workflow artifact types, deterministic tool contracts, runnable narrow LangGraph entrypoint, extraction probes, UI trace view-models, and research artifacts documenting the spike results.
</objective>

<execution_context>
@/Users/a/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-data-architecture-research/01-CONTEXT.md
@.planning/phases/01-data-architecture-research/01-RESEARCH.md
@.planning/YANDEX_AI_STUDIO_RESEARCH.md
@.planning/ARCHITECTURE_STACK.md
@requirements.txt
@app/llm/yandex_ai_studio.py
@.planning/phases/01-data-architecture-research/embedding-index-manifest.json

<interfaces>
From app/llm/yandex_ai_studio.py:
```python
@dataclass(frozen=True)
class YandexAIStudioConfig:
    api_key: str
    model: str
    base_url: str = DEFAULT_BASE_URL

class YandexAIStudioClient:
    def chat(
        self,
        messages: list[dict[str, str]],
        *,
        temperature: float = 0.1,
        max_tokens: int = 512,
        timeout: int = 60,
    ) -> str: ...

def smoke_prompt(profile: str = "QWEN") -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the Yandex AI Studio client for Qwen structured output</name>
  <files>requirements.txt, app/llm/yandex_ai_studio.py, .planning/phases/01-data-architecture-research/yandex-qwen-spike.md</files>
  <read_first>requirements.txt, app/llm/yandex_ai_studio.py, .planning/phases/01-data-architecture-research/yandex-qwen-spike.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/YANDEX_AI_STUDIO_RESEARCH.md, .planning/STATE.md</read_first>
  <behavior>
    - Test 1: The client aligns with the verified OpenAI-compatible base URL and auth header for Yandex AI Studio.
    - Test 2: The client exposes a structured-output or schema-driven request path for intent artifacts.
    - Test 3: Missing credentials produce explicit gated-skip evidence in the spike report rather than silent pass/fail ambiguity.
  </behavior>
  <action>Update `requirements.txt` with the minimum packages needed for this spike, including `openai` and `pydantic` if absent. Refactor `app/llm/yandex_ai_studio.py` to target Qwen per D-16, align with the verified `https://llm.api.cloud.yandex.net/v1` host, use `Authorization: Api-Key ...`, and expose a structured-output helper for typed intent or artifact schemas. Write `yandex-qwen-spike.md` documenting: exact env vars, model URI expectations, structured-output test outcome, tool-call readiness, and a `DeepSeek historical fallback only` note so DeepSeek is not treated as the default target.</action>
  <acceptance_criteria>`rg -n "llm\\.api\\.cloud\\.yandex\\.net|Api-Key|Qwen|DeepSeek historical fallback only" app/llm/yandex_ai_studio.py .planning/phases/01-data-architecture-research/yandex-qwen-spike.md` returns matches for all four requirements.</acceptance_criteria>
  <verify>
    <automated>rg -n "llm\\.api\\.cloud\\.yandex\\.net|Api-Key|Qwen|DeepSeek historical fallback only" app/llm/yandex_ai_studio.py .planning/phases/01-data-architecture-research/yandex-qwen-spike.md</automated>
  </verify>
  <done>The repo has a Qwen-targeted Yandex client path and a spike artifact that records either successful validation or explicit gated skip evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define typed workflow artifacts and implement the runnable narrow LangGraph flow</name>
  <files>app/artifacts/workflow_artifacts.py, app/workflow/graph_contract.py, app/workflow/run_graph.py, .planning/phases/01-data-architecture-research/langgraph-contract.md</files>
  <read_first>app/artifacts/workflow_artifacts.py, app/workflow/graph_contract.py, app/workflow/run_graph.py, .planning/phases/01-data-architecture-research/langgraph-contract.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/ARCHITECTURE_STACK.md, .planning/phases/01-data-architecture-research/01-RESEARCH.md, app/artifacts/source_cards.py, app/retrieval/hybrid_retrieval.py</read_first>
  <behavior>
    - Test 1: Typed artifacts cover intent/triage, research design, evidence bundle, coverage, extraction plan, dataset artifact, methodology note, visualization spec, critique, final answer, feedback artifact, source rejection, and trace event.
    - Test 2: The graph implementation names the supervisor, the three scouts, coverage/schema, extraction planner, deterministic tools, critic, narrator, and visualization roles from D-14.
    - Test 3: The graph describes and implements at least the narrow runnable path for representative lookup/research/no-data cases, while preserving skip/fan-out routing rules for direct and complex queries.
    - Test 4: The graph defines per-node budgets/tool scopes for direct lookup, ambiguous lookup, comparative query, research query, and no-data check.
    - Test 5: `app/workflow/run_graph.py` can run at least one representative golden case with trace events and a final typed artifact, or returns an explicit gated status when index/credentials are unavailable.
    - Test 6: Graph verification fails if trace output lacks selected sources, rejected candidates or no-data reasoning, coverage/extraction planning, and Qdrant/index status.
  </behavior>
  <action>Create `app/artifacts/workflow_artifacts.py` with Pydantic models or typed structures for `IntentFrame`, `ResearchDesignArtifact`, `CoverageReport`, `ExtractionPlan`, `DatasetArtifact`, `MethodologyNote`, `VisualizationSpec`, `CritiqueReport`, `FinalAnswer`, `FeedbackArtifact`, source rejection records, and the canonical `TraceEvent`. `TraceEvent` ownership must stay in `app/artifacts/workflow_artifacts.py`; do not redefine it in UI modules. Create `app/workflow/graph_contract.py` with node names, state shape, routing comments matching D-13 through D-15, per-node budgets/tool scopes, and explicit import/usage of the workflow-owned `TraceEvent` for emitted trace payloads. Create `app/workflow/run_graph.py` as a runnable narrow LangGraph entrypoint that wires the representative product path over the prepared retrieval/extraction contracts and emits trace events; it must not be a prose-only skeleton. It must expose a CLI path that can run a golden case and emit JSON with trace events, selected/rejected sources, coverage/extraction status, Qdrant/index status, final answer or gated status, and no unsupported numeric values. Write `langgraph-contract.md` with sections named exactly `Supervisor`, `Parallel scouts`, `Coverage and extraction`, `Critic and narrator`, `Visualization and final answer`, `Budgets and tool scopes`, and `Checkpoint and trace rules`. The document must state that this is an executable Phase 1 slice, not a bypass around plan verification, and it must note that Streamlit trace payloads are fed from `run_graph.py`/`graph_contract.py` through `workflow_artifacts.py` rather than a duplicate UI-local trace schema.</action>
  <acceptance_criteria>`rg -n "Supervisor|Parallel scouts|Coverage and extraction|Critic and narrator|Visualization and final answer|Budgets and tool scopes|Checkpoint and trace rules|runnable|executable" .planning/phases/01-data-architecture-research/langgraph-contract.md` returns required sections, `rg -n "class TraceEvent|IntentFrame|CoverageReport|ExtractionPlan|DatasetArtifact|VisualizationSpec|FinalAnswer|FeedbackArtifact" app/artifacts/workflow_artifacts.py` returns matches, `rg -n "from app\\.artifacts\\.workflow_artifacts import .*TraceEvent|TraceEvent|tool_scope|budget|Direct lookup|Research query" app/workflow/graph_contract.py` confirms the graph contract consumes the canonical trace model and budgets, and `python -m app.workflow.run_graph ...` returns machine-readable trace/final or gated evidence for at least one representative golden case.</acceptance_criteria>
  <verify>
    <automated>rg -n "Supervisor|Parallel scouts|Coverage and extraction|Critic and narrator|Visualization and final answer|Budgets and tool scopes|Checkpoint and trace rules|runnable|executable" .planning/phases/01-data-architecture-research/langgraph-contract.md && rg -n "class TraceEvent|IntentFrame|CoverageReport|ExtractionPlan|DatasetArtifact|VisualizationSpec|FinalAnswer|FeedbackArtifact" app/artifacts/workflow_artifacts.py && rg -n "from app\\.artifacts\\.workflow_artifacts import .*TraceEvent|TraceEvent|tool_scope|budget|Direct lookup|Research query" app/workflow/graph_contract.py && rg -n "build_graph|invoke|TraceEvent|IntentFrame|DatasetArtifact|gated|selected_sources|rejected_sources|qdrant" app/workflow/run_graph.py && python -m app.workflow.run_graph --goldens .planning/phases/01-data-architecture-research/golden-cases.yaml --case-index 0 --index-manifest .planning/phases/01-data-architecture-research/embedding-index-manifest.json --json-output .planning/phases/01-data-architecture-research/run-graph-smoke.json && python - <<'PY'
import json
from pathlib import Path
data = json.loads(Path('.planning/phases/01-data-architecture-research/run-graph-smoke.json').read_text())
assert data.get('status') in {'ok', 'gated'}
assert data.get('trace_events')
assert data.get('qdrant_status')
assert data.get('selected_sources') or data.get('no_data_reason') or data.get('status') == 'gated'
assert 'unsupported_numeric_claim' not in json.dumps(data)
print(data['status'])
PY</automated>
  </verify>
  <done>The phase contains a runnable narrow LangGraph flow with a single canonical TraceEvent owner that downstream UI work can reuse without contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prepare deterministic extraction tools/probes and Streamlit trace payloads while indexing runs</name>
  <files>.planning/phases/01-data-architecture-research/extraction-probes.md, app/data/deterministic_tools.py, scripts/run_extraction_probes.py, app/ui/trace_models.py, .planning/phases/01-data-architecture-research/trace-ui-demo.md</files>
  <read_first>.planning/phases/01-data-architecture-research/extraction-probes.md, app/data/deterministic_tools.py, scripts/run_extraction_probes.py, app/ui/trace_models.py, .planning/phases/01-data-architecture-research/trace-ui-demo.md, .planning/phases/01-data-architecture-research/01-CONTEXT.md, .planning/DATA_REPORT.md, .planning/phases/01-data-architecture-research/golden-cases.yaml, app/data/source_card_builders.py, app/artifacts/workflow_artifacts.py, app/workflow/graph_contract.py</read_first>
  <behavior>
    - Test 1: FedStat probe uses a DuckDB SQL-first path per D-09, shows wide-table normalization evidence per D-10, and records PyArrow/Polars usage or explicit non-usage rationale.
    - Test 2: World Bank probe uses a DuckDB SQL-first path per D-09, shows indicator/country/period coverage, and records canonical long-format adapter evidence per D-11.
    - Test 3: CKAN probe shows resource-level access path or explicit bounded skip evidence when a resource is unsuitable, without bypassing the SQL-first extraction contract.
    - Test 4: Deterministic tool contracts expose safe operations named `fedstat_normalize_preview`, `wb_coverage_preview`, `ckan_package_search`, `ckan_package_show`, `run_duckdb_query`, `build_dataset_artifact`, `export_csv_parquet_manifest`, and visualization rendering from `DatasetArtifact`.
    - Test 5: UI models import canonical workflow `TraceEvent` and represent index states: building, ready, stale, and gated_skip.
    - Test 6: Probe verification executes the probe runner and fails when probes only contain prose markers without machine-readable coverage/extraction evidence.
  </behavior>
  <action>Create `app/data/deterministic_tools.py` with safe tool contracts for `fedstat_normalize_preview`, `wb_coverage_preview`, `ckan_package_search`, `ckan_package_show`, `run_duckdb_query`, `build_dataset_artifact`, `export_csv_parquet_manifest`, and deterministic visualization rendering from `DatasetArtifact`/`VisualizationSpec` using Altair or Plotly where available. Create `scripts/run_extraction_probes.py` to inspect one representative FedStat wide parquet path, one World Bank parquet path, and one CKAN resource path chosen from the source cards. The probes must be DuckDB SQL-first per D-09, use PyArrow/source adapters where normalization is required, and use Polars only where useful with an explicit rationale when used or not used. The runner must write both `extraction-probes.md` and a machine-readable JSON evidence file with source family, source id, coverage status, extraction status, row/column shape when available, query/script artifact path, and gated/skip reason when applicable. Create `app/ui/trace_models.py` with typed diagnostic UI payloads for `FeedbackRequest`, `FixRequest`, and one or more view-model wrappers that import and reuse `TraceEvent` from `app/artifacts/workflow_artifacts.py`. Write `trace-ui-demo.md` with sections named exactly `State machine`, `Trace timeline`, `Artifacts panel`, and `Feedback and fix requests`, including index build/ready/gated-skip states. Keep UI work minimal and diagnostic; do not optimize visual polish in this plan.</action>
  <acceptance_criteria>`rg -n "^## FedStat wide Parquet probe$|^## World Bank parquet probe$|^## CKAN resource path probe$|FedStat normalizer evidence|World Bank canonical long-format adapter evidence|DuckDB SQL-first|PyArrow|Polars" .planning/phases/01-data-architecture-research/extraction-probes.md` returns all required section headers and evidence markers, `rg -n "fedstat_normalize_preview|wb_coverage_preview|ckan_package_search|ckan_package_show|run_duckdb_query|build_dataset_artifact|export_csv_parquet_manifest|DatasetArtifact|VisualizationSpec|Altair|Plotly" app/data/deterministic_tools.py` confirms deterministic tool contracts, `rg -n "from app\\.artifacts\\.workflow_artifacts import TraceEvent|class FeedbackRequest|class FixRequest|class WorkflowTraceViewModel|gated_skip|building|ready|diagnostic" app/ui/trace_models.py` confirms trace UI models, and automated verification runs `scripts/run_extraction_probes.py` and validates machine-readable coverage/extraction evidence.</acceptance_criteria>
  <verify>
    <automated>rg -n "^## FedStat wide Parquet probe$|^## World Bank parquet probe$|^## CKAN resource path probe$|FedStat normalizer evidence|World Bank canonical long-format adapter evidence|DuckDB SQL-first|PyArrow|Polars" .planning/phases/01-data-architecture-research/extraction-probes.md && rg -n "fedstat_normalize_preview|wb_coverage_preview|ckan_package_search|ckan_package_show|run_duckdb_query|build_dataset_artifact|export_csv_parquet_manifest|DatasetArtifact|VisualizationSpec|Altair|Plotly" app/data/deterministic_tools.py && rg -n "from app\\.artifacts\\.workflow_artifacts import TraceEvent|class FeedbackRequest|class FixRequest|class WorkflowTraceViewModel|gated_skip|building|ready|diagnostic" app/ui/trace_models.py && rg -n "State machine|Trace timeline|Artifacts panel|Feedback and fix requests|index" .planning/phases/01-data-architecture-research/trace-ui-demo.md && python scripts/run_extraction_probes.py --source-catalog-manifest .planning/phases/01-data-architecture-research/source-catalog-manifest.json --report .planning/phases/01-data-architecture-research/extraction-probes.md --json-output .planning/phases/01-data-architecture-research/extraction-probes.json && python - <<'PY'
import json
from pathlib import Path
data = json.loads(Path('.planning/phases/01-data-architecture-research/extraction-probes.json').read_text())
families = {item.get('source_family') for item in data.get('probes', [])}
assert {'FedStat', 'World Bank', 'CKAN'}.issubset(families), families
for probe in data['probes']:
    assert probe.get('coverage_status') in {'ok', 'gated', 'skipped_with_reason'}
    assert probe.get('extraction_status') in {'ok', 'gated', 'skipped_with_reason'}
    assert probe.get('source_id')
print(len(data['probes']))
PY</automated>
  </verify>
  <done>Extraction, workflow, and UI contracts are ready to consume the prepared index without waiting idly for embedding to finish.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add executable data relevance and extraction evaluation</name>
  <files>app/evals/run_eval.py, .planning/phases/01-data-architecture-research/data-relevance-eval.json, .planning/phases/01-data-architecture-research/data-relevance-eval.md</files>
  <read_first>app/evals/run_eval.py, .planning/phases/01-data-architecture-research/golden-cases.yaml, .planning/phases/01-data-architecture-research/eval-rubric.md, .planning/phases/01-data-architecture-research/retrieval-eval.csv, .planning/phases/01-data-architecture-research/extraction-probes.json, .planning/phases/01-data-architecture-research/embedding-index-manifest.json, app/workflow/run_graph.py, app/retrieval/hybrid_retrieval.py, app/data/deterministic_tools.py</read_first>
  <behavior>
    - Test 1: The eval runner loads golden cases and checks source-family relevance, top-candidate relevance, rejection reasons, Qdrant/dense status, coverage evidence, extraction evidence, no-data honesty, and trace completeness.
    - Test 2: The eval runner fails cases with unsupported numeric claims or numeric outputs lacking dataset/source/provenance evidence.
    - Test 3: Gated embedding/Yandex states are recorded explicitly and do not count as retrieval or extraction success.
  </behavior>
  <action>Create `app/evals/run_eval.py` as the Phase 1 executable quality gate. It must consume `golden-cases.yaml`, `retrieval-eval.csv`, `extraction-probes.json`, `embedding-index-manifest.json`, and representative `run_graph.py` outputs where available. It must write `data-relevance-eval.json` and `data-relevance-eval.md` with per-case pass/fail/gated results and aggregate counts. The scoring should prioritize correct data relevance and deterministic extraction over UI or final-answer polish.</action>
  <acceptance_criteria>`rg -n "golden-cases|retrieval-eval|extraction-probes|embedding-index-manifest|relevance|unsupported numeric|qdrant|coverage|trace" app/evals/run_eval.py` returns all concepts, and `python -m app.evals.run_eval ...` writes JSON/markdown reports with per-case statuses and no silent success for gated components.</acceptance_criteria>
  <verify>
    <automated>rg -n "golden-cases|retrieval-eval|extraction-probes|embedding-index-manifest|relevance|unsupported numeric|qdrant|coverage|trace" app/evals/run_eval.py && python -m app.evals.run_eval --goldens .planning/phases/01-data-architecture-research/golden-cases.yaml --retrieval-eval .planning/phases/01-data-architecture-research/retrieval-eval.csv --extraction-probes .planning/phases/01-data-architecture-research/extraction-probes.json --index-manifest .planning/phases/01-data-architecture-research/embedding-index-manifest.json --json-output .planning/phases/01-data-architecture-research/data-relevance-eval.json --markdown-output .planning/phases/01-data-architecture-research/data-relevance-eval.md && python - <<'PY'
import json
from pathlib import Path
data = json.loads(Path('.planning/phases/01-data-architecture-research/data-relevance-eval.json').read_text())
assert data.get('total_cases', 0) > 0
assert 'passed' in data and 'failed' in data and 'gated' in data
assert data.get('qdrant_status') in {'ready', 'gated_skip'}
assert all('case_id' in item and 'status' in item for item in data.get('cases', []))
print(data['total_cases'], data['qdrant_status'])
PY</automated>
  </verify>
  <done>Phase 1 has an executable data-relevance and deterministic-extraction quality gate before the diagnostic UI/demo package is considered ready.</done>
</task>

</tasks>

<verification>
Check that the Yandex client matches the verified endpoint/auth path, the graph contract names the full locked role set with budgets/tool scopes and explicit routing/trace rules, deterministic tools expose safe DatasetArtifact/VisualizationSpec operations, extraction probes execute over all three source families, the eval harness scores data relevance/extraction/trace evidence, and trace UI models expose prepared-index state without turning UI polish into a priority.
</verification>

<success_criteria>
Phase 1 has validated the LLM/orchestration/extraction/evaluation layer deeply enough that final integration can use the chosen graph, Qwen path, Qdrant retrieval, deterministic tools, and prepared index without renewed architecture shopping or UI-polish detours.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-architecture-research/01-04-SUMMARY.md`
</output>
