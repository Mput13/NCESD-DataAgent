---
context: phase
phase: 02-jury-mvp
task: post-merge-fixes
status: paused
last_updated: "2026-05-12T00:15:00Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. All fixes are in main._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Number verifier false rejects | assert_message_numbers_are_supported rejects valid answers when LLM rounds/abbreviates numbers (e.g. "1376 млрд" vs raw value 1376477.9) | advisory | Before fixing, check exact rejected numbers in response detail |
| LLM nondeterminism in extraction | GC-004 passes sometimes but not others — extraction_planner filters vary per run | advisory | Add temperature=0 to extraction_planner LLM call if not already set |

<current_state>
Branch codex/phase-2-jury-mvp-planning merged to main and pushed (0887e43). Web UI running on :8787. All 50 finalization tests green. Phase 2 pipeline works end-to-end (GC-004 passed in run 4). Main remaining gaps: GC-001 number verifier edge case, GC-004 nondeterminism, cross-language retrieval (Plan C), SSE hang.
</current_state>

<completed_work>

This session:
- 3 parallel agents: BL-03 (skip empty dataset export), test error string alignment, WR-06 (WB countries filter)
- WB country_id fix: parquet uses 2-letter codes (RU/KZ/CN), _resolve_countries returns ISO3 (RUS/KAZ/CHN) — fixed to use countryiso3code
- Period sanitization: "последние доступные годы" was passed as literal filter → 0 rows; fixed in coverage.py and _resolve_wb_periods
- Narrator coercion: limitations string→list
- Number verifier: Russian space-thousands format "1 376 477,9" now collapsed before matching
- Web frontend: pulled app/web/ UI overhaul from remote + added CSV download endpoint + sidebar button
- Merged to main and pushed
</completed_work>

<remaining_work>

1. **GC-001 not_found** (HIGH): Dataset has 2 rows (status=ok), but number verifier rejects. Likely LLM abbreviates "1 376 477,9 млрд" which becomes "1376477.9 млрд" = narrative uses "млрд" scale (×10^9) but raw value is already in billions... check exact rejected number.

2. **GC-004 nondeterministic** (HIGH): Sometimes passes (run 4), sometimes not_found. Extraction sometimes gets only CHN rows. Check temperature and extraction_planner country filter handling.

3. **SSE hang** (MEDIUM): UI sometimes stays on "Думает" — final SSE event not delivered. Check server.py _handle_stream finally block.

4. **Plan C — cross-language retrieval** (MEDIUM): GC-015/GC-017 get 0 sources because Russian queries don't match English WB source cards. Need bilingual embedding or query translation.

5. **Full 20-case run** (HIGH): Only tested 7/20 golden cases. Need to measure actual pass rate across all 20.
</remaining_work>

<decisions_made>

- Pulled only app/web/ from remote merge commit — backend changes would conflict with our fixes
- Web server uses PYTHONPATH=. python3 app/web/server.py --port 8787
- All fixes on codex/phase-2-jury-mvp-planning, now merged to main
</decisions_made>

<blockers>

- GC-001 number verifier: exact mismatch unknown — need to instrument assert_message_numbers_are_supported to log what it rejects
- GC-004 nondeterminism: extraction country filter depends on LLM temperature
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable task list
2. `app/workflow/nodes/narrator.py` lines 56-125 — assert_message_numbers_are_supported logic

## Infrastructure State
- Web server: `PYTHONPATH=. python3 app/web/server.py --port 8787`
- Qdrant: running on localhost:6333
- Credentials: YANDEX_API_KEY etc in .env
- Branch: main (merged), working branch was codex/phase-2-jury-mvp-planning

<context>
Spent session fixing WB pipeline infrastructure (ISO3 codes, period sanitization, narrator coercion). Got first `passed` on GC-004. Number verifier is the main remaining quality gate issue. Web frontend now has CSV download. All changes merged to main.
</context>

<next_action>
Start with: Debug GC-001 — add print/logging to assert_message_numbers_are_supported to see exact rejected numbers. Run: `PYTHONPATH=. python3 -c "import app.workflow.nodes.narrator as n; orig = n.assert_message_numbers_are_supported; ..."` or just run single GC-001 batch and check response.message for the actual numbers being rejected.
</next_action>
